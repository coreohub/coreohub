# ─── Playwright screenshots (Definition of Done workflow) ───────────────────
# Copie pra .env.local (ignorado pelo git) e preencha.

PLAYWRIGHT_BASE_URL=https://app.coreohub.com
PLAYWRIGHT_EMAIL=
PLAYWRIGHT_PASSWORD=

# ─── Frontend (já configurado) ──────────────────────────────────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
