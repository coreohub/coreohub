# CoreoHub — Histórico de sessões

Movido do CLAUDE.md em 2026-09-24 para reduzir o custo fixo de contexto. Texto original, sem alterações. Cronológico inverso.

Cronológico inverso. Detalhes individuais em `memory/`.

### 2026-09-25 (continuação 4) — Plano C: equipe (operadores) vale para todas as sessões do mesmo espetáculo ✅ DEV (commit `bb8d6c8`); migration e 3 edge functions JÁ EM PRODUÇÃO — 🚧 pendente merge em main

Detalhes em `memory/plano_c_equipe_por_grupo_de_sessoes_shipado_2026_09_25.md`.

- **Regra única no banco:** `team_event_ids(uid)` = evento vinculado (`profiles.team_event_id`) + sessões com o MESMO `session_group_id` e o MESMO `created_by` (sem a trava de produtor, o produtor B poderia puxar evento próprio pro grupo do A). Só devolve a lista do próprio chamador (ou service_role). Migration `20260929_team_access_by_session_group.sql`.
- **Trocado nas policies** de `events`, `audience_tickets`, `workshop_registrations`, `stage_timings` e nas funções `is_team_member_of_event` (também afeta `registrations`) e `caller_can_checkin_teammate`. Policies de `profiles` NÃO mudaram (só comparam o vínculo do colega). Trigger `protect_events_session_group` impede UPDATE direto de `session_group_id`.
- **Edge functions:** `_shared/team-access.ts`; `create-pdv-ticket` (v16), `delete-registration` e `revoke-expired-team-access` deployadas. A revogação agora expira pela ÚLTIMA sessão do grupo (decisão do produtor); grupo com alguma sessão sem data fica fora (conservador).
- **Frontend:** `fetchMyTeamEventIds`/`ownedOrTeamEventsFilter` em `services/supabase.ts`; `Schedule`, `StageMarker` e `VendasIngressos` listam as sessões irmãs pra equipe.
- **Validação:** smoke `BEGIN/ROLLBACK` com produtor B simulado + evento do mesmo produtor fora do grupo (0 vazamento). No sandbox, operador vinculado à sessão 2 vendeu no PDV nas sessões 3 e 5, viu as 5 no seletor, fez check-in em sessão irmã, não viu inscrições reais; após revogar, PDV deu 403. 13/13; vínculo revertido; baseline de produção idêntico (49/21/125/0 tickets/0 equipe).
- **⚠️ Achado, não corrigido:** `Credenciais`, `Registrations`, `Avisos`, `Cupons` e `Certificados` listam eventos só por `created_by` — equipe não vê seletor nelas (lacuna anterior às sessões). Além disso, o bypass `current_user IN ('postgres', …)` do padrão `protect_*` não funciona dentro de função SECURITY DEFINER (current_user vira o dono); o trigger novo é SEM definer de propósito. Vale auditar os `protect_*` existentes.

### 2026-09-25 (continuação 3) — Taxa fixa de plano (Essencial/Escala): 7 dias de tolerância + fatura refeita a cada clique ✅ DEV (commit `6756656`); banco e edge functions JÁ EM PRODUÇÃO — 🚧 pendente só merge em main + revisão do Termo

Detalhes em `memory/taxa_plano_tolerancia_7_dias_2026_09_25.md`.

- **Caso Lorrayne (Vicenza Dance Camp 2027):** ela tinha 2 faturas abertas pro mesmo evento (`pay_nocsi…` de 14/09 e `pay_4gwe…` de 18/09) e o webhook só reconhecia a salva no evento — pagar a outra deixaria o dinheiro entrar sem liberar o plano. Evento reapontado pra `pay_4gwe3yxuckmr5w3f` (com o trigger de proteção desligado só durante o UPDATE, mesmo padrão do 20260920); vencimento reaberto no painel Asaas pra 02/10/2026. "Lyris Dance Competition 2027" voltou pra Começo (10%) e o de 2026 (sem movimento) foi apagado.
- **Modelo novo:** evento nasce JÁ no plano escolhido com a taxa pendente (`billing_plan_fixed_fee_paid_at` NULL); prazo em `events.billing_plan_fee_due_at` (migration `20260925_plan_fee_due_at.sql`, coluna protegida). Só depois do prazo o painel trava.
- **`create-plan-fixed-fee-payment`:** idempotente (fatura em aberto reaproveitada; vencida/sumida apagada e refeita com vencimento +3 dias, sem estender o prazo do gate), 1ª fatura vence em +7 dias, boleto com `daysAfterDueDateToRegistrationCancellation: 0`, grava o plano na hora, migrada pro helper de ambiente (sandbox por evento, falha fechada — saiu da lista `LEGACY_DIRECT_READERS`).
- **`asaas-webhook`:** confirma pelo `externalReference`, não pelo id salvo; segundo pagamento do mesmo evento vira noop com log.
- **`PlanFeeGateModal`:** só "Pagar agora"; trava sem dispensar; reconsulta a cada 6s e destrava sozinho quando o webhook grava `paid_at`. "Descontar do meu saldo" saiu da UI (edge `deduct-plan-fee-now` e aviso do SuperAdmin ficaram).
- **Validado no sandbox** (conta `teste.produtor@coreohub.com`, eventos descartáveis já apagados): fatura nova/reaproveitada/plano diferente recusado/fatura sumida refeita; boleto de 0 dia aceito pela Asaas (volta `daysAfterDueDateToRegistrationCancellation: 0`); **webhook de ponta a ponta** (`POST /v3/sandbox/payment/{id}/confirm` → `paid_at` gravado em ~13s). Achado: o webhook ignorava todo sandbox que não fosse ingresso (`sandbox_ref_not_supported`) — agora aceita PLANFEE de evento `payment_sandbox=true` (commit `bc919e3`, mesma checagem de ambiente dos ingressos); `receiveInCash` NÃO serve pra simular (status `RECEIVED_IN_CASH` não está no STATUS_MAP).
- **Bloqueio de venda (commit `1110e23`, deployado nas 7 edges):** RPC `plan_fee_sales_blocked` (migration `20260925b`, regra única no banco) + `_shared/plan-fee-gate.ts` (falha aberta) em create-payment-asaas, create-aggregate-payment-asaas, create-audience-ticket, create-workshop-registration, create-workshop-pass-registration, create-video-selection-payment e create-pdv-ticket. Evento do fluxo novo fica fechado até pagar; evento anterior (Vicenza) só fecha depois do vencimento. Validado em produção com evento sandbox: pendente → "As vendas deste evento ainda não foram abertas…", paga → passa do bloqueio. Inscrição GRATUITA feita direto pelo cliente não passa por edge e não é bloqueada (sem receita, sem perda).
- **Aviso durante a tolerância (commit `6249a98`):** `components/PlanFeeReminderBanner.tsx` + `hooks/usePlanFeePending.ts` (compartilhado com o modal). Validado no sandbox em desktop e mobile.
- **Termo do Produtor v1.6 (commit `58cb0bb`):** cláusula 4-bis (taxa fixa, 7 dias, vendas indisponíveis, bloqueio do painel, multa 2% + juros 1% a.m.). **Redação a revisar juridicamente antes do merge.** Subir a versão exige que TODOS os produtores re-aceitem. Multa/juros prontos em `create-plan-fixed-fee-payment` (`APPLY_LATE_FEES`) mas DESLIGADOS até o Termo ir pra produção — ligar e redeployar a função no merge.
- **Decisões do produtor (fim do dia):** texto da cláusula 4-bis aprovado; evento não pago fica bloqueado a partir do vencimento e pode ser excluído 60 dias depois do vencimento (aviso por e-mail; nunca evento com venda); taxa não reembolsável, exceto desistência em 7 dias sem publicação nem venda (pesquisa: mercado trata setup fee como não reembolsável depois que o trabalho começou; CDC art. 49 dá 7 dias de arrependimento em contratação a distância). Commit `9481695`.
- **Multa/juros ligados, mas só pra quem aceitou o Termo ≥ 1.6** (`termsVersionAtLeast`, `profiles.producer_terms_version`); se a Asaas recusar o formato a fatura sai sem multa. Validado no sandbox: sem aceite `fine.value=0`; com aceite `fine {2, PERCENTAGE}` + `interest {1}`. Deployado. Hoje ninguém aceitou a 1.6, então nenhuma fatura sai com multa até o merge + aceite.
- **Só a Lorrayne precisa aceitar o Termo 1.6.** O aceite só é exigido ao conectar a conta Asaas (AccountSettings) — subir a versão não bloqueia quem já opera. 4 produtores estão na v1.3 (Lyris, Ecodança, Usualdance, Extreme) e 1 na v1.5 (Tamoios).
- **Não implementado:** exclusão automática de evento não pago (recomendado: rotina que só AVISA o admin e exclusão manual via `delete-event`); exigir aceite do Termo v1.6 ao escolher plano pago no wizard (sem isso a 4-bis não vale pra quem cria evento novo).
- **Pendente:** (1) merge dev → main — em produção o modal antigo (2 botões, gate de 1h) e o Termo 1.5 seguem até lá; (2) enviar à Lorrayne o link do Termo pra ela aceitar a 1.6 depois do merge.

### 2026-09-25 (continuação 2) — E-mail de estorno de ingresso + PDV com acompanhante ✅ DEPLOYADO em produção (send-email v71, refund-audience-ticket v26, create-pdv-ticket v14) — 🚧 falta confirmar a chegada dos e-mails

**Deploy feito no mesmo dia (a pedido do produtor, "se recomenda, pode deployar"):** a recomendação mudou de "esperar 27/09" para "deployar agora" depois de conferir o banco: nenhum evento real tem ingresso vendido (0 no total, 0 nos últimos 7 dias), o Extreme (26/09) é GRATUITO com venda desligada e nenhum evento real usa mapa de assentos. Flags de JWT preservadas (send-email sem JWT; as outras duas com JWT). **Validação pós-deploy no sandbox:** compra API → pagamento com cartão de teste (function temporária `temp-sandbox-pay`, já apagada) → webhook APROVADO → estorno pela API como produtor de teste: `200`, `emails: {buyer:true, producer:true}`, assento voltou `livre`; PDV Pix com PCD + acompanhante: 201, 2 ingressos numa cobrança só (R$ 43,16), QR, assentos M-8 cadeirante / M-7 acompanhante (mandados em ordem trocada). O primeiro teste do Pix falhou porque o PDV sempre mandava `split` e a subconta sandbox está bloqueada; `create-pdv-ticket` ganhou o mesmo `ASAAS_SANDBOX_SKIP_SPLIT` (só sandbox) do `create-audience-ticket`, e o rollback do ingresso funcionou (nenhum ticket órfão). **Falta:** o produtor confirmar no `contato@coreohub.com` a cópia "[SANDBOX] [Cópia] Estorno realizado" e, no Gmail `coreohub+sandbox@`, o e-mail "Estorno do seu ingresso". Baseline de produção: comm 49, pay 21, regs 125, tickets reais 0, **eventos reais 6** (era 7; um evento real de teste sumiu durante o dia por ação fora desta sessão — provável limpeza do "TESTE PRESENCIAL").

Detalhes em `memory/estorno_email_e_pdv_acompanhante_2026_09_25.md`. Commits `f5fae3e` (A) e `e142fb2` (B) na `dev`.

- **Achado (print de e-mail do produtor):** o estorno de ingresso feito pelo produtor (`refund-audience-ticket`) NÃO enviava e-mail a ninguém (só o estorno automático por pagamento tardio enviava). Boa prática (Sympla avisa o participante por e-mail em cancelamento/reembolso).
- **A. E-mail de estorno:** templates novos `audience_ticket_refunded` (comprador: evento, sessão, ingressos com lugar, valor, motivo, aviso de que o QR deixou de valer, botão "Ver o evento") e `audience_ticket_refunded_producer` (comprador, ingressos, valor, comissão devolvida) no `send-email`; `refund-audience-ticket` dispara os dois depois do estorno, best-effort, 1 e-mail por pedido (group_id). **Cópia fixa de TODO estorno para o admin** via `sendAdminCopy` → `ADMIN_NOTIFY_EMAIL` (padrão `contato@coreohub.com`, mesma convenção dos avisos de novo produtor/lead; `coreohub@gmail.com` é só o login super admin/conta master Asaas). O e-mail de pagamento tardio também ganhou cópia ao admin e o botão aceita `eventoUrl`. **Pendente:** passar `eventoUrl` no `asaas-webhook` (select `slug` + payload) depois que a outra sessão (correção do plano fixo/PLANFEE) commitar o arquivo — as 2 linhas foram removidas do working tree para não misturar.
- **B. PDV com acompanhante:** pesquisa (Ticketmaster/Bilheteria Digital/Sympla vendem PCD + acompanhante no mesmo pedido, acompanhante com meia; EUA: bilheteria bloqueia lugares juntos, alguns locais liberam até 3). `create-pdv-ticket` aceita `items[]` (1 item segue o caminho antigo v1, vários usam `try_reserve_audience_tickets_v2` sob 1 group_id), meia-entrada por venda inteira, e `alignSeatsToItems` (`_shared/seat-rules.ts`, testado) alinha os assentos por tipo no servidor. Tela do balcão (`VendasIngressos.tsx`): oferta "Vai com acompanhante?" quando 1 PCD escolhe assento especial com vizinho livre; tirar o PCD leva o acompanhante junto; payload `items` + `seat_ids`.
- **Validação:** função rodada localmente com Deno contra o banco (só evento sandbox, `cartao_tap`): caminho antigo intacto, PCD+acompanhante com assentos em ordem trocada mapeados certo (M-8 cadeirante → PCD, M-7 → acompanhante), recusas corretas; UI por Playwright (desktop e mobile, chamada interceptada): oferta, contador 2/2, desmarcar, payload; 137 testes, lint e `deno check` ok. **Falta:** Pix multi-item e o E2E dos e-mails (só possível após o deploy).
- **Limite conhecido:** oferta de acompanhante só para venda de 1 ingresso PCD (quantidade 1).

### 2026-09-25 (continuação) — Fase 3: matriz de testes pela tela nas sessões do sandbox ✅ DEV — 🚧 Fase 4 e Fase 5 pendentes

Detalhes em `memory/fase3_testes_sandbox_sessoes_2026_09_25.md`. Tudo no sandbox (evento `41d933c9…`, 5 sessões), nenhum evento real tocado; baseline de produção intacto (comm 49, pay 21, regs 125, eventos reais 7, tickets reais 0).

- **Compra pela tela (Playwright no checkout público):** 2 comuns na sessão 2 (A-5/A-6), o mesmo A-5 na sessão 4 sem conflito (reservado nas duas), comum em assento de cadeirante recusado com aviso, PCD + acompanhante (M-8 → oferta "Vai com acompanhante?" → M-7) aprovado, acompanhante sozinho recusado, assento vendido aparece "ocupado". Hold de assento expirou sozinho (CANCELADO) como esperado.
- **Pagamento e estorno:** pedido PCD+acompanhante (R$ 43,16) pago no sandbox com cartão de teste; o webhook aprovou e os assentos ficaram `vendido`; estorno pela tela do produtor (grupo inteiro) marcou ESTORNADO e devolveu M-8 (cadeirante) e M-7 (acompanhante) como `livre` com o tipo certo. O estorno de ingresso funciona no sandbox (o "saldo insuficiente" real em produção continua sendo o problema de split, sem chamado na Asaas).
- **Corte de 24 h (art. 23-A do Decreto 5.296):** `seat_general_release_open` só abre com as duas condições (falta < 24 h E comuns esgotados): perto+comuns livres = fechado, longe+esgotado = fechado, perto+esgotado = aberto; nesse último estado a tela deixou um ingresso comum escolher M-15 (cadeirante) e o servidor aceitou (201). Sessão 4 restaurada depois.
- **PDV (produtor de teste, `cartao_tap`):** comum B-3, PCD em M-16 (assento comum B-4 recusado antes) e acompanhante em M-17 vendidos; lista mostra "Lugar M-16 · PCD" e "Lugar M-17 · Acompanhante". Operador com login de equipe separado NÃO foi testado (só o produtor).
- **Correções:** mensagem de assento comum num pedido só de PCD/acompanhante ("Este pedido é de ingresso PCD ou de acompanhante…", antes dizia "já escolheu os lugares dos ingressos comuns"); `EventPickerSheet` acrescenta "· dd/mm/aaaa HH:MM" quando dois eventos da lista têm o mesmo nome (sessões duplicadas sem rótulo eram indistinguíveis); 134 testes.
- **Achados abertos:** a fatura da Asaas mostra o cliente antigo ("Teste da Silva / contato.estudio.cla@gmail.com") quando o CPF já existe como cliente com outro nome/e-mail (provável reuso de cliente por CPF em `create-audience-ticket`; conferir antes de produção); PDV vende PCD e acompanhante como duas vendas separadas (sem a oferta "Vai com acompanhante?" do checkout); automatizar a fatura do sandbox pela tela é frágil (autocomplete de cidade), então o pagamento de teste usou uma function temporária (`temp-sandbox-pay`, já apagada do ambiente); render do mapa lado a lado com as imagens do Guichê NÃO feito (imagens não foram reenviadas nesta sessão).

### 2026-09-25 — Fase 3, etapa 4: sessões do mesmo espetáculo no mesmo local ✅ DEV (staging) — 🚧 matriz E2E das 4 sessões pendente

Detalhes em `memory/sessoes_mesmo_local_etapa4_2026_09_25.md`. Commit `bcf64c2` na `dev`; migration `20260928_event_sessions.sql` já aplicada em produção (só adiciona coluna/funções e troca o guard do sandbox por versão compatível).

- **Modelo**: cada sessão é um evento próprio (padrão Guichê Web/Sympla/Diversos Ingressos, spec pendência 3). `events.session_group_id` liga as irmãs, sem tabela nova; a origem vira âncora do grupo na 1ª duplicação. `event_seats` já é por evento, então o mesmo assento em sessões diferentes não conflita.
- **`duplicate_event_session(evento, data, hora, nome?)`** (só dono/super admin): copia local, ingressos, preços, regras, comissão e textos da `configuracoes`; NÃO copia vendas, cupons nem taxa fixa paga; recusa data passada, hora inválida e data+hora repetida no grupo; slug `<base>-AAAA-MM-DD-HHMM` (sufixo numérico se colidir); gera os assentos da nova data. Sessão de evento sandbox nasce sandbox: o guard `guard_events_payment_sandbox` aceita o INSERT só quando a função marca (`app.session_clone_from`, na própria transação) uma origem que já é sandbox do mesmo dono.
- **`get_event_sessions_public`** + seção "Sessões" na vitrine (`PublicEventPage.tsx`, âncora "Sessões", só com 2+ sessões públicas; cada sessão mantém URL e JSON-LD `Event` próprios); cartão "Sessões / Duplicar como nova sessão" em Configurações → Geral (`components/EventSessionsCard.tsx`, só espetáculo ou mapa de assentos). Sitemap não precisou mudar (já lista cada evento por slug).
- **Validação**: smoke SQL em BEGIN/ROLLBACK (grupo, slug, sandbox herdado, 373 assentos/26 especiais por sessão, recusas), 4 sessões reais criadas no sandbox (12/12 19:30, 13/12 20:00, 19/12 19:30, 20/12 18:00), Playwright desktop+mobile na vitrine e no cartão (0 erros, sem overflow), 133 testes, lint e build ok. Baseline de produção intacto (comm 49, pay 21, regs 125, eventos reais 7, tickets reais 0).
- **Fora desta etapa**: cupons continuam por evento (sem compartilhar entre sessões); relatórios/vendas por sessão via seletor de evento já existente; seletor agregado de sessões no hub de vendas fica para depois.

### 2026-09-24 (noite) — Pagamento tardio corrigido + Fase 3 (PCD/cadeirante/acompanhante) etapas 1–3 ✅ DEV (staging) — 🚧 sessões e matriz E2E pendentes

Detalhes em `memory/pagamento_tardio_ingresso_shipado_2026_09_24.md`, `memory/fase3_etapas_1_2_tipos_assento_shipado_2026_09_24.md` e `memory/planta_nelson_camargo_extracao_2026_09_24.md`. Commits na `dev`: `51be9ee`, `d4f5c6b`, `f1ed6bc`, `574c0a1`, `cdb2b0b`, `dba0f86` (nada em `main`).

- **Pagamento depois da reserva expirar (bug real, venda dupla)**: janela de pagamento 15 min (Sympla Pix 15 min; Ticketmaster ~10 — a sugestão inicial de 30 min estava acima do mercado); edge `expire-audience-payments` (cron 1/min, só chama se houver ticket vencido) cancela a cobrança na Asaas (`DELETE`) antes de liberar ticket+assento; webhook (v69) trata pagamento tardio via RPC `reclaim_late_audience_payment` (religa assento/estoque) ou estorno automático + e-mails; reentrega de APROVADO após estorno é ignorada. Migrations `20260926` e `20260926b`. Validado no sandbox (4 cenários) e por smoke SQL.
- **Fase 3 — tipos de assento + regras no servidor** (migrations `20260927`, `20260927b`, `20260927c`): `event_seats.seat_tipo` (comum/cadeirante/pcd_largo/acompanhante) e `companion_of`; RPCs `validate_seat_cart` e `seat_general_release_open` (corte 24 h **E** sala esgotada, art. 23-A §3º do Decreto 5.296/9.404, sem cron); `hold_event_seats(..., p_pcd_qty, p_comp_qty)`; `create-audience-ticket` e `create-pdv-ticket` validam. Correção de rumo após pesquisa: acompanhante é ingresso próprio "Acompanhante de PCD" (mesmo pedido; Lei 12.933/Decreto 8.537), e PCD/acompanhante ficam fora do limite de 1 meia (`kind='outro'`). `generate_event_seats` passou a exigir dono do evento. Nelson Camargo conforme a planta real do Guichê (373 lugares; fileira N de 40 não é vendida): 7 ♿ + 6 ⇔ (legenda ⇔ não confirmada, tratado como PCD reservado) + 13 vizinhos de acompanhante.
- **Fase 3 — tela** (frontend, só na `dev`/staging): `SeatGrid` com ícones/tom azul por tipo, lugares travados com motivo e `SeatLegend`; `CheckoutIngresso` com carrinho PCD/acompanhante, "Vai com acompanhante?" e hold com quantidades; PDV com as mesmas regras; Configurações → Ingressos com "Tipo de assento" (só com mapa numerado); editor de Locais (`Venues.tsx`) com ferramenta de pintura (comum/cadeirante/PCD largo/acompanhante), vizinho sugerido e sincronização dos eventos vinculados (não apaga tipos ao salvar); etiqueta "Assento PCD" no ingresso do comprador, na lista de compradores e no check-in (tipo derivado do layout público via `utils/seatTipo.ts`). Lógica pura em `utils/seatSelection.ts` e `utils/venueRows.ts`. 133 testes.
- **Validação**: API real no sandbox (regras), Playwright em 1440×900 e 375×812 no checkout sandbox e no editor de Locais real (somente leitura, sem salvar), 0 erros de console, sem overflow; produção intacta (49 comissões, 21 pagamentos, 125 inscrições, 0 ingressos reais). Sessão de produtor do Playwright regenerada (`scripts/.playwright-auth/storage-produtor.json`, ignorado pelo git).
- **Pendente**: etapa 4 (sessões: `events.session_group_id`, "Duplicar como nova sessão", seção "Outras sessões"), matriz E2E de 4 sessões, PDV nunca clicado ao vivo, e-mails de estorno só conferidos no comprador, meia-entrada automática PCD (jurídico), Fase 4 (matriz E2E) e Fase 5 (Decreto 13.108/2026), Pix real de R$ 20, estorno do A-1 no painel da Asaas e chamado do "saldo insuficiente".

### 2026-09-24 (tarde) — Fase 1 do ingresso + sandbox de pagamento por evento (Fase 2) + achados ✅ MAIN (c0b7c52) — 🚧 pagamento tardio aberto

Detalhes em `memory/fase1_ingresso_completo_shipado_2026_09_24.md` e `memory/incidente_evento_teste_is_demo_apagado_2026_09_24.md`.

- **Incidente do evento de teste — causa real**: o cron `cleanup-expired-demo-events` (03:00 BRT) apagava todo `is_demo` com >24h, com cascata em tickets/payments. Corrigido: `_shared/demo-protection.ts` (cron e `seed-demo-event` delete/create nunca apagam demo com `payment_id`/`asaas_payment_id` real). **Nunca usar `is_demo` em evento de teste com pagamento.**
- **Fase 1 (ingresso)**: RPC `get_audience_ticket_by_token_v2` (data real, horário, cidade/UF, assento; a v1 lia `events.event_date` legado NULL); MeuIngresso completo (Maps, .ics, assento, "Ingresso 1 de N"); e-mail de confirmação idem; `utils/edgeError.ts` mostra o erro real das edge functions; SEO/AEO/GEO (JSON-LD com hora+fuso, offers, `api/og.ts` = HTML que Googlebot/IAs recebem); `noindex` em /meu-ingresso e /checkout-ingresso; taxa visível na vitrine ("+ taxa R$ X"). `create-audience-ticket` corrigido: fim das vendas por `end_date/start_date` (event_date era NULL).
- **Fase 2 — sandbox Asaas por evento, no mesmo banco (opção A)**: `_shared/asaas-env.ts` (+`-loader`) é o ÚNICO ponto que decide produção x sandbox (falha fechada; sandbox só com `events.payment_sandbox` + produtor `profiles.is_test_account`); catraca em `tests/asaas-env.test.ts` (function nova não pode ler `ASAAS_*` direto). Migration `20260925b_payment_sandbox.sql` (colunas + 5 triggers; `is_sandbox` carimbado pelo banco; comissão sandbox sem `release_at`). Webhook distingue ambiente pelo token (`ASAAS_SANDBOX_WEBHOOK_TOKEN`); sandbox só aceita `AT:`; repasse/KPIs filtram `is_sandbox=false`. Evento de teste `teste-sandbox-espetaculo` (Nelson Camargo, 373 assentos) com faixa AMBIENTE DE TESTE, fora de /festivais e do sitemap. Contas `teste.produtor@` / `teste.comprador@coreohub.com` (sem caixa de entrada: usar `coreohub+sandbox@gmail.com` p/ e-mail).
- **E2E sandbox validado**: compra com assento, hold, webhook, comissão sandbox, página do ingresso e estorno (cartão de teste `5162306219378829`). **Limites do sandbox**: BaaS trial bloqueou criar subcontas → `ASAAS_SANDBOX_SKIP_SPLIT=true` (cobra sem split); split, D+7 e `splitRefunds` NÃO testados; "Confirmar pagamento" do painel marca BOLETO (estorno só cartão/Pix).
- **✅ CORRIGIDO 2026-09-24 (noite, commit `51be9ee`, só na dev; backend já em prod) — pagamento depois da reserva expirar**: janela de pagamento 15 min (Sympla Pix 15 min; Ticketmaster ~10 — a sugestão inicial de 30 min estava acima do mercado); edge `expire-audience-payments` (cron 1/min) cancela a cobrança na Asaas (DELETE) antes de liberar ticket+assento (erro que não seja 200/404 mantém PENDENTE); cron SQL antigo vira rede de segurança; webhook `asaas-webhook` v69 trata pagamento tardio com RPC `reclaim_late_audience_payment` (religa assento/estoque) ou estorno automático + e-mails; reentrega de APROVADO após estorno é ignorada. Validado no sandbox (expiração, DELETE 400, tardio com assento livre, tardio com assento tomado). Migration `20260926_audience_payment_expiry.sql`. Achado: o sandbox agora dispara webhook sozinho ao pagar — para simular pagamento tardio, cancelar o ticket ANTES de pagar. Não testado: e-mails de estorno, estorno automático em produção com split. Ver `memory/pagamento_tardio_ingresso_shipado_2026_09_24.md`.
- **✅ Fase 3, etapas 1+2 (commit `f1ed6bc`, dev; banco e edge functions já em prod, frontend NÃO mudou) — tipos de assento e regras no servidor**: `event_seats.seat_tipo` (comum/cadeirante/pcd_largo/acompanhante) e `companion_of`; `rows_config` com `tipos` e `acompanhante`; RPCs `validate_seat_cart` e `seat_general_release_open` (corte 24 h E sala esgotada, art. 23-A §3º do Decreto 5.296/9.404, sem cron); `hold_event_seats(..., p_pcd_qty)`; `create-audience-ticket` e `create-pdv-ticket` validam. Nelson Camargo conforme a planta real do Guichê (373 lugares; a fileira N de 40 não é vendida): 7 ♿ + 6 ⇔ (legenda ⇔ não confirmada, tratado como PCD reservado) + 13 vizinhos de acompanhante. Validado no sandbox pela API real e em smoke SQL. **Falta:** tela (mapa com ícones/legenda, "vou com acompanhante", editor de Locais — `Venues.tsx` pode apagar `tipos` ao salvar), sessões (`session_group_id`, duplicar, "Outras sessões") e matriz E2E. Ver `memory/fase3_etapas_1_2_tipos_assento_shipado_2026_09_24.md` e `memory/planta_nelson_camargo_extracao_2026_09_24.md`.
- **Achados corrigidos**: `get_audience_ticket_siblings` nunca aplicada em prod (`position` é palavra reservada); guarda de ambiente do webhook usava `group_id` (agora `payment_id`).
- **🚨 Erro meu, 2h de produção parada**: script inseriu regras de `headers` DENTRO de `headers[2].headers` no `vercel.json` (JSON válido, schema inválido) → deploys de `dbf0dc1` falharam e a produção ficou com bundle antigo. Corrigido em `c0b7c52`; **`tests/vercel-config.test.ts`** e validação contra o schema oficial (`https://openapi.vercel.sh/vercel.json`) evitam repetir. Lição: ao editar JSON de config, nunca `str.replace` sem conferir o número de ocorrências.
- **Pendente**: Fase 3 (4 sessões no mesmo local, Plateia/PCD, Decreto 9.404, espaços de cadeirante), Fase 4 (matriz E2E), Pix real de R$20 em produção, Fase 5 (Decreto 13.108, jurídico), preço/taxa travados no hold. `temp-test-installment-split` e `20260915_pix_installments.sql` continuam não commitados (Pix parcelado).

### 2026-09-23/24 — Plano Espetáculo: página/wizard/Configurações adaptados + venue real + pesquisa de fluxo de ingresso + Info/FAQ/PDVs + hold de assento + zoom mobile ✅ MAIN (dc8fac6) — 🚨 evento de teste apagado sem querer

Vários blocos no mesmo dia (`f471a54` → `dc8fac6`). Detalhes em `memory/` (`ingressos_pagina_selo_slug_vitrine_2026_09_23`, `espetaculo_config_pagina_wizard_2026_09_23`, `backlog_mapa_nelson_camargo_venue_real`, `pesquisa_fluxo_ingresso_e_info_regras_2026_09_23`, `bug_refund_audience_ticket_saldo_insuficiente_2026_09_23`).

- **Espetáculo adaptado** (`1662851` + `68822a0`): `billing_plan='espetaculo'` → Configurações só com Geral/Pagamentos, página pública sem Premiação/Jurados/Inscrições/Workshops, wizard `/criar-espetaculo` pede descrição, valor do ingresso (mín. R$ 20 = piso do split Asaas) e meia (só se metade ≥ R$ 20). **Causa raiz de perda de dados achada**: o `select` de `events` em `AccountSettings.tsx` não trazia `start_date/end_date/event_time/ingressos_config/programacao_config/politica_ingressos`, então salvar gravava vazio por cima (evento criado por SQL perdeu data, ingressos e venda).
- **Página de ingressos** (`f471a54`): slug automático no 1º save, alerta de vitrine incompleta (`ProducerAlerts`), nota de meia e "vendas até" no cartão, capa padrão da marca, vitrine `/festivais` mostra "A partir de R$ X" só com ingresso. **Venue real** Centro de Convenções Jornalista Nelson Camargo (373 assentos, 13 fileiras, 7 PCD) em `/locais` (campos `espaco_antes`/`palco_apos` dentro do `rows_config`, sem migration). Campo WhatsApp com máscara no PDV (falta auditar `CheckoutIngresso`/`InscricaoWizard`/`EquipeProdutor`).
- **Pesquisa de mercado do fluxo de compra** (Sympla, Eventbrite, Ticketmaster, Seats.io, Guichê Web, com fontes): assento marcado escolhido depois de data/setor; Eventbrite 20 min de pedido e sem assento marcado em evento recorrente; Ticketmaster timer no checkout; hold é por **tempo esgotado**, não por fechar a aba (Eventbrite: assento indisponível até o limite; Seats.io: segurado até reservar ou expirar); Guichê lista "Pontos de Venda" no fim da página.
- **Shipado em main** (`98b5ca4` + `081f580` + `dc8fac6`): (C) `events.info_config` JSONB (migration `20260924_events_info_config.sql`) com 6 seções fixas + FAQ (12) + **pontos de venda** (10), editor em Configurações > Geral, seção pública "Informações e regras" com FAQPage JSON-LD; descrição 1500 → 4000. (A1) dica de lugar marcado + CTA "Escolher lugares" + aviso quando o assento escolhido some. (A2) reserva ao escolher: migration `20260924b_seat_hold_on_select.sql` (`event_seats.hold_token`, RPC `hold_event_seats` para anon, `get_event_seats_public_v2`, prazo único por token com `seconds_left` do relógio do banco — o relógio do aparelho tinha skew de ~1:40); edge `create-audience-ticket` reaproveita o hold (compatível com front antigo). (B) `SeatGrid` com ajuste à largura, zoom +/−/ajustar, pinça e 2 toques no celular. Selo Asaas 100×30 em todos os checkouts/pagamentos (só MeuIngresso/VendasIngressos estavam corrigidos antes).
- **`tsc`/`build` funcionam** com `NODE_OPTIONS=--max-old-space-size=3072` (tsc) e `4096` (build) — o "OOM do ambiente" era só heap.
- **🚨 INCIDENTE — evento de teste e o ingresso real A-1 sumiram do banco** (achado em 2026-09-24): "TESTE PRESENCIAL — Assento Numerado" (`72d2b7ab-…`, `is_demo=true`, conta do Hemer) não existe mais, junto com o ticket `45c33e18-…` e o registro do payment. Causa provável: `seed-demo-event` action `delete`/recriar demo apaga `events WHERE created_by = usuário AND is_demo = true` (linha ~752), cascateando tickets/payments — o evento de teste estava marcado `is_demo=true` de propósito (esconde de KPIs) e caiu junto. **Lição:** nunca deixar evento de teste com `is_demo=true` numa conta que tem "Recriar demo"; usar conta de teste própria. O(s) payment(s) `pay_j8cui5x2z02xprlq` (A-1) / `pay_w49cyq3hwqce569c` ainda existem na Asaas: o estorno tem de ser feito no painel da Asaas (não há mais ticket no CoreoHub), a partir de ~2026-09-26 (retenção D+7). Venue Nelson Camargo continua no banco (`c2b9346b-…`), o "SMOKE E2E Venue" também.
- **Achados na revisão dos prints do produtor (ainda NÃO corrigidos):** (1) `get_audience_ticket_by_token` (migration `20260513`) devolve `e.event_date`, coluna legada sempre NULL → **a página do ingresso nunca mostra data**; também não traz horário (`event_time`), cidade/UF nem o **assento** (`audience_tickets.seat_id`); (2) checkout de ingresso mostra o erro genérico "Edge Function returned a non-2xx status code" porque `CheckoutIngresso.tsx` usa `invokeErr.message` em vez do corpo da resposta (o servidor manda a mensagem real); (3) preview do WhatsApp sem imagem em algum caso — em eventos reais (Tamoios/Vicenza) o `og:image` responde 200 e ≤122 KB, então a causa é específica do evento de teste ou de rotas sem rewrite de bot (`/checkout-ingresso`, `/meu-ingresso`); (4) sessões múltiplas com assento numerado exigem 1 evento por sessão, porque `event_seats` é por evento.

### 2026-09-22 — Formato "Não Competitiva" com rótulo configurável + Landing do Espetáculo reescrita (2 rodadas) + decisão sobre preset de preço progressivo ✅ SHIPADO

3 commits (`70a0d1a` → `bcc7ffa`), mais 1 decisão de produto sem código. Sessão de continuidade do Plano Espetáculo (ver 2026-09-19) e de nomenclatura do formato não competitivo.

- **Rótulo do formato "Avaliada" vira configurável** (`b8a517e`, migration `20260922_formato_avaliada_label.sql` aplicada em prod): pesquisa de mercado confirmou "Não Competitiva"/"Avaliativa"/"Comentada" como sinônimos do mesmo mecanismo (feedback do júri sem nota/ranking/prêmio) em regulamentos diferentes — "Mostra Avaliada" era nomenclatura fixa desde o início do projeto, sem base de mercado única. Novo seletor em Configurações → Geral (`configuracoes.formato_avaliada_label_mode`/`_custom`, mesmo padrão de `medal_labels`/`medalLabelMode`) deixa o produtor escolher o termo — só o escolhido aparece, nunca os três juntos. `utils/formatoParticipacao.ts` centraliza a resolução, aplicado em Inscrições/Wizard/vitrine/Apuração/Cronograma/Terminal do Júri (3 idiomas)/certificados. Valores internos no banco continuam `'Competitiva'`/`'Avaliada'` — zero migração de dado, eventos existentes (Tamoios, Will Nunes) já aparecem com o rótulo certo. **Bug real corrigido no caminho**: `RegulationAIParser`/`gemini-analysis` gravavam vocabulário próprio (`MOSTRA_AVALIADA`/`COMPETITIVA`/`NAO_COMPETITIVA`/`PARTICIPATIVA`) direto na coluna que Wizard/Terminal/Apuração esperam só `'Competitiva'`/`'Avaliada'` — causa raiz de um array corrompido achado no evento duplicado "Lyris Dance Competition" (mapeamento adicionado antes de salvar). **Removidos** do seletor de Formato do Evento: "Ranking por Médias" e "Torneio de Batalhas" (0 uso real em produção, nenhuma lógica implementada, confirmado via banco) e "Espetáculo de Dança" (desconectado do Plano Espetáculo real, que roda por `billing_plan='espetaculo'`). `EventFormat.PEDAGOGICAL/GRADUATED/BATTLE` (`services/format_resolver.ts`) confirmado legado sem uso real (lê `configuracoes.id='1'` fixo) — só rótulo atualizado, não removido ainda. 3 edge functions redeployadas (`judge-login`, `get-certificate-pdf`, `emit-certificates-batch`).
- **Landing do Espetáculo — 2 rodadas de reescrita de copy** (`70a0d1a` + `bcc7ffa`, `pages/LandingEspetaculo.tsx`): 1ª rodada trocou o ângulo operacional (planilha/fraude) pelo ângulo emocional real do público (dono de estúdio de dança, não produtor de festival competitivo) — pesquisa confirmou que o medo real é "plateia vazia"/"pais não rematricularem", não o valor da taxa (fontes globais Gostudiopro/Stage Stubs/Booktix/Bookeo + BR Flly IA/Ballare). Seções passam a alternar fundo claro/escuro (diferencia visualmente de `LandingPage.tsx`/`LandingGoverno.tsx`, 100% escuras). FAQ ganha 2 perguntas novas ("e se a plateia não lotar", "dá pra ativar perto da data"). 2ª rodada, a partir de feedback direto do produtor: foto do hero trocada pra uma gerada por ele (finale de espetáculo infantil, plateia aplaudindo, convertida via ffmpeg pra webp desktop+mobile); H1 mais direto ("Venda o ingresso do seu espetáculo de fim de ano"); seção AEO para de abrir com "O Plano Espetáculo é..." e passa a abrir com "A CoreoHub é..." (marca que o dono de estúdio já reconhece — "Plano Espetáculo" fica só em metadados/SEO); "produtor"→"estúdio" no FAQ (vazamento de linguagem do público de Festival); removida lista de reasseguros estilo disclaimer jurídico que cortava o ritmo emocional. Validado nas 2 rodadas: lint+build limpos (heap aumentado — OOM pré-existente do ambiente, não relacionado), desktop 1440×900 + mobile 375×812, 0 console errors, sem overflow, alternância clara/escura confirmada via DOM.
- **Decisão — sem preset/template pra preço progressivo por bailarino** (sem código, só decisão registrada): avaliado se o `pricing_type: 'PROGRESSIVE_PER_DANCER'` (Configurações → Formações) merece um atalho tipo "aplicar modelo de pacote progressivo" ou um 3º template no wizard de criação de evento, pra reduzir o tipo de erro que gerou os 4 bugs em cascata de 2026-09-20. Pesquisa de mercado (Sympla, Eventbrite, Ticket Tailor, RunSignUp) mostrou que **nenhuma plataforma oferece preset pros valores de faixa** — todas usam edição manual linha-a-linha, e a config de preço progressivo é sempre tela secundária/avançada, nunca parte do wizard inicial de criação de evento. O único elemento "preset-like" encontrado (RunSignUp) escolhe o *modo de cálculo* via dropdown (Fixo/Desconto Total/Desconto por Registrante), nunca os valores em si — e o editor atual da CoreoHub já cobre esse nível de sofisticação. **Decisão: não implementar preset nem 3º template** — o editor manual de faixas já está no nível (ou acima) do líder de mercado mais próximo, e nenhum dos 4 bugs de 20/09 foi causado por erro de digitação no editor (foram bugs em lógica de pagamento/comissão adjacente: case-sensitivity, estimativa do agregado, fatura reaproveitada — já corrigidos naquela sessão). Reduz o risco de reabrir essa pergunta numa sessão futura sem esse contexto.
- **🚧 Em andamento, NÃO commitado — Pix Parcelado**: `supabase/migrations/20260915_pix_installments.sql` (tabela `payment_installments`, cobre os 2 fluxos de inscrição avulsa e carrinho agregado) e `supabase/functions/temp-test-installment-split/` (function explicitamente marcada "TEMPORARY — não é feature de produto, deletar após uso", teste isolado de parcelamento PIX + split proporcional na Asaas) existem no working directory mas **nunca foram commitados nem aplicados em produção**. Decisão de produto já registrada (comentário na própria migration): liquidação por parcela paga, não antecipação total — modelo Sympla de antecipação exigiria caixa próprio/linha de crédito que a CoreoHub não tem. Não tratar como feature pronta; próxima sessão que retomar isso precisa decidir se aplica a migration, promove a function de teste pra definitiva, e integra no fluxo real de checkout.

### 2026-09-20 (continuação) — Comissão da Tamoios restaurada (bug de case-sensitivity) + "Pagar Tudo" sem split de baixo valor ✅ SHIPADO

Continuação da sessão do Pacote Progressivo (ver entrada abaixo) — fechou a pendência crítica deixada em aberto (comissão zerada) e um SEGUNDO bug achado ao vivo por um novo print da Daniele durante a própria correção.

- **Fix 1 — case-sensitivity no `EventCommissionModal`**: `pages/SuperAdmin.tsx` comparava `type === 'PERCENT'` (maiúsculo) contra `commission_type` do banco (sempre `"percent"` minúsculo) — qualquer save do modal sem trocar o Plano de cobrança junto zerava `commission_percent` silenciosamente. Corrigido normalizando `type` pra `'percent'|'fixed'` minúsculo (5 ocorrências no arquivo: state, botões, 2 comparações no save, 1 na exibição da tabela). Auditoria dos 8 eventos da plataforma confirmou que **só a Tamoios foi afetada**. `commission_percent=5` restaurado via SQL direto (precisou desabilitar/reabilitar `protect_commission_columns_trigger` — rodar via `supabase db query` não carrega contexto `service_role`, mesmo padrão já documentado na migration `20260920_billing_plan_fee_gate.sql`).
- **Fix 2 — "Pagar Tudo" nunca teve absorção de taxa de baixo valor**: no meio do Fix 1, a Daniele mandou print real do erro "O valor total do Split R$ 35,00 excede o valor a receber da cobrança R$ 33,01" ao tentar pagar 2 coreografias juntas — confirmando de quebra que o cálculo de faixa progressiva (20+15) estava certo, só que o fluxo agregado não sabia lidar com o resultado. Causa: `create-payment-asaas` (fluxo single) já tinha, desde `20260916_low_value_transfers.sql`, um mecanismo de "sem split quando a comissão fica abaixo de ~R$3 (piso mínimo da Asaas)" — nunca portado pro `create-aggregate-payment-asaas` ("Pagar Tudo"). Com `fee_mode='absorver'`, mesmo com 5% de comissão correta, o valor do split ainda excedia o "valor a receber" (que já desconta a taxa própria da Asaas, ~R$1,99). Fix: portado o mesmo mecanismo pro agregado — migration `20260920b_low_value_transfers_aggregate.sql` torna `low_value_transfers.registration_id` nullable e adiciona `payment_group_id` (CHECK XOR, já que 1 fatura agregada cobre N registrations sob 1 único `asaas_payment_id`, violaria o `UNIQUE` se tentasse 1 linha por registration). `asaas-webhook`'s `handleAggregatePayment` replica a mesma detecção do fluxo single e **pula a inserção em `platform_commissions`** pra fatura sem split (evita duplicar saldo "a liberar" no D+7, já que esse dinheiro nunca passa pela subconta do produtor nesse caso).
- **Validação**: `npm run lint` + `deno check` nas 3 functions tocadas (3 erros pré-existentes confirmados via `git stash`, não relacionados) + smoke transacional (BEGIN/ROLLBACK) simulando o INSERT exato que o código real faria, incluindo o novo CHECK constraint. Deploy das 3 edge functions direto em produção via CLI. Merge `dev→main` (fast-forward) autorizado e feito.
- **Lição registrada**: Ticko cobrou (com razão) que o fix do dia anterior devia ter sido validado simulando o payload real da Asaas (valores de split vs. valor a receber), não só o cálculo de preço isolado — a produtora real não pode ser quem acha o próximo bug. Ver `memory/feedback_validar_fluxo_completo_antes_de_ship.md`.
- **✅ Validação E2E real feita na sequência**: usada a conta de teste `inscrito@coreohub.com` contra o **evento real da Tamoios** (mesmo `event_id`, garante fidelidade total de config, sem tocar no dado/conta real da Daniele) — 2 registrations de teste inseridas via SQL, sessão Playwright gerada, "Pagar Tudo" clicado de verdade contra produção. Confirmado no banco (não só na tela): `payments.value_total=35`, `commission_total=0`, `producer_total=35`, `asaas_payment_id` real gerado sem erro de split, `low_value_transfers` criado certo com `payment_group_id`. Dados de teste limpos depois.
- **🔧 Achado no caminho, corrigido junto — modal de CPF exigia 2 cliques**: durante o E2E acima, achado que o modal "Confirme o CPF do pagador" (1ª compra de cada conta) só salvava o CPF e fechava, sem retomar o pagamento — pagador precisava clicar "Pagar Tudo"/"Pagar Só Esta"/"Pagar Taxa" de novo depois de confirmar. Fix (`pages/MinhasCoreografias.tsx`, commit `87727cc`): `cpfPendingActionRef` guarda a ação de pagamento que disparou o modal; `requireCpf(onConfirmed?)` armazena esse retry; `handleSaveCpfFromModal` dispara sozinho depois de salvar. Validado via Playwright contra **dev server local** (mudança de frontend ainda não deployada no momento do teste): clique único em "Pagar Só Esta" sem CPF confirmado já disparou a chamada de pagamento sozinho após salvar o CPF (bloqueado só por CORS de localhost contra produção — esperado, não relacionado ao fix).

Detalhes completos em `memory/commission_case_bug_e_low_value_split_aggregate_shipado_2026_09_20.md` e `memory/cpf_modal_retoma_pagamento_sozinho_shipado_2026_09_20.md`.

### 2026-09-20 (continuação 2) — "Pagar Tudo" reaproveitava fatura desatualizada (4º bug do dia) + matriz completa de preço progressivo validada E2E ✅ SHIPADO

Continuação direta da sessão acima. A Daniele mandou mais 2 prints reportando comportamento estranho testando "Pagar Tudo" (fatura travando em R$35, "Solo Pacote Progressivo saiu por R$20 duas inscrições") — 4º bug real do mesmo dia, cada um uma causa diferente.

- **Causa raiz**: `handlePagarAgregado` (`pages/MinhasCoreografias.tsx`) reaproveitava a fatura PENDENTE existente do evento (`grupo.payment.payment_url`) sem checar se ela cobria TODAS as coreografias pendentes atuais. `grupo.payment` = a única fatura PENDENTE do evento (1 query por user); `grupo.pendentes` = TODA registration `PENDENTE`, incluindo qualquer inscrição criada DEPOIS da fatura já existir. Cenário real: Daniele gerou fatura A só com "Teste" (tier1, R$20), depois criou "Teste 2" (tier2, R$15) enquanto a fatura A ainda estava PENDENTE — a tela mostrava "2 coreografias em 1 PIX" (contagem certa) mas o clique redirecionava pra fatura A antiga, deixando "Teste 2" de fora silenciosamente.
- **Fix**: antes de reusar a fatura existente, checa se `grupo.pendentes.every(r => r.payment_group_id === grupo.payment.id)`. Se alguma pendente não estiver coberta, cancela a fatura desatualizada via `cancel-aggregate-payment` (mesmo padrão já usado em `handleApplyAggregateCoupon` pro cupom) e gera fatura nova cobrindo o conjunto completo.
- **🚨 Cobrança explícita do Ticko**: "esse teste de todas as formas e todos os fluxos deveria ter sido feito pela gente e não por ela, temos que ter esse compromisso com o cliente" — resultou em pedido explícito de testar a MATRIZ completa (não só o bug pontual) antes de considerar pronto. Ver `memory/feedback_testar_matriz_completa_antes_de_delegar_ao_cliente.md`.
- **Validação E2E real contra o evento da Tamoios** (conta de teste `inscrito@coreohub.com`, chamadas diretas às edge functions via API, `npx supabase db query --linked` pra todo acesso a banco — nunca expondo a service role key no bash): (1) reprodução exata do bug — fatura A cancelada + fatura B gerada com R$35 (20+15), zero registration órfã, confirmado no banco (`payments.status`, `charged_amount`, `payment_group_id`); (2) matriz completa do cálculo progressivo — 1 bailarino com 5 inscrições cruzando Solo→Duo/Trio→Grupo→Solo→Solo numa única fatura "Pagar Tudo" (mais 4 bailarinos extras nas formações de grupo) fechou em **R$140 exato** (20+35+70+15+0), confirmando que a posição do bailarino atravessa formações diferentes e que a 4ª inscrição própria trava exatamente nos R$60 do regulamento, com a 5ª+ zerada. 92/92 testes automatizados + `tsc --noEmit` + build limpos. Dados de teste (elenco + registrations + payments com IDs previsíveis) todos limpos do banco no fim — os dados de teste da própria Daniele ("Teste"/"Teste 2"/"Teste 3") foram deixados intactos, sem apagar sem autorização dela.

Detalhes completos em `memory/pagar_tudo_fatura_desatualizada_shipado_2026_09_20.md`.

### 2026-09-20 — Pacote Progressivo (Tamoios): 4 bugs reais em cascata ✅ SHIPADO

Disparado por print de WhatsApp da produtora Daniele Costa (Tamoios): "a segunda coreografia está saindo gratuita como pacote progressivo" + "o site não está abrindo em dispositivos iOS" + confusão sobre R$21 vs R$20 na aba de pagamento. Virou 4 bugs reais corrigidos em cascata — cada um achado testando ao vivo a correção do anterior — e 1 achado crítico não fechado.

- **Bug 1 (o mais grave) — Pacote Progressivo aprovado de graça, sem passar pelo Asaas**: `isFormacaoGratuita` (`InscricaoWizard.tsx`) decide "essa formação é grátis? aprova na hora sem cobrar" lendo `firstLote?.preco ?? formacao?.fee ?? ... ?? 0`. Formações `PROGRESSIVE_PER_DANCER` guardam `lotes: [{preco: 0}]` como placeholder estrutural (preço real vive em `progressive_tiers`), mas `0 ?? qualquerCoisa` avalia pra `0` (`??` só cai no fallback com `null`/`undefined`, nunca com `0` literal) — **toda inscrição em Pacote Progressivo era aprovada com R$0, pulando o Asaas inteiro**, confirmado com evidência real (registro de teste "Teste 2" da própria Daniele aprovado às 14:49 com `valor_pago=0.00`). Mesmo padrão bugado achado em MAIS 3 lugares: `Checkout.tsx` (pagamento individual antigo), "Valor estimado" no Resumo do Wizard (ficava oculto), e o card da tela "Como você vai se apresentar?" (mostrava "R$ 0,00"). Fix nos 4 lugares: usa `progressive_tiers[0]?.valor` em vez de `lotes[0]?.preco` quando `pricing_type==='PROGRESSIVE_PER_DANCER'`. Commits `a014388`+`9e062ad`. Validado ao vivo via Playwright contra dev server (mesmo backend de produção): registrou de verdade as 3 formações (Solo/Duo-Trio/Grupo — Pacote Progressivo), todas ficaram `PENDENTE` (não mais auto-aprovadas); contagem por posição testada via RPC real `count_dancer_paid_registrations` em transação `BEGIN/ROLLBACK` (zero risco). Dados de teste sempre limpos depois.
- **Bug 2 — tela de login congelada/lavada no iOS (bfcache do Safari)**: print mostrou `/login` renderizando quase invisível, travado em "AUTENTICANDO...". Causa: trocar de app (WhatsApp) no meio do login e voltar restaura a página do bfcache, que suspende o `requestAnimationFrame` da animação Framer Motion (`initial={{opacity:0}}`) e nem sempre retoma sozinho. Fix (`Auth.tsx`): listener de `pageshow` com `event.persisted` força `window.location.reload()` — mesmo princípio de fix já existente em `MinhasCoreografias.tsx` pro spinner travado pós-Asaas. Commit `9fc305a`. Validado com teste sintético via Playwright disparando `pageshow persisted:true` (recarrega) vs `persisted:false` (não recarrega à toa).
- **Bug 3 — duplicação de elenco por CPF, bug de PLATAFORMA INTEIRA**: investigando por que "a segunda coreografia ainda tá cobrando 20, é pra cobrar 15" (mensagem real da equipe da Daniele), descobri que `InscricaoWizard.tsx` sempre fazia `INSERT` cego em `elenco`, nunca verificava CPF repetido. Confirmado no banco: 2 registrations da Daniele com o MESMO CPF geraram 2 `elenco.id` diferentes, 84 segundos um do outro. Como `count_dancer_paid_registrations` conta por `elenco.id` (nunca por CPF), o desconto progressivo nunca reconhecia a mesma pessoa dançando de novo. **Levantamento confirmou bug histórico de toda a plataforma**: query `GROUP BY user_id, cpf HAVING count(*)>1` achou dezenas de contas com 4-5 elenco duplicados pro mesmo CPF. Fix (`InscricaoWizard.tsx`, commit `9beaaaa`): antes de inserir elenco novo, busca se o usuário já tem um registro com aquele CPF e reaproveita o ID; `createdElencoIds` (usado no rollback de erro) só lista o que foi REALMENTE criado nessa submissão. Validado ao vivo: 2 coreografias com mesmo CPF via Playwright agora compartilham o mesmo `elenco.id`; com a 1ª simulada como aprovada (`BEGIN/ROLLBACK`), a RPC conta certo (`qtd=1`) → 2ª calcularia tier 2 (R$15). **Pendente**: fix só previne duplicatas NOVAS — as dezenas de contas já duplicadas na produção continuam fragmentadas, nenhum merge retroativo em massa foi feito (risco de mexer em dado histórico real sem confirmação).
- **Bug 4 (o mais sutil) — "Pagar Tudo" não reconhecia coreografias irmãs ainda PENDENTES**: mesmo depois do Bug 3, o cenário real da Daniele (cadastrar 2-3 coreografias e só depois clicar "Pagar Tudo") ainda quebrava — `create-aggregate-payment-asaas` chamava `count_dancer_paid_registrations` isoladamente por registration dentro do mesmo loop; como a RPC só conta coreografias JÁ APROVADAS, e nenhuma das 2-3 do carrinho está aprovada ainda no momento de gerar a fatura, cada uma isoladamente calculava "0 anteriores" e todas caíam na 1ª faixa (20+20 em vez de 20+15). Fix (commit `634758a`): contador de posição por `elenco_id` que vive pra fatura INTEIRA — seed de 1 chamada batched à RPC (histórico já aprovado), processa as registrations em ordem de `created_at` e incrementa a cada uma (qualquer formação consome 1 posição pro bailarino, mesmo escopo sem filtro que a própria RPC já usa). Validado por simulação matemática isolada (não testado com Asaas real, por segurança): 3 coreografias do mesmo bailarino, todas pendentes, batem com R$45 (20+15+10) em vez de R$60.
- **Dado real mesclado manualmente**: "Teste"/"Teste 2" da Daniele (mesmo CPF, elenco duplicado de antes do fix) mesclados pro mesmo `elenco.id` via UPDATE direto, pra ela conseguir confirmar o desconto (R$35) no "Pagar Tudo" sem precisar recriar os testes.
- **Achado crítico (resolvido na sessão seguinte, mesmo dia) — comissão zera sozinha ao salvar modal do SuperAdmin**: ao trocar `fee_mode` de "repassar" pra "absorver" no evento Tamoios, `commission_percent` zerou sozinho pra 0,00% por bug de case-sensitivity no `EventCommissionModal`. ✅ Corrigido no código + dado restaurado — ver entrada "2026-09-20 (continuação)" acima.
- **Erro de processo**: commitei um fix acidentalmente na `main` em vez da `dev` (sem checkout explícito antes) — corrigido na hora via `git cherry-pick` + `git reset --hard origin/main`, sem sobrescrever um commit de trabalho paralelo que já estava em `origin/main`.
- **Validação geral**: 92/92 testes + lint + build limpos em cada commit. 2 merges `dev→main` (fast-forward, sem conflito). Edge function `create-aggregate-payment-asaas` deployada em produção 2x. Deploy confirmado comparando hash dos arquivos JS entre `app.coreohub.com` e o build local do mesmo commit (idênticos) + print do Vercel Dashboard mostrando `Production`/`Ready`.

Detalhes completos em `memory/pacote_progressivo_bugs_reais_shipado_2026_09_20.md`.

### 2026-09-19 (continuação) — Fase 2 Stage 3+4: assento numerado ligado ao checkout público + PDV, smoke E2E com dinheiro real 🚧 EM ANDAMENTO — refund pendente de reteste

2 commits em `dev` (`13e634e` + `00af396`), mesmo dia da sessão que fechou a spec + Stage 1+2 (ver entrada abaixo). Implementa a parte que faltava do assento numerado: ligação de verdade no fluxo de compra e no painel do produtor.

- **Stage 3 — checkout público escolhe assento**: `CheckoutIngresso.tsx` ganha o passo "Escolher lugar" quando o evento tem `seat_map_enabled`. `create-audience-ticket` e `create-pdv-ticket` aceitam `seat_ids[]`, reservam via `reserve_event_seats` (all-or-nothing) antes de criar a cobrança Asaas, confirmam via `confirm_event_seats` depois. `asaas-webhook` marca `event_seats.status='vendido'` no pagamento aprovado. Migration `20260920_seat_map_fase2_stage3.sql`: RPC `get_venue_layout_public` (leitura anônima do layout pro comprador não-logado ver o mapa), `mark_audience_ticket_refunded` libera o assento de volta no refund, cron `expire-audience-reservations` libera o assento junto quando o PENDENTE expira (mesmo padrão do `expire-pending-payments` já existente).
- **Stage 4 — painel do produtor**: `VendasIngressos.tsx` ganha mini-mapa + seletor de assento no modal "Vender no balcão" (PDV); `CheckIn.tsx` mostra o assento (Fileira/Nº) pro porteiro no check-in.
- **`/code-review` nível high achou 6 problemas antes de qualquer teste com dinheiro real, 5 corrigidos**: (1) assento órfão real — falha da Asaas (customer/payment) DEPOIS do assento já confirmado deletava o ticket sem chamar `release_event_seats` (função existia desde a Stage 1, nada chamava); corrigido nos 2 edge functions. (2) bug introduzido pelo próprio fix do #1 — `event_seats.audience_ticket_id` tem `ON DELETE SET NULL`, então apagar o ticket ANTES de `release_event_seats` fazia a liberação não achar mais nada; corrigido invertendo a ordem (libera primeiro, apaga depois) — só pego rodando o smoke de novo e vendo o resultado bater errado. (3) `audience_tickets.seat_id` nunca era escrito — a feature do `CheckIn.tsx` nunca funcionaria de verdade; corrigido via migration `20260920b_seat_map_fase2_stage3_fixes.sql` (`confirm_event_seats` passa a gravar o `seat_id` no ticket). (4) erro de RPC engolido silenciosamente nos 2 frontends (mesmo anti-padrão já documentado no `CLAUDE.md` — select sem checar error); corrigido. (5) `CheckoutIngresso.tsx` não limpava `selectedSeats` ao trocar de evento sem reload; corrigido junto com o #4. (6) **✅ resolvido na sessão seguinte (mesmo dia)**: lógica da grade de assento duplicada quase igual entre `CheckoutIngresso.tsx` e `VendasIngressos.tsx` — extraído `hooks/useSeatMap.ts` (fetch de layout + polling de status, com `markSeatsOccupied()` pra atualização otimista) e `components/SeatGrid.tsx` (renderiza só a grade, parametrizado por `size` md/sm e `variant` dark/auto pra cobrir o checkout público sempre-escuro e o PDV do produtor que segue tema). Commit `0322f9e`.
- **Smoke E2E com dinheiro real**: evento+venue de teste (`SMOKE E2E Event`/`Venue`, `is_demo=true`) criados sob a conta real do produtor Usualdance/Hemer — achado registrado: Hemer é o próprio dono da CoreoHub, não cliente terceiro, então usar essa conta não afeta dado de cliente alheio. **Piso real de valor mínimo descoberto empiricamente**: checkout de plateia com split rejeita qualquer valor de R$5 a R$19 (Asaas: "Split excede valor a receber da cobrança" — a cobrança `UNDEFINED` reserva uma taxa mínima assumida de boleto que come quase tudo em valor baixo); **R$20 é o piso mínimo que passa** (testado com curl direto, 8 tentativas, R$5 a R$19 falharam e liberaram o assento sozinhas via o fix #1, R$20 passou). **Compra real validada 100% end-to-end**: R$22 (R$20 base + R$2 taxa repassada) pago via PIX real, webhook confirmou sozinho, `seat_id` gravado, `event_seats.status='vendido'`, comissão lançada — tudo automático. **🟡 Refund real ficou bloqueado**: Asaas recusou com "não há saldo suficiente" mesmo o card do produtor mostrando saldo real disponível; hipótese não confirmada é a janela de retenção D+7 (o próprio card mostrava esse valor específico como "retido · libera em 7 dias"). Produtor decidiu esperar a liquidação (~2026-09-26) em vez de sacar agora, pra não perder a chance de testar o refund nesse ticket específico.
- **✅ Stage 4 validada via API direta (mesmo dia, sessão seguinte)**: sem seletor de evento em `VendasIngressos.tsx` (a tela sempre resolve o evento real via `resolveActiveEventId()`, nunca o demo — fix intencional de 2026-07-12 pra não sombrear evento real), então a UI da Stage 4 não dava pra clicar direto contra o evento de teste sem mexer no evento real do Usualdance. Validado no nível de servidor: `create-pdv-ticket` com `payment_method='cartao_tap'` (venda de balcão sem Asaas, comissão 0%, é o fluxo de "já paguei em dinheiro/maquininha física" — zero cobrança nova) vendeu o assento A-2 do `SMOKE E2E Event`, confirmado via banco (`audience_tickets.seat_id='A-2'` gravado, `event_seats.status='vendido'`) e via a mesma RPC `get_event_seats_public` que o mini-mapa/checkout usam. Ticket de teste revertido depois (`release_event_seats` + delete) — A-2 voltou a `livre`. Valida a mesma lógica de servidor que a UI do PDV chama; a renderização em si (mini-mapa, modal, grade) não foi clicada ao vivo, só validada por tipagem/build (refactor de paridade com o checkout, que já foi validado ao vivo).
- **Pendências**: retestar o refund real do ticket `b529da11-5a89-4670-8837-ab0b25177a10` (payment `pay_j8cui5x2z02xprlq`) depois que a retenção liberar (~2026-09-26); limpar o evento/venue de teste (tem registro financeiro real vinculado, não apagar antes do refund resolver); validar a UI da Stage 4 clicando de verdade (precisa ou de um evento real com `seat_map_enabled` ou de um jeito de apontar `VendasIngressos.tsx` pro evento de teste). Detalhes completos em [[seat_map_fase2_stage3_stage4_shipado_2026_09_19]].

### 2026-09-19 — Plano Espetáculo: spec fechada + Fase 1 completa (Partes A+B) + Fase 2 assento numerado Stage 1+2 🚧 EM ANDAMENTO

Sessão de continuidade da exploração de mercado de bilheteria pra espetáculo de fim de ano de estúdio de dança (iniciada 2026-09-16/18), estendida em 2 sub-sessões subsequentes no mesmo dia. Produto novo **"Plano Espetáculo"**, vertical comercial irmã do Festival — documentado do zero em `docs/mostra-pricing-spec.md`. 5 commits em `dev` (`6e6df15` → `68e4a99`), nada em `main` ainda.

- **Spec fechada com pesquisa de mercado real (não só Guichê Web)**: % da taxa fechado em **7,9%** sobre o GMV (sem mínimo, tudo incluso, mesma política de absorver 100% do processamento do Festival) — abaixo de toda a faixa mapeada (Sympla ~12-12,5%, faixa geral de plataformas grandes 10-20%, estimativa não-oficial do Guichê Web 7,99-15%). **Decisão registrada**: 7,9% é público e fixo na comunicação (não abre negociação caso a caso — protege o argumento de "transparência de taxa" contra o Guichê Web, que não divulga preço), mas fica tecnicamente editável por evento via super admin (reaproveita a mesma coluna/trigger que o Festival já usa, válvula de escape interna e excepcional). Ticket médio revisado **de R$45 pra R$32** depois de achar um 2º ponto de dado real na mesma praça/mesmo concorrente (27º Espetáculo Academia Almagêmea, Votuporanga, R$28 — mais forte que o R$35 isolado do "29º Espetáculo" usado antes).
- **Modelagem de sessões/elencos resolvida via pesquisa cross-platform**: testada a hipótese de que a "limitação" de sessão-como-evento-separado seria exclusiva do Guichê Web (fraca em dança) — **descartada**. Confirmado em Sympla (que TEM feature nativa de multi-sessão, "Grupos de Ingressos", e mesmo assim o evento real "1º Cuiabá em Dança (1ª Sessão)" foi cadastrado como evento próprio) e Diversos Ingressos ("Alice no País das Maravilhas - 2º Sessão - Elenco 2", evento próprio, sem link cruzado pra outros elencos). Eventbrite documenta oficialmente um 3º padrão (sessão vira tipo de ingresso nomeado dentro de 1 evento) que já é estruturalmente idêntico ao `events.ingressos_config[]` que a CoreoHub já tem — zero schema novo necessário pro MVP.
- **Plano técnico de assento numerado (Fase 2) escrito na spec, não implementado**: pesquisa de arquitetura real (Seats.io + padrão `SELECT...FOR UPDATE SKIP LOCKED`) corrigiu uma decisão errada do rascunho inicial — em vez de derivar disponibilidade de `audience_tickets` via advisory lock por hash, o plano final materializa `event_seats` (1 linha por assento por evento, barato nessa escala) pra usar o padrão de concorrência testado da indústria. Detalhes completos (schema, RPC, UX de checkout, gerador de grade, biblioteca de locais reutilizável, riscos) na seção "Fase 2" da spec.
- **Fase 1 (bilheteria simples) — Parte A shipada, B e C pendentes**: escopo mínimo aprovado (marcação manual + comissão automática, sem esconder menu ainda, sem tela self-service inicialmente — depois ampliado a pedido do produtor pra incluir self-service + esconder menu + Fase 2 também). Migration `20260919_billing_plan_espetaculo.sql` (aplicada em produção, smoke transacional validado via CLI simulando contexto `service_role`) estende o trigger `sync_commission_percent_from_billing_plan` já existente (20260904/20260913d) com o branch `espetaculo→7,9%`. `SuperAdmin.tsx` ganha o plano no seletor + badge + esconde bloco de "componente fixo" (não existe nesse plano). **Parte A**: rota `/criar-espetaculo` nova — `CriarEventoGate.tsx` virou genérico (prop `wizard` + copy parametrizável, reaproveita 100% a lógica de auth/signup/promote-to-organizer do funil de Festival) + `EspetaculoWizard.tsx` novo (formulário single-step, sem formações/categorias/júri). Achado técnico que simplificou a Parte A: `protect_commission_columns_trigger` só roda em UPDATE, não em INSERT — o produtor já pode setar `billing_plan='espetaculo'` direto na criação, sem gate de pagamento (diferente do Essencial, que tem componente fixo).
- **1 bug real achado e corrigido no processo**: input de arquivo nativo (upload de capa) causava 9px de overflow horizontal em 375px — corrigido com `flex-col` no mobile. Validado via Playwright contra dev server local (não produção, que ainda não tinha a rota) — cuidado extra tomado pra não sobrescrever `scripts/.playwright-auth/storage-produtor.json` de produção (backup + restore depois do teste local).
- **Decisão de processo**: produtor pediu pra dividir o trabalho em partes e receber um **prompt de continuação pronto** no chat a cada parte concluída, pra rodar a próxima numa sessão nova em vez de continuar tudo na mesma sessão — seguido em cada sub-sessão abaixo.
- **✅ Parte B shipada (commits `282e1f8` + `4fca2bf`) — menu de Festival escondido pro produtor Plano Espetáculo**: `App.tsx` ganha flag derivada `espetaculoOnlyProducer` (mesmo padrão de `videoSelectionFromEvents` já existente — consulta os eventos reais do produtor sob demanda, sem estado persistido) — `true` só quando **todos** os eventos reais (`is_demo=false`) do produtor são `billing_plan='espetaculo'`; produtor misto (qualquer evento Festival junto) mantém o menu completo, nunca perde acesso. Threadeada por `privateRouteProps → PrivateRoute → PrivateLayout` até `Sidebar.tsx` (esconde Jurados/Regulamento IA/Seletiva de Vídeo/Cronograma/Telão/Workshops/Apuração/Premiação/Certificados/Coordenador do Júri via `ESPETACULO_HIDDEN_PATHS`, filtro aplicado dentro do pipeline `visibleSections`) e depois estendida pro `BottomNavBar.tsx` (menu mobile) — achado no meio da sessão: o slot fixo "Crono" da barra principal (fora do sheet "Mais") também precisava sumir, virou "Ingressos" quando `espetaculoOnlyProducer`; o sheet "Mais" (`MORE_GROUPS`) filtra pelos paths literais usados nesse arquivo (3 deles — Regulamento/Jurados/Apuração — já divergiam da rota canônica por um bug pré-existente de 2026-08-23, não relacionado, filtrar pelo path literal ainda funciona). Inscrições fica sempre visível (decisão explícita do produtor — spec não a lista como fora de escopo com a mesma força que júri/cronograma). Validado com Playwright local (não só leitura — testado o estado espetáculo-only E o estado misto criando/apagando 2 eventos de teste isolados do próprio super admin) desktop+mobile, 0 erros, sem overflow.
- **✅ Fase 2 (assento numerado) Stage 1 shipado (commit `b4d2b5e`) — schema + RPCs + cron, sem UI**: migration `20260919b_seat_map_fase2.sql` aplicada em prod via CLI. `venues` (biblioteca reutilizável, `rows_config` JSONB por fileira, `total_seats` sempre recalculado via trigger — nunca confia no client, `is_shared` nasce `false` por decisão da spec). `event_seats` (1 linha por assento por evento, materializada a partir do venue; `status` livre/reservado/vendido/cortesia; anon nunca acessa a tabela direto, mesmo padrão anti-enumeração de `audience_tickets` — leitura pública só via RPC). RPCs: `generate_event_seats` (idempotente), `get_event_seats_public` (auto-expira holds vencidos na leitura), `reserve_event_seats` (`FOR UPDATE SKIP LOCKED`, all-or-nothing sobre o carrinho inteiro), `confirm_event_seats`, `release_event_seats`. Cron `expire-event-seat-holds` (1x/min) — deliberadamente **pura SQL sem edge function** (só faz `UPDATE`, sem chamada externa, então não precisa de `service_role_key` hardcoded na migration, evitando o problema de placeholder documentado em `_operating_manual.md` item 3). **2 bugs pegos no próprio smoke transacional antes de fechar**: `FOR UPDATE` não combina com `array_agg` na mesma query (Postgres erro `0A000`) — resolvido com CTE; e o parâmetro de saída `seat_id` da `RETURNS TABLE` colidia com a coluna homônima da CTE — resolvido qualificando `candidate.seat_id`. Smoke final (BEGIN/ROLLBACK) validou o ciclo completo — matemática bateu exata (15 assentos gerados − 2 reservados = 13 livres, all-or-nothing confirmado, hold expirado corretamente revertido).
- **✅ Fase 2 Stage 2 shipado (commit `68e4a99`) — biblioteca de locais + gerador de grade**: `pages/Venues.tsx` novo, rota `/locais` (gated por `perm="vendas_ingressos"`, mesma permissão de Ingressos), adicionado ao Sidebar (seção Vendas) e ao sheet "Mais" do BottomNavBar — feature de **produto compartilhada entre todos os planos comerciais** (Começo/Essencial/Escala/Espetáculo), por isso deliberadamente **fora** de `ESPETACULO_HIDDEN_PATHS`. Gerador de grade estruturado (não editor livre): nº de fileiras, nomenclatura A-Z ou 1-N, assentos por fileira (padrão + override por linha), corredor central opcional, clique no assento da prévia alterna PCD. Modal "Vincular local" com `EventPickerSheet` + botão que liga `events.venue_id`/`seat_map_enabled` e chama `generate_event_seats`, mostrando quantos assentos foram gerados — detecta evento já vinculado e vira "Desativar mapa neste evento". **Validado ponta a ponta via Playwright interativo** (script próprio, não o `screenshot.mjs` read-only) contra evento+local de teste isolados do super admin: gerou 3 fileiras × 6 assentos = 18, marcou 1 PCD, salvou, vinculou a um evento — RPC respondeu "18 assentos gerados" e o banco confirmou (`event_seats`: 18 linhas, 1 `is_pcd=true`). Eventos/local de teste apagados, `storage-admin.json` de produção restaurado a cada rodada (backup antes, restore depois, mesmo cuidado da Parte A).
- **🚧 Pendente — Fase 2 Stage 3+4** (a parte mais arriscada, deliberadamente adiada pra sessão nova por decisão do produtor): Stage 3 = integração no checkout público (`CheckoutIngresso.tsx` ganha "Escolher lugar" quando `seat_map_enabled`; `create-audience-ticket` precisa aceitar `seat_ids` e chamar `reserve_event_seats`/`confirm_event_seats`; `refund-audience-ticket`/`expire-pending-payments` precisam chamar `release_event_seats`). Stage 4 = mini-mapa no painel do produtor (`VendasIngressos.tsx`) e no PDV. Ambas tocam o fluxo de pagamento ao vivo (receita real), por isso a decisão de não emendar na mesma sessão das Stages 1-2 (que não tocam nenhum caminho de cobrança). Prompt de continuação já escrito e entregue no chat. Pendência 4 da spec (convite grátis pro elenco incluído na mensalidade) segue aguardando validação com estúdio parceiro — pesquisa de mercado já esgotada, sem confirmação direta no Brasil.

### 2026-09-16 — PWA prompt de update + SuperAdmin plano vira badge por evento + título "Acesso ao evento" ✅ SHIPADO EM PRODUÇÃO

8 commits (`cf49430` → `5fe4d5d`), 2 merges `dev→main` no mesmo dia. Sessão de polimento pós-Tamoios: fechou o backlog do PWA disparado no dia anterior, mais 2 achados de auditoria/suporte.

- **PWA — banner "Nova versão disponível" substitui `autoUpdate` silencioso** (5 commits): `registerType: 'autoUpdate'` deixava aba aberta durante deploy rodando JS velho até fechar/recarregar — já tinha revertido o fix do preço progressivo 2x na sessão anterior (ver 2026-09-15 abaixo). Trocado por `'prompt'` + componente novo `components/PwaUpdatePrompt.tsx` (`useRegisterSW`), banner fixo com botão Atualizar, sem timeout automático (não força reload no meio de formulário sendo preenchido). **3 fixes achados testando em staging antes de considerar pronto**: (1) `useRegisterSW` sozinho não detecta update numa aba parada sem navegação — `onRegisteredSW` força `registration.update()` a cada 3min; (2) clique em Atualizar não fazia nada visível em deploys em sequência rápida (listener `'controlling'` da lib não disparou) — failsafe de reload manual forçado 2s depois; (3) botão sem `cursor-pointer` (Tailwind v4 não seta isso no Preflight) parecia não-clicável. Detalhes em [[backlog_pwa_update_prompt_forcar_reload]].
- **SuperAdmin — plano comercial vira badge por evento, remove comissão-padrão morta**: `profiles.default_commission_percent` era editável no painel mas **nunca era lido em nenhuma function/trigger/criação de evento** — existia só pra aparecer editável, sem efeito real. Removido (coluna, state, handler, select). Tabelas "Eventos da Plataforma" e "Produtores Ativos" ganham o nome do plano (Começo/Essencial/Escala) como badge visível, fonte única continua sendo `billing_plan` por evento (padrão Stripe: plano vive na subscription, não no customer). Junto: contraste do lima (`#e3ff0a`) corrigido em 5 telas que ficavam ilegíveis no light mode; **falso-positivo real** no card "Comissões retidas +14 dias" (contava refund 100% como travado, mesmo sendo ignorado de propósito pelo cron `daily-release-funds`) — era a causa do alerta de R$72 que preocupou o produtor, não bug de payout; ícones $ (Tornar gratuito) e gráfico (Editar comissão) estavam trocados. Detalhes em [[superadmin_billing_plan_badges_shipado_2026_09_16]].
- **Vitrine — título "Entrada gratuita" vira "Acesso ao evento" quando há nota customizada**: disparado pela Giselle (equipe da Tamoios) pedindo pra tirar "GRATUITA" de um banner — com razão, entrada por 1kg de alimento não é gratuita de verdade. A nota (`entrada_gratuita_nota`, shipada 2026-09-15) já cobria o parágrafo, mas o título fixo acima continuava sempre "Entrada gratuita". Agora, com nota preenchida, o título vira neutro ("Acesso ao evento"); sem nota, comportamento de sempre. Generaliza pra qualquer produtor com entrada solidária/condicional, não só a Tamoios.
- **Validação**: `npm run lint` + `npm run build` limpos em cada commit.

### 2026-09-15 — Performance do domínio de marketing (PSI mobile 70→~90, desktop 87→97) ✅ SHIPADO EM PRODUÇÃO

3 commits (`f3d36ab` → `fa5ee17`, dev→main fast-forward), sessão fora do VS Code disparada por auditoria PageSpeed Insights do `coreohub.com`. `App.tsx` importava eager várias telas autenticadas (Dashboard, Bailarinos, MinhasCoreografias, CentralDeMidia, Profile, Auth) mesmo na landing pública — mesmo bundle Vite serve `coreohub.com` (marketing) e `app.coreohub.com` (app autenticado).

- Telas internas viraram `lazy()` — bundle principal 581 KB → 336 KB.
- Hero responsivo (`hero-festival-mobile.webp` via srcset/sizes) + Google Fonts async + domínio de marketing não espera mais `supabase.auth.getSession()` antes de pintar.
- Framer Motion removido da LandingPage (fade-in vira CSS keyframe puro); `Sidebar`/`Header`/`BottomNavBar`/banners viram `lazy()` (só existem no `PrivateLayout`, mas forçavam vendor-motion/vendor-supabase em qualquer rota); `supabase-js` vira `import()` dinâmico.
- **Resultado real** (múltiplas rodadas PSI em prod): mobile 70 → média ~90; desktop 87 → 97.
- **Decisão explícita**: não separar a landing como site estático isolado — manutenção duplicada não compensa o resíduo de JS não usado.
- **Ressalva de baixo risco registrada**: em `coreohub.com`, `App.tsx` não rastreia sessão nenhuma (auth `useEffect` dá `return` cedo nesse hostname) — seguro hoje porque todo link "Entrar" da landing aponta pra `https://app.coreohub.com/login` (cross-domain). Se um dia trocarem esses links pra apontar pro `/login` interno do próprio domínio de marketing, login vai quebrar (sessão nunca populada). Lembrar antes de mexer nesses links.
- **Validação**: `tsc --noEmit` + 92 testes + `npm run build` limpos, smoke manual via `vite preview`.

Detalhes em [[perf_psi_landing_lazy_load_2026_09_15]].

### 2026-09-15 (continuação) — Preço progressivo por bailarino (feature nova) + Tamoios configurado do zero ✅ SHIPADO EM PRODUÇÃO

4 commits (`63d6806` → `d0dcd5c`), disparado por produtora nova (Danielle Costa, II Mostra Regional de Dança de Tamoios) travada desde a criação do evento — `formacoes_config` vazio, nunca tinha configurado preço, o que também disparava sem ela entender o gate "Ativar evento gratuito" (`is_event_fully_free`, ver 2026-08-02).

- **Feature nova genérica — `pricingType: 'PROGRESSIVE_PER_DANCER'`** em `formacoes_config`: preço da coreografia cai conforme o MESMO bailarino acumula coreografias pagas no evento (1ª cheia, seguintes mais baratas, pode zerar/travar num teto a partir de certa posição). **Confirmado como padrão real de mercado via pesquisa** (não feature de nicho pra 1 evento só) — Festival Caixa Preta 2026 e Festival Adhering de Dança fazem exatamente isso. RPC `count_dancer_paid_registrations` (mesmo padrão de join `elenco`↔`bailarinos_detalhes` já usado em `detect_workshop_combo`) + editor de faixas na aba Formações (toggle "vale daqui em diante" pra faixa recorrente) + preço sempre recalculado no servidor em `create-payment-asaas`, nunca confiado no client. **Decisão registrada**: cancelamento/estorno não recalcula retroativamente a posição das outras coreografias do mesmo bailarino — mesmo padrão observado nos regulamentos reais pesquisados, que não tratam esse caso.
- **3 bugs reais pegos e corrigidos na mesma sessão** (nenhum sobreviveu até o fim): (1) interpretação errada do "4ª em diante: R$60 (não sofre alteração)" do regulamento — configurado a princípio como valor MARGINAL repetido a cada coreografia extra, quando na verdade R$60 é o TOTAL que trava a partir da 4ª (corrigido: marginal 4ª=R$15 fecha os R$60, 5ª+=R$0). (2) `formacoesAdapted` calculava `fee`/`base_fee` de exibição a partir de `lotes[0]`, vazio pro tipo progressivo — qualquer save na tela zerava o preço mostrado na vitrine (cobrança real sempre esteve certa, só a exibição quebrava). (3) `detectarMinFixo` só reconhecia nome EXATO ("solo","duo"...) — nomear os formatos com sufixo do modelo de cobrança ("Solo — Taxa Fixa") fazia o teto de participantes sempre abrir pra 99 a cada save. Corrigido pra considerar só o texto antes do "—"/"-" + novo helper `detectarMaxComposto` pra faixas tipo "Duo/Trio" (intervalo 2-3, não é 1 tamanho fixo).
- **Armadilha operacional real**: os bugs 2 e 3 pareciam "voltar sozinhos" depois de corrigidos — na verdade era a aba de Configurações aberta desde antes de cada deploy, rodando o JS antigo do PWA (`registerType: 'autoUpdate'`) e revertendo o fix ao salvar. **✅ Resolvido no dia seguinte** — ver Histórico recente 2026-09-16 (banner de update).
- **Evento configurado 100% a partir do regulamento**: 6 formações (Solo/Duo-Trio/Grupo × Taxa Fixa/Pacote Progressivo, tempos 4min/6min por tamanho), 10 estilos (`event_styles`), 6 faixas etárias, descrição da vitrine reescrita citando os 2 modelos. **Feature nova pequena junto**: `configuracoes.entrada_gratuita_nota` (opcional, 200 chars) — produtor com evento gratuito pode substituir o texto fixo "Não é necessário ingresso..." por uma nota própria (ex: entrada solidária com doação de alimento), editável em Configurações → Geral quando a política é Gratuito.
- **Bug real achado no mesmo evento — URL da vitrine não sincronizava com o nome** (commit `4796a4c`): Danielle digitou o nome errado na criação, a URL nasceu do typo junto, e corrigir o nome depois nunca corrigiu a URL — ficou presa no erro original até o preview do WhatsApp falhar pra visitante anônimo. Fix: ao salvar Configurações, se o nome mudou E a URL ainda bate com o que o nome ANTIGO geraria sozinho (nunca customizada na mão), regenera a URL automaticamente + grava a antiga em `event_slug_history` (link velho continua redirecionando). Padrão "self-healing URL" confirmado via pesquisa (Amazon/Medium) — respeita URL customizada manualmente.
- **FocalPointPicker (capa do evento) — 2 melhorias** (commits `ad261ff`, `f80cd7d`): indicador visual (bolinha + pulse durante drag) sinalizando que os previews Card/Banner sincronizam ao vivo; badge "corte forte" (âmbar, não bloqueante) quando a proporção da imagem diverge demais do preview (ex: poster vertical vs banner largo) — pesquisa de mercado (Payload/WordPress/DatoCMS) confirmou que nenhum concorrente oferece múltiplos pontos focais, só aviso quando o recorte fica ruim.
- **Outro produtor — Wizard aprovava inscrição gratuita errado** (commit `02cb460`, achado no mesmo dia, evento do Will Nunes/Extreme Festival): texto prometia aprovação automática pra formação R$0 mas sempre mandava pro carrinho PENDENTE — 7 inscrições reais travadas nesse limbo, corrigidas com backfill.
- **Polimento à parte**: proxy same-origin `api/documento.ts` pro link de "Baixar regulamento"/documentos extras da vitrine (antes apontava direto pro Supabase Storage — cross-origin quebrava o `download`, clique abria aba nova mostrando a URL crua do supabase.co) + limite de caracteres da Descrição do Evento subiu de 600 pra 1500 (o valor antigo não tinha nenhuma justificativa de mercado documentada, era só um default arbitrário no código).
- **Pendente**: confirmar com a Danielle se mínimo de 4 participantes pra "Grupo" faz sentido pra ela — o regulamento dela nunca define esse número, foi assumido por padrão de mercado (mesmo corte usado pelo Caixa Preta).
- **Validação**: `npm run lint` + `npm run build` + 92 testes limpos em cada commit.

### 2026-09-13 (continuação) — billing_plan sincroniza workshop+ingresso + simulador de /planos + fixes SEO/AEO/GEO ✅ SHIPADO EM PRODUÇÃO — 🚨 pendência de cobrança real

7 commits (`c8949f6` → `f9b8d5f`), merge `dev→main` feito (fast-forward). Disparado por gap real testando com a Lorrayne (Vicenza Dance Camp 2027, evento `8613207d-...`, mesma conta do [[lyris_migracao_e_proposta_comercial]] — ver entrada anterior do mesmo dia): ela via 10% no checkout de workshop mesmo depois do evento estar no plano Essencial (5%). Detalhes completos em `memory/billing_plan_sync_workshop_audience_shipado.md`.

- **Causa raiz**: o trigger `sync_commission_percent_from_billing_plan` (20260904) só sincronizava `events.commission_percent` — cobrindo inscrições. Workshops (`workshops.workshop_commission_percent`) e ingressos de plateia (`events.audience_commission_percent`) usavam campos totalmente separados, default hardcoded `10`, nunca escutando `billing_plan`.
- **2 migrations novas** (`20260913c`/`20260913d`, smoke transacional antes de aplicar): trigger AFTER UPDATE OF billing_plan propaga pra `workshop_commission_percent` de todos os workshops do evento; o trigger BEFORE já existente ganha também `audience_commission_percent`. Ambas com coluna `_manual` pra preservar override feito via SQL direto (nenhum dos 2 campos tem UI pro produtor editar — só super admin, e hoje só via banco). **Achado de segurança fechado junto**: `audience_commission_percent` nunca esteve protegido por `protect_commission_columns` — produtor podia, em tese, zerar a própria comissão de ingresso via API direta.
- Frontend: `WorkshopsManagement.tsx`/`RegulationAIParser.tsx` passam a herdar o `commission_percent` vigente do evento ao criar workshop (antes hardcode `10`). Evento real da Lorrayne corrigido pra Essencial (5%) nos 3 campos.
- **`/planos` — simulador ganha campo numérico**: "Simule seu festival" só tinha slider (sem opção de digitar), pedido do produtor. Novo componente `SimField` — pill editável no lime `#E3FF0A` (mesmo tema dos badges de plano abaixo), sincronizado nos 2 sentidos com o slider, clampa pro limite só no blur (não a cada tecla, senão travava digitar "150" partindo de "1"). Mobile: fonte 16px+ (evita zoom automático do Safari/iOS), legendas de mínimo/máximo voltaram a aparecer.
- **Verificação SEO/AEO/GEO pedida explicitamente após as mudanças, achou 3 gaps reais**: `api/og-marketing.ts` tinha a description do `/planos` hardcoded e desatualizada — essa rota estática é servida especificamente pros bots que NÃO executam JS (matcher em `vercel.json` inclui GPTBot/ClaudeBot/PerplexityBot/Google-Extended/Amazonbot/CCBot/etc, exatamente os crawlers de GEO/AEO), corrigida. `/planos` nunca esteve no sitemap dinâmico (`supabase/functions/sitemap-xml`) — adicionado, deployado via CLI, confirmado ao vivo em produção. `components/OnboardingWizard.tsx` (resumo do plano no `/criar-evento`) também tinha "10% sobre venda" desatualizado — corrigido.
- **🚨 Pendência real, não é código**: corrigi o `billing_plan` da Lorrayne direto via SQL (fora do fluxo normal `create-plan-fixed-fee-payment` + webhook `PLANFEE:`) — isso contornou a cobrança adiantada de R$250 que o Essencial exige. Confirmado no banco: `billing_plan_fixed_fee_paid_at IS NULL`. Ela está no plano de 5% sem ter pago o fixo. Decisão combinada: mandar WhatsApp avisando as duas coisas (comissão caiu pra 5% + cobrança dos R$250 sai amanhã, 2026-09-14) antes de gerar a cobrança — mensagem rascunhada na sessão, não enviada ainda, cobrança não montada ainda.
- **Discussão de estratégia (não implementada, só decidida observar)**: produtor questionou se deixar produtor iniciante entrar no Essencial pagando R$250 é perda de receita. Matemática: break-even Começo×Essencial é R$5.000 de faturamento — abaixo disso o fixo favorece a CoreoHub (funciona como piso), acima disso o Começo teria dado mais receita, e Essencial não tem acerto de fechamento (diferente do Escala) pra recuperar essa diferença depois. Risco real identificado: não existe hoje nenhuma qualificação antes de liberar Essencial — seleção adversa em potencial. Decisão: cedo pra mudar a régua com só 1 cliente pagando de verdade no modelo; observar padrão antes de implementar qualquer gate. Detalhes em `memory/decisao_pricing_essencial_vs_comeco_breakeven.md`.
- Validação: `npm run lint` + `npm run build` + `npm test` (92 testes) limpos em cada commit. Screenshot Playwright local (dev server) do simulador expandido, mobile+desktop, 0 console errors, sem overflow, teste funcional do clamp do input numérico (999→500, 123→125, 7→5).

### 2026-09-13 — Vicenza Dance Camp: lote avança por quantidade + description no card + 2 gaps reais achados e corrigidos ✅ SHIPADO E VALIDADO EM PRODUÇÃO

Continuação da configuração do evento Vicenza Dance Camp 2027 (Lorrayne, mesma conta do Lyris Dance Competition — ver [[vicenza_dance_camp_planejamento_2026_09_12]]). 3 commits de código (`b016d38`, `b45a0ad`, `bd90bd2`) + 1 doc (`1d1897e`), merge `dev→main` autorizado e feito (fast-forward, sem conflitos), em produção real.

- **`get_workshop_stock()` recriada** — escolha do lote ativo agora considera esgotamento por QUANTIDADE, não só janela de data. Regra em 2 estágios: lotes com `data_inicio` explícita continuam preferindo a maior ordem já iniciada (preserva o fix de 2026-06-23); sem isso, cai pros lotes sem nenhuma data e pega o de menor ordem ainda não esgotado por `quantidade_maxima`, avançando sozinho quando esgota. Padrão confirmado em Eventbrite/Ticket Tailor (tiered pricing com progressão automática por quantidade) antes de implementar. **Bug achado no smoke**: rascunho tinha `SELECT capacidade_max ... FROM workshops` sem alias — ambíguo com o OUT param da própria função, o mesmo erro já corrigido uma vez em 2026-06-24 (`w.capacidade_max`) que se perdeu ao recriar a função do zero. Corrigido antes de aplicar. Validado via smoke transacional (BEGIN/ROLLBACK) com 2 cenários: progressão só-por-quantidade (0→14→15 vendidos, troca exata no 15º) + regressão do caso por data de 2026-06-23 — ambos OK.
- **`workshops.description` passa a renderizar no card destacado da vitrine** (`pages/PublicEventPage.tsx`) — campo já existia no schema, nunca era buscado nem exibido. Adicionado ao select + tipo do state + parágrafo condicional (só quando `is_featured`).
- **Descrição do evento enriquecida** — parágrafo real da Lorrayne (bolsas/oportunidades/conexões, texto dela, não reescrito) apendado em `events.description` + `configuracoes.descricao` (dual-row), via SQL idempotente.
- **🚨 Gap 1 — achado DEPOIS de mandar a mensagem de status pra cliente**: auditoria completa no banco pra confirmar o que foi comunicado revelou que o Vicenza Experience só tinha **1 lote cadastrado** (Lançamento, R$697, `quantidade_maxima=15`) — nenhum 2º lote. Sem ele, quando as 15 vagas esgotassem, `get_workshop_stock` não achava nenhum lote ativo, e 2 coisas ruins aconteciam ao mesmo tempo: o checkout caía no fallback `preco_padrao` (não subia de preço, quebrando a promessa) E a flag de esgotado ficava `FALSE` por acidente do fallback (venda ilimitada e silenciosa a R$697). Corrigido com INSERT direto do "1º Lote" (ordem 2, R$797 — valor já mencionado por ela na config original, sem limite de quantidade). Confirmado via `get_workshop_stock` ao vivo pós-fix.
- **🚨 Gap 2 — achado via `/code-review` dos próprios commits, era regressão ao vivo em produção**: `utils/lotes.ts` tinha `resolveActiveWorkshopLot()` — cópia client-side da regra de "qual lote está ativo", extraída em 2026-06-20 pra deduplicar `PublicEventPage.tsx`/`WorkshopsManagement.tsx`/`PublicWorkshopPage.tsx`, com docstring afirmando "mesma regra do RPC `get_workshop_stock`". A migration do Gap 1 tornou o RPC quantity-aware, mas ninguém tocou essa função gêmea — ela continuou só data/`is_active`, sem noção de `quantidade_maxima`/vendidos. Resultado real reproduzido via Playwright: card do Vicenza Experience na vitrine mostrava "1º LOTE R$797,00" com **0 vendas** (deveria ser "LANÇAMENTO R$697,00", que é o que o checkout de fato cobra) — o card e o checkout mostravam preços DIFERENTES pro mesmo produto. Mesmo bug no painel do produtor (`WorkshopsManagement.tsx`). Fix: as 2 telas passam a chamar `get_workshop_stock` via RPC (`Promise.all` por workshop) em vez de duplicar a lógica em TS — fonte única de verdade. `resolveActiveWorkshopLot()` virou código morto e foi removida de `utils/lotes.ts` (nenhum caller restante). `PublicWorkshopPage.tsx` não tinha esse bug (já usava a RPC pro preço, só `findNextWorkshopLot` client-side pro aviso "sobe em X dias", que é cosmético).
- **Lição consolidada dos 2 gaps**: toda feature tipo "avança sozinho entre lotes/tiers" comunicada como pronta merece (1) checar se TODOS os degraus da escada existem, não só o 1º, e (2) rodar `/code-review` sobre o próprio diff antes de considerar pronto — o Gap 2 só apareceu porque o review comparou a mudança no RPC contra os outros lugares que replicavam a mesma regra de negócio; sem isso, o bug ficaria em produção indefinidamente (0 console errors, 0 crash — só preço errado, silencioso).
- **Validação final**: `npm run lint` + `npm run build` + 92 testes limpos nos 3 commits de código. Playwright contra produção real (`--as=admin`, storage state regenerado) confirmou o antes/depois do Gap 2 — screenshot pré-fix mostrando "1º LOTE R$797,00", screenshot pós-fix (mesmo evento, mesmo estado do banco) mostrando "LANÇAMENTO R$697,00" correto, desktop+mobile, 0 console errors, 0 overflow. Painel do produtor (`/workshops-do-evento`) revalidado com conta real (Hemer/Usualdance, workshops com múltiplos lotes de verdade) — sem crash, preços corretos.

### 2026-09-10 — Auditoria completa de código morto (Fases 1-3) ✅ SHIPADO — encerrada de propósito antes da Fase 4 granular

5 commits em `dev` (`de12835` → `e87b639`, todos pushados). Primeira varredura sistemática de código morto do projeto — nunca tinha sido feita. Metodologia: ferramentas determinísticas (`ts-prune` + `depcheck`; `knip` foi tentado primeiro mas quebrou por um bug nativo do `oxc-parser` em Windows/Node 24, `RangeError: Array buffer allocation failed`, sem contorno viável) pra achar candidatos, seguido de verificação manual caso a caso (histórico git, grep amplo incluindo `.rpc()` e migrations SQL, contagem de linhas reais em prod) antes de qualquer remoção — pego 3 falsos positivos no caminho (`minor_guardian_consents`/`producer_terms_acceptances`, acessadas só via RPC; `sweepProducerBalance`, invisível pro `ts-prune` porque `tsconfig.json` exclui `supabase/functions`).

- **Fase 1 — Frontend (`de12835`)**: 5 páginas órfãs do commit inicial do scaffold ("Dance Pro Festival", antes do rebrand) — `JudgeCompetencyManager.tsx`, `ShiftChangeOverlay.tsx`, `TeamManagement.tsx`, `EventConfig.tsx`, `FestivalShowcase.tsx` (essa já tinha morte documentada em `App.tsx`, substituída por `FestivalRootRedirect`). Cluster de batalha abandonado — `services/battle.ts` + `components/BracketTree.tsx` + `components/JudgeBattleVote.tsx` (confirmado que o Battle Arena Live real, já shipado, não usa nada disso — é tentativa anterior substituída). 2 componentes residuais do scaffold — `EventFormatSelector.tsx` (função já reimplementada em `CreateEvent.tsx`), `SupabaseConnectionTest.tsx` (widget de debug nunca usado). 3 deps npm mortas — `@supabase/auth-ui-react`, `@supabase/auth-ui-shared`, `react-markdown`. **Regressão real causada e corrigida na hora**: remover as libs Supabase levou `@types/react` junto (dependência transitiva oculta nunca declarada própria) — `tsc --noEmit` quebrou em ~35 lugares não relacionados, corrigido reinstalando `@types/react`/`@types/react-dom` como devDependency explícita.
- **Fase 2 — Edge functions (`2b8fc93`)**: as 61 functions locais batem 100% com o que está deployado em prod — zero código local nunca deployado. Mas achado o inverso: **3 functions deployadas em produção sem nenhum código-fonte no repo** — `mp-webhook` (rodando com `verify_jwt:false`, endpoint público exposto há meses), `create-payment`, `mp-oauth-callback` — resíduo real da migração Mercado Pago → Asaas (2026-05-15), documentada como "legacy MP removido" mas nunca desligada de fato no Supabase. Apagadas via `supabase functions delete` com autorização explícita.
- **Fase 3 — Banco de dados (`69c00ee`, `9d55d81`, `e87b639`)**: 2 tabelas fantasma com 0 linhas, zero código, nunca versionadas em nenhuma migration (`equipe_convites` — superada por `team_invites`; `popular_votes` — tentativa antiga de votação dentro do banco da CoreoHub, abandonada quando o Voto Popular virou produto em infra isolada). 25 colunas mortas em 6 tabelas — `profiles.instagram_consent` (opt-in LGPD de marcação no Instagram planejado em 2026-05-15, nunca virou UI nenhuma — achado de compliance, não só código morto); 11 colunas de `configuracoes` + `coreografias.bailarinos_ids` + `judges.assigned_categories` + `profiles.perfil_tipo` + `registrations.nome_coreografo` + `evaluations.audio_feedback_url` (resíduo do schema inicial do scaffold, substituídas por `formacoes_config`/`event_styles`/`elenco`/`competencias_generos`/`role`/`audio_url`); `evaluations.final_score`/`text_comments` (a própria migration de origem já documentava como substituídas por `final_weighted_average`/`feedback_text`); `payments.swept_*`/`profiles.sweep_*` (mecanismo de auto-saque abandonado, substituído pelo Settlement D+7 de 2026-05-20). Schema de cada tabela/coluna registrado antes do `DROP` (sem backup automático no plano Free).
- **Fase 4 — julgamento granular, decidido NÃO fazer**: o restante da lista do `ts-prune` (funções soltas dentro de arquivos que continuam vivos, ex. `formatWhatsApp`, `unenrollFactor`) tem risco maior de falso positivo (mesma limitação do `tsconfig` excluir `supabase/functions`) e ganho baixo (1 função de poucas linhas, não uma tela/tabela inteira) — encerrado por decisão explícita do produtor.
- **Validação**: `npm run lint` + `npm run build` + 92 testes limpos depois de cada bloco de remoção (frontend e banco). Mudança de banco aplicada direto em produção via CLI (mesmo padrão já documentado do projeto), sempre com contagem de linhas reais conferida antes do `DROP` e confirmação explícita do produtor por tabela/grupo de coluna.
- **Achado colateral — CLAUDE.md desatualizado 2x na mesma sessão**: os números de `pages/`/`components/`/`edge functions`/`migrations` no topo deste arquivo estavam errados por quase 2x antes da sessão (~71/~30/31/90+ vs. real 82/43/61/192) — e voltaram a mudar de novo só de rodar a própria limpeza (77/39/61/195). Reforça que esses números merecem reconferir a cada sessão grande de refactor, não só documentar 1x.
- **Pendente**: nenhuma pendência de validação — toda remoção já passou por build+lint+testes e checagem de dado real em produção antes de aplicar. Sem necessidade de teste manual em staging (nenhum `.select()` explícito nomeava as colunas apagadas, confirmado por grep antes do `DROP`).

### 2026-08-16 (continuação) — CoreoHub no feminino + 3 causas raiz de console errors resolvidas ✅ SHIPADO

Commit `877f12b`. Continuação da sessão de modo claro no mesmo dia — 2 pedidos do produtor: (1) "a CoreoHub" no feminino em vez de "o CoreoHub" (preferência de marca), (2) investigar de verdade os 4 itens deixados como "não corrigido" na sessão anterior, em vez de deixar como pendência genérica.

- **CoreoHub no feminino**: grep completo por artigo masculino antes de "CoreoHub" achou 38 ocorrências — 28 eram comentário de código/JSDoc (não user-facing, deixados como estão) e 10 eram copy real visto pelo usuário, corrigidas: banner de instalar PWA, aviso de erro do sistema (corpo de email mailto), 2 textos em Integrações de Marketing, texto de upload de certificado custom, convite de equipe via WhatsApp, 2 textos da Landing Estúdios, 2 emails transacionais (`auth-email-hook`: convite + notificação genérica). Convenção registrada no topo do `CLAUDE.md` pra não regredir.
- **`/evento/:slug` (401)**: `track-event-view` (conta visitas anônimas pro funil Visitas→Leads→Pagas, feature de 2026-05-28) foi deployada com `verify_jwt: true` — bloqueava QUALQUER visitante anônimo desde a criação, mesmo o próprio código comentando "Não exige auth — vitrine é pública". Achado idêntico ao bug do `get-winner` (2026-07-10). Redeployada com `--no-verify-jwt`. **A métrica "Visitas" do funil no `ProducerDashboard` pode estar zerada/subestimada desde 28/05** — não investigado quanto histórico foi perdido (métrica é só contagem, sem replay possível).
- **`/ingressos` (400×2) — bug funcional real, não só console noise**: `pages/Ingressos.tsx` selecionava `preco_pago` de `audience_tickets`, coluna que não existe (a real é `preco`). Como o código só desestruturava `data` sem checar `error` (mesmo anti-padrão documentado múltiplas vezes no projeto), a query falhava silenciosamente e "Meus Ingressos" **sempre pareceu vazio pra qualquer comprador logado**, indistinguível de "você não tem ingresso nenhum". Corrigido nome da coluna + `error` agora logado.
- **`/manage-schedule` mobile (35 erros) — 2 causas empilhadas**: (1) RPC `get_schedule_publish_read_stats` (migration `20260709`, feature "quantos inscritos leram o aviso de cronograma publicado") nunca tinha sido aplicada em produção — clássico caso de "migration commitada ≠ aplicada", aplicada agora via CLI. (2) 34 trilhas do Usualdance Festival com `trilha_url` apontando pra arquivos **já apagados** — mesmo incidente de Egress de 2026-07-15 (`cleanup-orphan-tracks` deletou 36 trilhas reais por engano, produtor já tinha backup próprio, sem perda real) — só que o campo `trilha_url` nunca foi limpo pra maioria delas (só 7 na época). `Schedule.tsx` sonda a duração de toda trilha via `preload='metadata'` assim que a tela abre (`pumpTrackDurationQueue`), então cada trilha morta virava 1 erro 400 garantido, toda vez. Autorizado pelo produtor: `UPDATE registrations SET trilha_url = NULL` pras 37 registrations afetadas do evento (já encerrado, sem necessidade operacional da trilha).
- **Item vago "abas cortadas/badge quebrando linha" do painel do inscrito**: localizado em `/minhas-coreografias` — régua de abas "Próximas/Todas/Em análise/Anteriores" tinha `overflow-x-auto` real, mas sem nenhum indicador visual, então a 4ª aba aparecia com só uma lasquinha de texto cortada na borda direita, parecendo bug de layout. Fix: mesmo fade `mask-image` já usado em `EventAnchorNav.tsx` e na tabela de Cupons (shipado mais cedo no mesmo dia).
- **Método**: dev server local + Playwright com storage state próprio (login real contra `localhost:3000`, não produção) + listener de `response`/`pageerror` pra capturar status code e corpo do erro de verdade, não só a contagem. Todas as 4 rotas re-testadas depois do fix — 0 erros em todas.

### 2026-08-16 — Modo claro como padrão (app interno) ✅ SHIPADO na staging

Execução do plano da auditoria de 2026-08-08 (ver P2 acima), Opção B: claro vira padrão só pro app interno (produtor/inscrito/admin), vitrine pública e telas de palco continuam escuras. Escopo confirmado explicitamente com o produtor antes de codar.

- **5 bloqueadores corrigidos**: `AIAnalysis.tsx` (tela inteira estava presa ao dark, ganhou `PageHeader` + tokens claros), `JudgeManagement.tsx`/`equipe-jurados-config` (cards + tags de competência), `TelaoControle.tsx` (card do código do telão virou fundo escuro fixo intencional — decoração, não segue tema), `BattleConfig.tsx` (cards "Parâmetros Técnicos"/"Preview do Chaveamento"), `RegistrationGradeConfig.tsx` (stub ganhou `PageHeader` + título traduzido + `break-words`).
- **Padrão sistêmico corrigido na causa raiz**: `disabled:opacity-XX` sobre botão de cor saturada (`bg-[#ff0068]`/emerald) lava pra pastel de baixo contraste quando a página de fundo é branca — trocado por cores de estado desabilitado explícitas (`disabled:bg-slate-200 dark:disabled:bg-white/10` etc.) em `ProducerBalanceCard.tsx` ("Transferir agora"), `OnboardingWizard.tsx` (2 botões "Continuar"), `VendasIngressos.tsx` ("Vender no balcão"), `ValidarCertificado.tsx` ("Validar"). Ícones de power/toggle em `Coupons.tsx`/`Avisos.tsx` (`text-slate-400`→`text-slate-500 dark:text-slate-400`).
- **Telas de palco confirmadas seguras sem tocar em código**: Marcação de Palco, Telão preview/Display, Battle Arena Live já usavam fundo hardcoded sem nenhum `dark:` prefix — nunca dependeram do toggle global, então trocar `App.tsx` default de `'dark'`→`'light'` não as afeta.
- **3 bugs funcionais achados no caminho, corrigidos por pedido explícito** (fora do escopo de tema): tabela de Cupons ganhou fade `mask-image` sinalizando scroll horizontal no mobile (mesmo padrão do anchor nav da vitrine, `679935d`); bio do professor duplicada no workshop `jazz-funk-com-jonathan-lupe` — dado real (`professor_bio_short` quase idêntico a `professor_bio`), nulado direto no banco; ícone de espadas sobrepondo título no Battle Arena mobile — tamanhos/gaps viraram responsivos (`BattleArenaLive.tsx`).
- **✅ Os 4 itens não corrigidos na sessão anterior — todos resolvidos em 2026-08-16 (commit `877f12b`) via reprodução ao vivo com Playwright + storage state contra dev server local**: `/evento/:slug` (401) — `track-event-view` deployada com `verify_jwt: true` por engano, bloqueava todo visitante anônimo desde a criação (a métrica "Visitas" do funil pode ter ficado zerada esse tempo todo), redeployada com `--no-verify-jwt`. `/ingressos` (400×2) — **bug funcional real**, `Ingressos.tsx` selecionava `preco_pago` (não existe em `audience_tickets`, é `preco`) — "Meus Ingressos" nunca mostrou nenhum ingresso comprado pra ninguém logado, sempre parecia vazio. `/manage-schedule` mobile (35 erros) — RPC `get_schedule_publish_read_stats` commitada em 09/07 mas nunca aplicada em prod (aplicada agora) + 34 trilhas do Usualdance Festival apontando pra arquivos já apagados no incidente de Egress de 15/07 (autorizado pelo produtor, `trilha_url` zerado pras 37 registrations afetadas). Item vago do painel do inscrito — localizado em `/minhas-coreografias`: 4ª aba ("Anteriores") cortada sem indicador de scroll, fade `mask-image` aplicado.
- **Validação**: `tsc --noEmit` + `npm run build` limpos. Playwright local (dev server, storage state próprio pra localhost) nas 11 rotas tocadas, desktop+mobile, tema claro — 0 overflow, os únicos console errors capturados são pré-existentes e não relacionados (404 de asset em dev server, bug de realtime subscription já existente em `VendasIngressos.tsx`).
- **Pendente**: dois achados foram descobertos e corrigidos ANTES desta sessão mas não documentados até agora, achados enquanto se confirmava histórico com o produtor — checagem de áudio pré-entrada do júri (2026-07-16) e Telão com LED físico (validado ao vivo no Usualdance, 11/07) já estavam de fato prontos, só a memória estava desatualizada (corrigido nesta sessão, ver entradas correspondentes acima). Fluxo de Premiação (registra-primeiro-revela-depois) segue sem confirmação de uso ao vivo.

### 2026-08-06/07 — Apuração: agrupamento por Estilo+Formação+Subgênero + nome de faixa configurável ✅ SHIPADO (8 commits em prod)

Disparado por reclamação do produtor Bheto (19° Festival Ecodança): "está uma bagunça, Trio competindo com Grupo em Apuração". Virou correção de plataforma inteira depois de auditoria pós-fix achar o mesmo bug, bem maior, no Usualdance Festival (evento real já realizado).

- **Bug raiz**: `pages/ResultsPanel.tsx` agrupava ranking só por `estilo_danca|categoria`, ignorando **Formação** (Solo/Duo/Trio/Conjunto) e **Subgênero** (ex: K-Pop → Cover/Creative). Corrigido pra `estilo|categoria|formação|subgênero` em tela, PDF e CSV — aplica a toda a plataforma, não só o evento que reportou. Mesma info (Formação + Subgênero) adicionada em `Schedule.tsx` (badge + PDF "Ordem de Apresentação") e `JudgeTerminal.tsx` (5 pontos de exibição), pra locutor/jurado nunca verem "Estilo Livre · Infantil" sem saber qual formação é.
- **🚨 Achado maior na auditoria pós-fix**: Usualdance Festival (real, já aconteceu em 11/07) tinha grupo "Jazz/Contemporâneo · Adulto" misturando **10 inscrições** entre Solo/Duo/Trio/Grupo — pior que o caso do Bheto que motivou a sessão. ~19 registrations já tinham `resultado_publicado=true`/`classificacao_final` calculados com o agrupamento errado. **Verificado**: nenhum certificado com esses números foi baixado ainda (só 3 rows em `certificates_issued`, nenhuma das afetadas) e as MEDALHAS não mudam (ambos eventos são `THRESHOLD` — nota mínima decide a medalha, não posição) — só o número cru de posição fica desatualizado até o produtor clicar "Publicar Resultados" de novo. Sem dano real, mas precisa avisar Hemer e Bheto antes de republicarem.
- **Nome de faixa configurável** (`configuracoes.medal_labels`, JSONB novo): regulamento do Bheto (seção 15) chama a faixa de nota mínima de "1º/2º/3º Lugar", não "Ouro/Prata/Bronze" — mecanismo continua THRESHOLD, só o texto muda. UI em Configurações → Avaliação → Faixas de Medalha: 3 cartões seletores (Ouro/Prata/Bronze | 1º/2º/3º Lugar | Personalizado), só o Personalizado revela campo de texto. Default null preserva todo evento existente.
- **Bug corrigido no caminho**: `utils/awardClassification.ts` `classifyAward()` só reconhecia "ouro/prata/bronze" no nome do prêmio especial — os 3 troféus que o Bheto já tinha configurado numa sessão anterior ("Troféu 1º/2º/3º Lugar") caíam como deliberação manual em vez de faixa automática por nota. Corrigido regex pra reconhecer "1º/2º/3º lugar"/"primeiro/segundo/terceiro lugar".
- **Bug corrigido no caminho (UX)**: seletor de faixa (3 cartões) tinha "qual está ativo" deduzido comparando valores — clicar em "Personalizado" quando já estava em "1º/2º/3º Lugar" reaplicava os mesmos valores e a dedução nunca detectava a troca (produtor: "não parece fazer nada"). Trocado por estado explícito (`medalLabelMode`), não mais deduzido. Lição em [[feedback-estado-ui-nao-deduzir-de-valores]].
- **Dados do evento do Bheto reconciliados** (escopados só a esse evento): `event_styles`/`registrations.estilo_danca` sem sufixo "e suas vertentes" (bate com documento oficial dele); "Encanto-(obs trio infantil)" corrigido de Duo pra Trio (documento oficial confirma); `registrations.categoria` normalizada pro nome curto oficial (tinha o texto descritivo do regulamento inteiro salvo, ex. "Infantil - de 7 a 12 anos" → "Infantil" — resultado de importação fora do Wizard); formação "Solo Masculino" (usada numa inscrição real) cadastrada em Configurações → Formações (preço cosmético, esse produtor usa só o Terminal de Júri avulso).
- **Pendente**: avisar Hemer (Usualdance) e Bheto (Ecodança) antes de republicarem resultados — números de posição vão mudar (pra melhor). Detalhes completos em [[apuracao-agrupamento-estilo-formacao-subgenero]].

### 2026-08-02 — Taxa de ativação pra evento 100% gratuito (Modelo B) ✅ SHIPADO E VALIDADO COM PIX REAL

Commit `cd35fe8`, disparado por pergunta do produtor sobre zerar a comissão do Will Nunes — virou discussão maior: nada impedia um produtor rodar evento 100% gratuito usando toda a infra da CoreoHub (IA de regulamento, narração, terminal de júri, certificados) sem gerar receita nenhuma (gap já documentado em backlog). Pesquisa de mercado em 2 rodadas (1ª ancorou errado em Bizzabo/Cvent; 2ª mostrou que Sympla/Even3/Doity dão evento grátis de graça, mas não são concorrentes diretos — CompetitionSuite/DanceComp Genie, o nicho de verdade, praticamente nunca oferece modo gratuito, sem benchmark de preço pra puxar de lá). Decisão: cobrar sim, sem gate por feature individual (terminal de júri/apuração é o produto em si pra esse público, não um extra).

- **Modelo**: R$297 (até 50 inscrições) / R$497 (51-150) / R$797 (151+), faixa por estimativa autodeclarada do produtor. Cobra só quando o evento é 100% gratuito (inscrição+workshop+ingresso+seletiva de vídeo todos R$0). Trava no momento de tentar inscrever, não na criação do evento. Todo evento que já existia na data da migration ficou isento retroativo (grandfather, validado 3/3). Se o produtor passar a cobrar algo depois, a taxa não se repete — comissão de 10% assume. Upgrade de faixa é automático (cobra só a diferença via Asaas quando ultrapassa o teto). Reaproveita `standalone_pricing_config` (mesma tabela do Terminal de Júri avulso do Bheto), categoria nova `setup_gratis`, editável em `/cotacoes-terminal-juri` → Configurar Valores sem deploy.
- **Arquitetura**: migration `20260802_setup_fee_evento_gratis.sql` (colunas `setup_fee_*` em `events`, função `is_event_fully_free()`, trigger `enforce_free_event_setup_fee` BEFORE INSERT em `registrations` bloqueando inscrição sem taxa paga ou acima do teto — bypass service_role/super admin). Edge function `create-setup-fee-payment` nova cobra o PRODUTOR direto na carteira master (sem split, sentido inverso do fluxo normal). Branch novo no `asaas-webhook` (prefixo `SETUP:`/`SETUPUP:`) confirma pagamento e libera o gate sozinho — trigger reavalia em tempo real no próximo INSERT, sem cron nem ação manual. `ProducerDashboard.tsx` ganha banner+modal de ativação (createPortal). `InscricaoWizard.tsx` traduz o erro do trigger pra mensagem amigável. `SuperAdmin.tsx` ganha coluna "Taxa Setup" + o modal "Tornar gratuito" (que virou modal custom nessa mesma sessão, substituindo confirm()/prompt() nativos do browser) agora também isenta da taxa de setup.
- **2 bugs de idempotência corrigidos na autorrevisão** antes de aplicar em prod: INSERT das 3 faixas usava `ON CONFLICT DO NOTHING` sem coluna-alvo (`chave` não tem UNIQUE, cairia no PK sempre novo — duplicaria numa 2ª execução) → trocado por `INSERT ... WHERE NOT EXISTS`; policy nova sem `DROP POLICY IF EXISTS` → corrigido.
- **Achado real do smoke**: Asaas rejeita cobrança com `billingType:'UNDEFINED'` abaixo de R$5,00 — não afeta os valores reais (R$297+), só limitou o quanto deu pra baratear o teste.
- **Smoke E2E com PIX real validado**: evento de teste isolado (conta do super admin, não mexeu em produtor real) → banner apareceu → modal gerou cobrança real (R$5, faixa rebaixada só pro teste) → PIX pago de verdade → webhook confirmou sozinho, sem ação manual → inscrição real nesse evento aceita depois do pagamento. Dados de teste limpos, faixa restaurada pro valor real (R$297).
- **Pendente**: upgrade de faixa nunca testado com pagamento real (só a ativação inicial foi validada ponta a ponta); `/super-admin` e `/cotacoes-terminal-juri` não validados visualmente (atrás do gate de MFA, sem código TOTP ao vivo disponível na sessão); nenhum produtor real pagou o valor cheio (R$297+) ainda, só o R$5 do smoke test.

Detalhes em `memory/setup_fee_evento_gratis_shipado.md`.

### 2026-07-19 — Equipe vs inscrito: home adaptativa + revogação de acesso pós-evento + 2 achados de segurança (1 P0 auto-causado) ✅ SHIPADO E VALIDADO EM PROD

7 commits (`3a4dc04` → `e9ff1eb`), disparado por relato do produtor: membro de equipe loga e cai na mesma tela do inscrito comum. Sessão longa em 3 etapas — fix do bug relatado, pesquisa de mercado + plano de UX aprovado, implementação do plano + revogação de acesso — com 2 achados de segurança reais no meio do caminho (um deles causado pela própria sessão, pego e corrigido antes de anunciar como pronto).

- **Causa raiz do bug relatado**: `Auth.tsx` só reconhecia `role==='ORGANIZER'` no redirect pós-login; `TeamInvite.tsx` (fluxo de aceite de convite) navegava hardcoded pra `/dashboard`, ignorando qualquer permissão. Fix: `resolveFirstEquipeRoute()` (`utils/permMenu.ts`, nova fonte única de `PERM_MENU`) manda equipe pra primeira rota que a permissão dela já libera, usado nos 2 pontos de entrada (login E aceite de convite).
- **🐛 Bug lateral achado no smoke — header mostrava "SUPER ADMIN" pra cargo de equipe**: `Header.tsx` tinha `ROLE_OPTIONS.find(...) ?? ROLE_OPTIONS[0]` — `ROLE_OPTIONS` só cobre os papéis do dropdown "Visão" do super admin, então cargo operacional (COORDENADOR/MESARIO/...) não achava match e caía silenciosamente no PRIMEIRO item da lista (Super Admin, por acaso). Confirmado em prod via interceptação da resposta real de `/rest/v1/profiles` (o banco tinha o role certo, o bug era só de render). Fix: `resolveRoleBadge()` com fallback neutro, nunca mais em admin.
- **Pesquisa de mercado + plano aprovado**: HubSpot 2026 (home adaptativa por papel, não account switcher) escolhido sobre padrão GitHub/Slack (trocar de contexto inteiro) — coordenador que também dança usa o MESMO evento/dado, só quer priorizar o que usa mais. `isEquipeAtiva()`/`showInscritoContent` (generaliza `isInscrito` pra também cobrir quem TEM inscrição mesmo com role de equipe) + abas leves "Meu evento"/"Minhas inscrições" só aparecem pra quem de fato tem os 2 papéis — maioria dos usuários não vê mudança nenhuma.
- **Revogação de acesso pós-evento** (aprovada explicitamente: cron automático, não logout forçado — JWT é stateless, revogar `permissoes_custom` já é suficiente já que RLS reavalia em toda request; grace period configurável; botão manual): `events.equipe_access_days_after` (default 30d) + cron `revoke-expired-team-access` (usa `configuracoes.data_evento`, não `events.event_date` — mesmo achado de sempre NULL já documentado em certificados) + botão "Encerrar acesso de todos" em `/minha-equipe`. **Validado em produção real pelo produtor** — revogou o acesso de 3 membros reais (Cultural Estúdio, Gravidade, Will Nunes), confirmado por screenshot antes/depois.
- **🚨 Achado de segurança #1 — P0 auto-causado**: a migration do dia anterior (`20260718_protect_permissoes_custom.sql`) protegeu a coluna contra escrita direta — mas `EquipeProdutor.tsx` ("Editar Permissões"/"Remover") fazia `UPDATE` direto do client, e o produtor real (ORGANIZER, não super admin) tinha esse UPDATE revertido silenciosamente pelo trigger, com a UI mostrando "sucesso" sem persistir. Pego durante a implementação (não em prod sem querer) — fix: nova edge function `manage-team-member` (service_role, valida ownership via `created_by` do evento) centraliza toda escrita privilegiada. Validado end-to-end: chamada real como produtor, `permissoes_custom` persistiu de verdade, e tentativa de mexer em evento de outro dono voltou 403.
- **🚨 Achado de segurança #2 — policy fantasma em `profiles`**: `"any_authenticated_updates_profile_safe"` (`USING(true) WITH CHECK(true)`) — 3ª ocorrência dessa MESMA classe de bug no projeto (depois de `registrations` 2026-05-22 e `events` 2026-07-17). Qualquer usuário logado podia sobrescrever `full_name`/`cpf_cnpj`/`pix_key`/`cargo` de QUALQUER outro perfil (colunas privilegiadas já eram protegidas pelo trigger, essas não). Dropada — a policy `auth.uid()=id` já cobre 100% do self-service legítimo.
- **`/code-review` pego ANTES de anunciar como pronto**: dropar a policy fantasma ia quebrar Check-in de equipe (`CheckIn.tsx` grava `last_checkin_at` no perfil de OUTRO colega ao escanear o QR dele — dependia inteiramente da fantasma, nunca teve policy própria). Corrigido com policy nova escopada por evento (produtor dono OU colega com `checkin_equipe` do mesmo `team_event_id`) antes de considerar a sessão fechada. Bônus: `.select('id')` hardening que faltava nesse trecho (o caso irmão WORKSHOP no mesmo arquivo já tinha).
- **Técnica operacional nova**: sessão paralela editando `Header.tsx`/`CLAUDE.md` ao mesmo tempo (não commitado) — usado `git stash push -- <arquivo>` pra isolar, commitar só as próprias mudanças, depois `git stash pop` (auto-merge limpo, sem overlap de linhas) pra devolver o WIP alheio. Repetido 2x na sessão sem perder nada de ninguém.
- **Pendente**: cron `revoke-expired-team-access` nunca disparou de verdade num evento que passou do grace period (só testado via chamada direta simulando o dia). Validar quando o Usualdance Festival tiver 30+ dias de encerrado.

Detalhes em `memory/equipe_dashboard_ux_e_revogacao_2026_07_19.md`.

### 2026-07-18 — Certificados: presets "Oficial Dourado" (Mostra + Workshop) via moldura de design real + validação por código + fixes de dados ✅ SHIPADO

Sessão longa (~20 commits), disparada por pedido de melhorar o design do certificado. Pesquisa de mercado (Sympla, Certifier, LearnWorlds) confirmou que molduras desenhadas em `pdf-lib` puro nunca competem visualmente com design de verdade — decisão: presets Moderno/Prestígio/Workshop-simples aposentados do seletor (código mantido, quem já tinha salvo continua renderizando), substituídos por **"Oficial Dourado"** (moldura pronta CoreoHub, gerada via Gemini a partir de prompts iterativos, imagem full-bleed sem overlay/moldura de código por cima) e **"Customizado"** (upload da moldura pronta do próprio produtor/designer, mesmo tratamento sem overlay).

- **Mostra e Workshop usam imagens SEPARADAS** com geometria própria (`isOficialMostra`/`isOficialWorkshop` no `get-certificate-pdf`) — Mostra tem faixa+selo na base (~12% da altura), Workshop tem decoração só nos 2 cantos opostos (selo+fita no canto superior-direito, bloco espelhado inferior-esquerdo). Coordenadas de cada asset medidas direto no arquivo real (crop via ffmpeg + inspeção visual do PNG) — nunca estimadas de olho. **Lição cara**: 2 tentativas de "chutar" o layout do Workshop erraram (uma sobrepôs texto em cima da assinatura, outra corrigiu mas deixou vazio grande embaixo) até o produtor pedir pesquisa de verdade — layout final baseado em **rule-of-thirds** (princípio documentado de composição vertical UX/UI: terço superior=título, terço central=conteúdo principal, terço inferior=assinatura+QR), validado via mockup HTML com a moldura real antes do deploy.
- **QR de validação não depende mais da arte reservar espaço** — o código desenha um cartão branco atrás dele em runtime, funciona em qualquer imagem nova sem precisar coordenar com quem fez o design.
- **🚨 Achado real — `events.event_date` frequentemente NULL**: data real do evento mora em `configuracoes.data_evento` (coluna legada separada, mesmo padrão de outros campos já documentados no projeto). Corrigido tanto no preview quanto no `emit-certificates-batch` (emissão real) + backfill dos 3 certificados já emitidos pro Usualdance Festival que tinham data vazia (nenhum tinha PDF cacheado ainda, seguro corrigir sem afetar entrega já feita).
- **Achado real — "Código: XXXXXXXX" impresso no PDF nunca teve como ser validado manualmente**: só existia RPC de match exato por UUID completo (via QR). Nova RPC `validate_certificate_by_code` (migration `20260718`, prefixo case-insensitive, 0 ou >1 match tratados igual "não encontrado" — nunca expõe ambiguidade) + campo de busca manual em `/validar-certificado` + rota sem hash + esconde o form quando o QR já validou sozinho (achado do produtor: campo continuava visível dando impressão de que faltava preencher algo).
- **Achado real — nome de arquivo do preview**: `File` object com `.name` correto **não** é respeitado pelo botão de download nativo do visualizador de PDF do Chrome (só olha a URL, sempre mostra o UUID do blob — confirmado testando "Propriedades do documento", que também mostra o UUID). Só `<a download="nome.pdf">` funciona de verdade. Botão de preview trocado de `window.open` pra download direto via `<a>`.
- **Preview ao vivo (card na tela) virou o PDF real embutido** — antes era mock em CSS que sempre divergia do resultado de verdade (posição do selo/QR, marca d'água, fonte real). Gerado sozinho via `useEffect` sempre que o template salvo ou o evento selecionado mudam. Preview também passa a usar nome/data/local do evento REAL escolhido no dropdown "Emitir Certificados" (antes sempre "Festival CoreoHub Demo" fictício, ownership checado contra o producer_id do template).
- **Marca d'água "AMOSTRA" testada e revertida**: overlay diagonal (28% opacidade) atrapalhava a própria conferência visual que é o uso real da tela (produtor revisando design) — virou nota pequena anexada ao rodapé de sempre, só no PDF de preview.
- **Pendente**: certificado da Mostra ainda mostra a classificação numérica crua (`classificacao_final`, ex. "obtendo 1") em vez da faixa Ouro/Prata/Bronze que Telão/Apuração/Premiação já usam como fonte única de verdade desde 11-12/07 — são 2 sistemas de classificação desconectados historicamente. Produtor pediu pra deixar pra depois (fechar a moldura primeiro). Sem smoke real em evento ao vivo com produtor emitindo certificados de verdade pro público — só testado via preview com dados fictícios/evento real.

Detalhes em `memory/certificados_oficial_dourado_2026_07_18.md`.

### 2026-07-17 — 🚨 RLS fantasma em `events` + excluir evento/inscrição + picker de evento + trilha não bloqueia ✅ SHIPADO

5 commits (`6b606dc` → `bf6b14e`), sessão disparada por 3 pedidos reais do produtor no mesmo dia: "criei um evento de teste e não achei como excluir", "criei uma inscrição de teste e não achei como excluir", "ao criar evento novo, Configurações não deixa escolher qual editar".

- **🚨 Achado de segurança crítico** (`6b606dc`, migration `20260717_fix_events_rls_and_delete.sql`): tabela `events` tinha policy fantasma **"Acesso Total Events"** (`USING(true) WITH CHECK(true)`, todos os comandos, qualquer `authenticated`) — mesma classe de bug já corrigida em `registrations` em 2026-05-22. Qualquer conta logada, inclusive inscrito comum, podia ler/editar/apagar QUALQUER evento de QUALQUER produtor via API direta. Substituída por 3 policies escopadas (`producer_manages_own_events`, `team_reads_linked_event`/`team_updates_linked_event` via função `SECURITY DEFINER get_team_event_id()`) + trigger `protect_commission_columns()` estendido pra proteger `created_by` (achado do `/code-review`: policy de equipe validava a LINHA mas não as COLUNAS — membro de equipe podia sequestrar o evento setando `created_by` pro próprio uid).
- **Excluir evento** (`6b606dc`): edge function `delete-event` nova — valida dono + nome digitado como confirmação + zero movimento real (registrations/tickets/payments/comissões/coreografias/workshops), nunca confia em RLS. Botão em zona de perigo (`AccountSettings` → Geral).
- **Excluir inscrição de teste** (`826d392`): botão de lixeira em `Registrations.tsx` sempre existiu desabilitado, nunca implementado. Edge function `delete-registration` nova, mesma cautela (só `PENDENTE` + zero sinais de pagamento/avaliação/certificado). Bug pego testando com dado real: colunas `numeric` voltam do PostgREST como STRING (`"0.00"` é truthy em JS) — checagem corrigida pra `Number(x) > 0`. Bônus: `Festivais.tsx` só lista na vitrine eventos com capa E descrição preenchidas (evento abandonado no meio some sozinho, sem mexer em `is_public`).
- **Picker de evento** (`91c4b56`): `AccountSettings`/`RegulationAIParser` resolviam "o evento" sempre como o mais recente por `created_by`, sem deixar escolher — mesma classe do bug de sombreamento por `is_demo` de 2026-07-12, só que aqui é 2 eventos reais do mesmo produtor. `EventPickerSheet` (já usado em outras 5+ telas) aplicado nas duas + `Certificates.tsx` (consistência visual). Achado colateral: 3 pontos de save/upload em `AccountSettings` ignoravam o picker e re-resolviam o mais recente por conta própria — corrigidos, senão o picker só afetaria a leitura.
- **🚨 Regulamento IA salvava em colunas que não existem** (`5cdba47` + `6b77891`, migration `20260716_ai_usage_log.sql`): auditoria achou campos órfãos (`score_scale`/`criteria`/tolerâncias/tempos gravados em colunas que o motor de avaliação real nunca lê), reimportação duplicando `workshops`/`event_styles`/`categories` (INSERT sem checar duplicata) e Seletiva de Vídeo nunca sendo extraída pelo parser. Proteção de custo nova (`ai_usage_log`, 15 análises/dia por produtor). **Revisão pós-shipping no dia seguinte achou bug pré-existente mais grave**: `.update()` é atômico — `updateActiveEventConfig` nunca checava `error`, então qualquer chave que não bate com coluna real (confirmado: `address`, `registration_deadline`, `registration_lots`, `categories_config`, `formacoes_config` em `configuracoes`, `tiebreaker_rules`, `url_ingressos` — nunca foram colunas reais nesses lugares) fazia o UPDATE INTEIRO falhar silenciosamente, mostrando "Configurações Aplicadas!" sem salvar nada. Toda importação com categoria/formação/prêmio estava quebrada desde a criação da feature. Destinos corrigidos (`address`→`events.location`, `formacoes_config`→`events.formacoes_config`, etc.) + `updateActiveEventConfig` agora propaga `error`.
- **Duração da trilha nunca bloqueia + certificado Prestígio** (`bf6b14e`): tempo acima do máximo da modalidade vira aviso não-bloqueante (penalização é na apuração, ver [[backlog-penalidade-tempo-excedido]] no backlog abaixo, não motivo pra travar inscrição). Upload real sempre sobrescreve duração declarada. `utils/audioDuration.ts`+`components/TrackDurationBadge.tsx` novos (dedup Wizard/CentralDeMidia). Certificado Prestígio recentralizado (era o único preset left-aligned) — faixa decorativa de rodapé removida por ora (geometria calculada às cegas colidia com selo/QR).
- **Fade no anchor nav da vitrine** (`679935d`): `mask-image` sinaliza scroll horizontal (padrão Apple/Stripe), some sozinho sem overflow.

Detalhes em `memory/rls_fantasma_events_e_delete_shipado.md`, `memory/regulamento_ia_colunas_fantasma_shipado.md`, `memory/picker_evento_e_trilha_polimentos_2026_07_17.md`.

### 2026-07-16 — Terminal de júri: checagem de áudio pré-entrada + responsividade do comentário ✅ SHIPADO

6 commits (`67e411f` → `7e15c35`). Sessão que fechou o backlog aprovado em 2026-07-15 (ver Histórico anterior) e resolveu 2 bugs reais achados pelo produtor testando.

- **Checagem de áudio pré-entrada** (`48f6a54` + `b66ea17`): `components/JudgeMicCheck.tsx` novo — pede permissão de mic, lista dispositivos, jurado fala, barra de volume em tempo real, "Continuar" só libera com som detectado. **"Continuar sem áudio" sempre visível** — decisão final: quem opera é a EQUIPE DO EVENTO configurando o tablet, não o jurado; nunca pode travar o festival. Gate roda 1x por sessão de PIN (localStorage próprio, não mexe no tipo `JudgeSession`), device escolhido propaga pra gravação real. 2 rodadas de `/code-review` acharam bugs de interação: hooks de auto-start disparavam em paralelo ao gate (usando device errado), "Testar de novo" não parava gravação em andamento, condição de corrida em troca rápida de device. Fix de light mode do dropdown de mic no Chrome/Windows no dia seguinte (`7e15c35`).
- **Responsividade do comentário** (`67e411f` + `0d53607`): campo reposicionado pra logo após os quesitos, sempre visível e maior em notebook/tablet-landscape, modal full-screen em portrait/janela estreita (evita cobertura por teclado virtual). Fix de `text-sm`→`text-base` (Safari/iPad zoom automático em campo <16px).
- **🐛 "Marcar Destaque" sumia em janela BAIXA** (`2b12c06`, achado pelo produtor redimensionando o Chrome): 3 causas encadeadas (campo de comentário maior empurrando o botão + `<section>` sem `min-h-0` nunca deixando o `overflow-y-auto` rolar de verdade + card sempre ~48px mais alto que o espaço disponível, cortado silenciosamente). Fix: `min-h-0` + `sticky bottom-0` + altura compensada.

Detalhes em `memory/backlog_checagem_audio_pre_entrada_juri.md` (atualizado pra ✅ shipado) e `memory/terminal_juri_responsividade_comentario_2026_07_16.md`.

### 2026-07-15 — Storage: 3 incidentes reais achados no próprio smoke test (certificado sobrescrito + 36 trilhas apagadas) + fix de Egress 801% ✅ SHIPADO

5 commits (`b725a18` → `ea75ff3`), disparados por pedido do produtor pra liberar espaço no Supabase (banner de cota estourada). Sessão que virou 3 incidentes reais de produção descobertos rodando o próprio smoke test das correções — nenhum deles causado por má-fé, todos por auditoria incompleta de código pré-existente que só foi lido de verdade quando a function rodou de fato.

- **Causa raiz real do "espaço cheio"**: não era Storage Size (68%, ok) — era **Egress 801% da cota** (40GB de 5GB no ciclo). `trilhas`/`narrations` salvavam com `cacheControl` de 1h (ou default, igual) mesmo o path de upload sendo único por timestamp (conteúdo nunca muda) — cada replay depois de 1h rebaixava o arquivo inteiro de novo. Fix: `cacheControl` pra 1 ano nos 3 pontos de upload (`InscricaoWizard.tsx`, `CentralDeMidia.tsx`, `generate-narration/index.ts`). Só vale pra uploads novos — ver pendência de verificação semanal no backlog.
- **3 edge functions de limpeza criadas/corrigidas**: `cleanup-orphan-tracks` (bucket trilhas) e `cleanup_old_audio_feedbacks` (RPC de 2026-05-01) tinham o cron **documentado mas nunca agendado de verdade** — nenhuma das duas rodava sozinha nunca. `cleanup-orphan-tracks` também tinha o anti-padrão `authHeader.includes(serviceKey)` (lição `feedback_jwt_role_check`). Nova function `cleanup-audio-feedbacks` fecha o ciclo da RPC (que só zerava a coluna no banco, nunca apagava o arquivo físico). Nova function `cleanup-orphan-narrations` (mesmo padrão, bucket narrations).
- **🚨 Incidente 1 — certificado "Ouro Real" do produtor sobrescrito**: `seed-demo-event` sempre fez `DELETE FROM certificate_templates WHERE producer_id = X` antes de recriar o demo — mas `certificate_templates` é por PRODUTOR (não por evento, `UNIQUE(producer_id, template_type)`), então isso sempre sobrescreveu o certificado REAL do produtor a cada regeneração do demo. Descoberto rodando o smoke test do seed atualizado (ver entrada de véspera abaixo). Restaurado manualmente (preset Ouro Real + assinatura "Hemer Roger dos Santos Martin", confirmados pelo produtor) + seeding de certificados removido do `seed-demo-event` de vez (certificados não são mais tocados pelo demo, nunca mais).
- **🚨 Incidente 2 — 36 trilhas reais do Usualdance apagadas**: `cleanup-orphan-tracks` (pré-existente, só a auth foi tocada) tinha a checagem de "está em uso" excluindo qualquer `trilha_url` em formato de URL completa (`!u.startsWith('http')`) — mas **100% das 67 trilha_url do banco inteiro** são URLs completas, nunca paths relativos. A checagem nunca reconheceu nenhuma trilha como "em uso", pra nenhum evento, desde que a function existe. Rodar manualmente apagou 36 coreografias reais do Usualdance (confirmado via HTTP HEAD em cada URL). **Produtor já tinha backup próprio de todas as 36 — sem perda real de dado**, mas incidente de produção de verdade. Cron pausado (`cron.unschedule`) assim que descoberto, fix de detecção aplicado (extrai o path de dentro da URL em vez de descartá-la, mesma lógica já correta em `cleanup-orphan-narrations`), cron reagendado só depois da correção.
- **Bônus autorizado**: 7 trilhas do Usualdance (as que ainda estavam em uso, evento já encerrado) baixadas pro computador do produtor + removidas do Supabase + `trilha_url` limpo nas inscrições, com autorização explícita passo a passo (2 perguntas via AskUserQuestion: escopo só-Usualdance, limpar campo no banco).
- **`seed-demo-event` ganhou features novas** (mesma sessão, antes dos incidentes): Telão ativo (reusa `regenerate_telao_code` via client do produtor), 2 Avisos, prêmios com campos `winner_*` (Voto Popular já revelado, deliberação ainda não registrada — produtor testa o fluxo), 1 Workshop Pass, documentos extras + link de destaque na vitrine.
- **Lição**: 2 dos 3 incidentes vieram de código **pré-existente nunca auditado**, só descoberto rodando a function de verdade em produção (não em teste isolado). "Só mexi na autenticação, não a lógica de negócio" não é desculpa suficiente — qualquer function que vai ser reativada/reagendada merece 1 leitura completa antes.

### 2026-07-15 — Jurados: vínculo explícito por evento (`event_judges`) ✅ SHIPADO

2 commits (`ec06480`, `fac6723`), disparados por pergunta do produtor sobre como o terminal de jurado sabia quais jurados eram do evento (a URL é uma só por produtor). Virou 2 features seguidas:

- **Fase 1 — código de acesso do jurado por evento** (`ec06480`): `profiles.judge_short_code`/`judge_access_token` sempre foram por PRODUTOR — com 2+ eventos, o terminal resolvia "o evento" como o mais recente por `created_at` (mesma classe do bug de 2026-07-12 abaixo). Migration `20260715_judge_access_per_event.sql` move o código curto pra `events.judge_short_code`; `resolve_judge_short_code`/`regenerate_judge_short_code` passam a exigir `event_id`. `judge-login` (edge function) exige `event_id` em toda action que lê dados de evento. `JudgeLogin.tsx`/`JudgeTerminal.tsx` propagam `event_id` na URL/sessão (corte seco — sessão antiga sem isso vira inválida). `JudgesManagement.tsx` ganha `EventPickerSheet` no painel "Código de acesso", com o nome do evento explícito ao lado do QR.
- **Fase 2 — jurados em si vazavam entre eventos** (`fac6723`): mesmo depois da Fase 1, produtor reportou jurados fictícios do evento demo aparecendo junto com os reais no `/judge-login` do Usualdance. Causa: `judges` (a lista de jurados) sempre foi lida só por `created_by=produtor` — todo jurado do produtor aparecia em QUALQUER evento dele. Nova tabela `event_judges` (event_id, judge_id) — jurado só aparece num evento se explicitamente vinculado. `judges` continua sendo entidade do produtor (reaproveitar jurado entre edições continua funcionando). Backfill vincula todo jurado real a todo evento real que já existia; trigger novo vincula automaticamente os jurados ativos do produtor a todo evento real criado a partir de agora. **2 RPCs corrigidas** (`get_public_judges_for_event`, `get_telao_state`) que tinham o mesmo vazamento. `JudgesManagement.tsx` ganha toggle "Vincular a este evento" por jurado; Súmula/Cronograma de Jurados (PDF) trocam a gambiarra de adivinhar por interseção de gêneros pela atribuição real. SuporteJuri/Credenciais/Schedule também passam a filtrar por `event_judges`.
- **Bug no próprio backfill, corrigido em produção**: jurados fictícios do demo pertencem à MESMA conta do produtor (`created_by` igual) — a primeira versão do backfill vinculou os 3 jurados demo + 1 jurado órfão de sessão antiga ("Jurado DEMO", sem PIN sentinela) ao Usualdance real, reintroduzindo o mesmo bug que a migration corrigia. Corrigido excluindo por PIN sentinela (1111/2222/3333) + nome contendo "DEMO", tanto no backfill quanto no trigger.
- Confirmado via API + screenshot: Usualdance retorna só os 4 jurados reais, evento demo retorna só os 3 fictícios.

### 2026-07-13 — Terminal de júri: MediaRecorder nunca parava entre apresentações, corrompendo 58% dos áudios ✅ RESOLVIDO

Causa raiz do incidente do Usualdance Festival (66/114 áudios corrompidos, header EBML ausente): navegar entre apresentações sem clicar Finalizar (Anterior/Próximo/pular/auto-avanço da Mesa de Som) só zerava o buffer, nunca parava o `MediaRecorder` em si — como o header WebM/EBML só vem no 1º chunk de cada `MediaRecorder`, o áudio da apresentação seguinte ficava sem container válido. Fix: `mediaRecorderRef` (não state) guarda o recorder atual, `stopRecording()` fica idempotente. 66/66 áudios corrompidos recuperados via reconstrução de header WebM. Detalhes em `memory/audio_juri_corrompido_58pct_usualdance.md`.

### 2026-07-12 — Suporte: evento DEMO sombreava config do Usualdance ("configurações sumiram") ✅ RESOLVIDO (era 🟡 contorno, virou fix de raiz)

Produtor reportou que "todas as configurações do Usualdance sumiram" — susto real, mas **nenhum dado foi perdido**. Causa: um evento `[DEMO]` criado às pressas mais cedo no mesmo dia (pra testar o fix de `/meus-resultados`, ver entrada abaixo) ficou com `created_at` mais recente que o Usualdance Festival (criado em 12/05). `AccountSettings.tsx` (linha ~1752) resolve "qual é o evento do produtor" com `order('created_at desc).limit(1)` **sem excluir `is_demo`** — passou a mostrar a config do evento demo (quase vazia) em vez da real. **O mesmo padrão está no helper compartilhado `resolveActiveEventId()` (`services/supabase.ts:326`)**, usado por várias telas — qualquer evento demo criado depois de um evento real pode sombreá-lo em qualquer tela que use esse helper. Contorno imediato aplicado nesta sessão: deletado o evento demo via `seed-demo-event` action `delete`. **Fix de raiz shipado depois, em 3 rodadas**: `974da22` (mesmo dia, `resolveActiveEventId()` + `AccountSettings.tsx` ganham `order('is_demo', ascending)` antes de `created_at`), `56635f6` (mesmo dia, mesmo tiebreak em `DemoBanner.tsx`/`ProducerAlerts.tsx`, que tinham cópia duplicada da mesma query), `5eac3c3` (2026-07-15, mesmo tiebreak em `Schedule.tsx` + 3 pontos remanescentes em `AccountSettings.tsx` — upload de regulamento/documento extra/salvar config). Escopo deliberado: telas com seletor de evento EXPLÍCITO (Registrations/ProducerDashboard/Coupons/Credenciais/Schedule kanban) não entram — o bug era só na resolução silenciosa sem picker.

### 2026-07-15 — Certificados (upload de logo real + preset), Super Admin (receita por fonte + KYC + filtro demo), Cronograma (mesmo tiebreak is_demo) e Pagamento do Inscrito (config de evento errada) ✅ SHIPADO

6 commits (`1fe7458` → `65c4270`), sessão paralela a outra no mesmo dia (ver entradas acima) — não documentada em detalhe aqui, só resumida a partir das mensagens de commit:

- **Certificados**: logo do evento virou upload de arquivo de verdade (base64 inline, PNG por causa do pdf-lib); trocar de preset sempre aplica a paleta default daquele preset (antes só em template nunca salvo); aviso de alteração não salva antes de "Pré-visualizar PDF"; preview ao vivo usa o nome do evento selecionado em vez de "Festival CoreoHub Demo" fixo; fonte cursiva (Dancing Script) cacheada a nível de módulo na edge function (evita refetch a cada PDF).
- **Super Admin**: card "Receita por Fonte" (`platform_commissions.kind`) plugado na tela; badge de KYC real (`profiles.asaas_kyc_status`) substitui heurística antiga; eventos demo ganham badge + toggle "Mostrar demo" (ocultos por padrão nos KPIs/tabelas); busca por nome/e-mail em Produtores. Rodada seguinte de `/revisar`: contadores de filtro saem de dentro do `.map` pra `useMemo`, `aria-pressed`/`aria-label`/`aria-hidden` nos filtros novos.
- **Cronograma**: mesmo bug de sombreamento por `is_demo` do Schedule.tsx (ver entrada de 2026-07-12 acima) — corrigido junto com 3 pontos remanescentes em AccountSettings.
- **Pagamento do Inscrito**: `PagamentoInscrito.tsx` buscava `events.formacoes_config` do evento MAIS ANTIGO de toda a plataforma (sem filtrar por dono/evento real) pra calcular o valor exibido — cobrança em si não era afetada (server-side já calculava certo), só o valor mostrado antes de pagar podia bater com a config errada quando o inscrito tinha inscrição em mais de 1 evento.

### 2026-07-12 — Cronograma: player com seek de trilha + Marcador de Palco com refresh/blocos ✅ SHIPADO

Commit `958a9ea`. Trabalho que já estava pronto localmente (não commitado) de uma sessão anterior — revisado (lint+build+88 testes limpos) e commitado nesta sessão.

- **Player do Cronograma**: seek de trilha (botões ±10s Rewind/FastForward + barra de progresso arrastável/clicável, `role=slider`, atualiza via DOM durante o gesto pra não re-renderizar a lista inteira a cada `pointermove`, commit só no `pointerup`) + atalhos de teclado (Espaço play/pause, setas ±10s, ignorando quando o foco está em elemento interativo). Player virou `sticky top-0` — em cronograma com muitos inscritos, ficava fora da viewport ao rolar a lista pra escolher a próxima música. Badge "Última tocada" sobrevive ao Stop (antes, encerrar a transmissão fazia a linha perder todo destaque e a produção tinha que caçar a mesma faixa numa lista de 100+). Fix de bug real: apertar play/pause com a trilha tocando durante o modo SISTEMA etapa 3 caía na lógica de narração e reiniciava a narração de entrada por cima da música ainda tocando.
- **Marcador de Palco**: `fetchData` extraída pra `useCallback` + botão "Atualizar" manual + realtime em `configuracoes` (tempo/gatilho de marcação) — antes a tela só carregava 1x no mount, reordenar o Cronograma ou editar o tempo em Configurações depois de abrir o Marcador nunca refletia sem F5. Mostra o Bloco do Cronograma (Manhã/Tarde/etc) por apresentação — a tela era uma lista plana desconectada da organização real do produtor. Refresh com cronômetro rolando relocaliza a apresentação por ID (não índice), pra reordenação concorrente não fazer a contagem continuar pra uma coreografia diferente.
- **Pendente**: sem smoke em evento ao vivo real (Usualdance já tinha acontecido em 11/07 quando isso foi commitado).

### 2026-07-12 — Fix: `/meus-resultados` nunca mostrava nota publicada pro inscrito ✅ SHIPADO

Commit `3f83008` + migration `20260712_inscrito_reads_judge_name_published.sql`. `evaluations.judge_id` **nunca teve FK pra `judges.id`** no banco (confirmado via `pg_constraint` em prod) — o embed `select('*, judges(name)')` do PostgREST sempre falhava com PGRST200, e o código só desestruturava `{ data: evals }` sem checar `error`, então a query quebrada virava silenciosamente `[]` e toda inscrição aparecia como "Sem avaliação" mesmo com `resultado_publicado=true` de verdade no banco (38/38 coreografias do Usualdance já publicadas). Fix trocou o embed por 2 queries client-side (evaluations + judges por id, batched com `.in()`) + nova RLS liberando o inscrito ler o nome do jurado **só quando** o resultado da própria coreografia já foi publicado (mantém sigilo do júri durante a competição, mesmo princípio da seletiva por vídeo).

### 2026-07-11/12 — Premiação: fonte única de verdade (Telão só revela, registro fica em Premiação) ✅ SHIPADO

15 commits (`a9fcbf4` → `5d13f4c`, noite de 11/07 até manhã de 12/07). Disparado pelo pós-mortem do Usualdance Festival (evento real em 11/07) — vários bugs de premiação só apareceram testando o fluxo ao vivo com dados reais, seguidos de 2 rodadas de `/code-review` (8 achados + 3 achados numa 2ª rodada sobre a 1ª correção).

- **Arquitetura nova — "registra primeiro, revela depois"** (`1b8b27d`, `6fd3ab3`, `c89a16d`): antes, vencedor digitado na mão (Melhor Bailarino/Coreógrafo, Voto Popular) ia só pra `events.telao_premiacao` ("o que mostrar agora"), sobrescrito ao trocar de prêmio — nunca ficava gravado em lugar nenhum. Padrão de mercado (CompetitionSuite/DanceComp): resultado é REGISTRADO, cerimônia só REVELA. `Deliberacoes.tsx` (`/deliberacoes`) virou a única tela que registra (campo editável + botão "Salvar vencedores", grava em `configuracoes.premios_especiais`); `TelaoControle.tsx` virou só leitura, revela a partir do registro salvo; botão "Puxar do Voto Popular" preenche automático a partir de `get-voto-popular-winner` (digitar na mão continua como fallback de empate/votação aberta).
- **🚨 Vazamento público antes da revelação** (achado mais grave da 1ª rodada de `/code-review`, `adb7667`): a vitrine mostrava o vencedor assim que "Salvar vencedores" rodava em Premiação — nenhuma trava. Fix: campo novo `winner_revealed`; salvar em Premiação só REGISTRA (nunca marca revelado); só o Telão, ao efetivamente clicar pra revelar, marca `winner_revealed:true`. Vitrine pública só mostra vencedor quando `revealed===true`.
- **🚨 AccountSettings sobrescrevia vencedor por uma 3ª porta** (achado mais grave da 2ª rodada, `3db27f5`): a aba Configurações → Prêmios salvava `premios_especiais` com o snapshot carregado na ABERTURA da tela, sem passar pela RPC nova — editar nome/valor de um prêmio ali horas depois de uma revelação ao vivo revertia `winner_revealed`/`winner_items`/`winner_nome` pro estado velho. Fix: antes de salvar, busca o estado mais recente do banco e sobrepõe os campos de vencedor por cima do array editado localmente.
- **Corrida real entre abas** (`adb7667`): Telão e Premiação faziam SELECT+UPDATE independentes na mesma coluna JSONB — produtor usa as duas em abas simultâneas no evento ao vivo, a escrita que terminava por último apagava a mudança concorrente da outra aba pra um prêmio diferente. Fix: RPC nova `update_premios_winners` (migration `20260712_update_premios_winners_rpc.sql`) faz o read-modify-write num único UPDATE atômico (patch parcial via `jsonb ||`, aplica N prêmios de uma vez) — fecha a corrida na fonte.
- **Bug real de recálculo** (`02ed00a`): `winnerEdits` era inicializado pra TODO prêmio no fetch (até os automáticos, com objeto vazio `{nome:'',estudio:''}` quando nunca editado) — `if (edit)` em `saveWinners` era sempre verdadeiro (objeto vazio ainda é truthy), então o recálculo de faixa/maior_nota nunca rodava e regravava o valor velho/vazio. Achado em prod: Ouro/Prata/Bronze regravavam nomes residuais do antigo seletor manual ("Essência"/"Meow"/"Último suspiro") toda vez que clicava Salvar. Fix: classifica o tipo PRIMEIRO — faixa/maior_nota sempre recalculam do zero, só prêmio de pessoa/manual usa `winnerEdits`.
- **Classificação de prêmio centralizada** (`adb7667`): lógica de tipo (faixa/maior_nota/premio/manual) estava copiada à mão em 3 lugares (TelaoControle, ResultsPanel, Deliberacoes) — extraída pra `utils/awardClassification.ts` compartilhado.
- **PDF de Apuração reorganizado** (`d8ee513`, `2cc1327`): misturava 2 modelos de premiação — tabelas do corpo dividiam tudo por estilo+categoria (várias mini-tabelas), enquanto Telão/Premiação mostram faixa Ouro/Prata/Bronze como FESTIVAL INTEIRO (1 lista só). Seção "Premiação" (mesma fonte que o Telão revela) virou a PRIMEIRA seção do PDF, logo após a capa; tabelas por estilo+categoria viraram anexo "Detalhamento por Estilo e Categoria" com nota explícita de que a premiação oficial está nas páginas anteriores.
- **Apuração ganha seção Premiação na tela** (`5c1b6de`, `5d13f4c`): tela só mostrava a lista por gênero+categoria (ferramenta de auditoria nota-por-nota), sem visão da premiação real. Nova seção no topo usa a mesma fonte/precedência do PDF e do Telão. Card de Premiação em Apuração virou atalho compacto pra `/deliberacoes` em vez de duplicar os cards ali (feedback do produtor: esperava atalho, não cópia da tela).
- **`/deliberacao` só oferece prêmios que dependem do jurado** (`a9fcbf4`): `get-starred` devolvia TODOS os prêmios habilitados, inclusive Ouro/Prata/Bronze/Maior Nota/Voto Popular (calculados sozinhos, atribuir marcação a eles não tinha efeito nenhum) — filtrado pra só prêmios do tipo deliberação.
- **Pendente**: fluxo novo (registra-primeiro-revela-depois + RPC atômica) shipado todo depois do Usualdance já ter acontecido — sem validação com produtor operando ao vivo numa cerimônia real ainda, só smoke transacional (BEGIN/ROLLBACK) em prod.

### 2026-07-10 — Véspera do Usualdance: acesso único do jurado + trava real de deliberação + Voto Popular automático no Telão + fix multi-tenant no Marcador de Palco ✅ SHIPADO

11 commits (`685592c` → `4bfe5f6`) + 1 commit no repo isolado do Voto Popular (`4174b3c`) + 1 deploy de correção feito nesta sessão. Dia inteiro de polimento pré-evento (Usualdance Festival, 11/07) disparado por teste real do Hemer no terminal + revisão de PDF gerado.

- **PDF "Cronograma de Jurados"** (`1061811` → `ab0237c`, 5 commits): botão novo em `JudgesManagement.tsx` (menu Jurados, ao lado do "Súmula (PDF)"), **uso do Coordenador do Júri** — mostra quando a banca de jurados muda ao longo da sequência de apresentação. Ordem real do Cronograma (`ordem_apresentacao`) agrupada por bloco real + banca por estilo (`judges.competencias_generos`). Colunas fixas "Jurado 1/2/3": cada jurado fica na mesma coluna enquanto continua avaliando, célula rosa = quem entrou naquela troca, cinza = quem continua; linha "TROCA" com réguas rosa marca onde muda (comparação contínua entre blocos). Validado com dados reais do Usualdance (trocas em 4,5,15,16,17,19,20,21,33). Lições jsPDF: glifo `⟳` vira "'ó" na Helvetica nativa (só texto puro); pill stadium via `didDrawCell` (`roundedRect` raio=½altura) exige `cellWidth` explícito senão a coluna colapsa; 7 colunas só cabem em **paisagem**. **Backlog aberto**: criar papel/permissão real "Coordenador do Júri" na tela de Jurados (hoje só produtor/equipe com acesso geral) — pedido do Hemer, NÃO implementar ainda.

- **Telão — Troféu Voto Popular automático** (`685592c`, `45bb941`): nova edge function `get-voto-popular-winner` (CoreoHub) chama `get-winner` no repo isolado do Voto Popular e preenche nome/estúdio do vencedor sozinho no card "Troféu Voto Popular" do `TelaoControle.tsx` — antes o produtor digitava na mão. Busca manual continua como fallback (votação aberta/empate/integração falha). **🚨 Bug real encontrado e corrigido nesta sessão de teste** (fora dos commits, direto em prod): a função `get-winner` foi deployada em 07/07 sem `--no-verify-jwt`, então o gateway do Supabase rejeitava a chamada da CoreoHub (que manda só `x-vote-token`, sem `Authorization`) **antes mesmo do código rodar** — a automação nunca funcionou desde que foi criada. Confirmado via reprodução direta contra prod (`{"code":"UNAUTHORIZED_NO_AUTH_HEADER"}`) comparando com `import-groups`/`stage-webhook` (mesmo padrão, JWT corretamente desligado). Fix: `supabase functions deploy get-winner --project-ref aghmjmqrkwuxmrctslkf --no-verify-jwt`, revalidado (`{"error":"unauthorized"}` = chegou no código). **Lição**: toda função nova que reusa o padrão de segredo compartilhado (`x-vote-token`) precisa do flag explícito no deploy — o default da plataforma é `verify_jwt=true`, e nada no dashboard avisa que uma função ficou fora do padrão das irmãs.
- **Acesso do jurado — unificado num único link** (`5be7c42` → `83300c9`, 4 commits, 2 iterações de design): existiam 2 caminhos concorrentes (link cru `/judge-login/<uuid>` + código curto `/entrar-juri`) confundindo o produtor sobre qual compartilhar. Iteração 1 unificou em painel com 2 abas; iteração 2 (`83300c9`, decisão final) elimina a exposição do link assinado por completo — só existe **"Código de acesso"**, QR aponta pra `/entrar-juri?code=XXXXXX` (autosubmit ao escanear) e o botão "Copiar convite" manda 1 mensagem de WhatsApp pronta com link+código. Padrão Kahoot (1 link + 1 PIN, QR só atalho). `/judge-login/:token` continua existindo como destino interno do redirect, só deixou de ser algo que se compartilha direto. A11y (`e92c675`): tabs da iteração 1 ganharam `role=tablist/tab/tabpanel` antes do refactor final simplificar pra 1 painel só.
- **Deliberação de Prêmios — trava real + marcação órfã visível** (`1e31459`, `45bb941`): `submit-deliberation` no `judge-login` ganhou a checagem de `deliberation_status==='LIBERADO'` que a UI já prometia mas nunca existia no servidor — POST direto ainda sobrescrevia deliberação pós-liberação. Nova action `remove-marcacao` (não exige a registration existir) resolve marcação ⭐ órfã na tela (card ficava invisível mas contava no total). **Diagnóstico corrigido no meio da sessão**: a causa real da "órfã" não era `ON DELETE CASCADE` (que já existe desde a migration `20260503` — deleção de verdade já limparia a marcação junto), era a query `get-starred` não checar `error` — falha transitória fazia toda marcação parecer órfã (`registrations` virava `[]`), sintoma real batido em prod ("3 marcadas, 0 aparecendo"). Fix real: retorna erro explícito em vez de mascarar como lista vazia.
- **Súmula geral em PDF** (`1e31459`): botão novo em `JudgesManagement.tsx` gera 1 folha em branco por jurado ativo (fila filtrada pelos mesmos gêneros do terminal, critérios/pesos reais de `regras_avaliacao`) — contingência de papel se o terminal falhar no dia. Pula jurados cuja fila fica vazia no evento atual (`45bb941`).
- **Terminal do júri — busca por nome + atalho Deliberação** (`685592c`, `45ce62b`): popup "Ir pra apresentação" aceitava só número, ganhou busca por nome de coreografia/estúdio — unificado num campo só (número pula direto, texto filtra). Atalho "Deliberação de Prêmios" no menu ⋮, gate corrigido de "fase DELIBERACAO/CONFERENCIA" pra "qualquer fase != COLETANDO" depois de o evento real do Hemer (já em LIBERADO) não mostrar o atalho.
- **Apuração/PDF — 6 fixes de rótulo enganoso** (`45ce62b`): "⚠" renderizava como "&" no PDF (Helvetica sem esse glifo) → texto puro; coluna "Pos." (1º/2º/3º) some em modo THRESHOLD (nota mínima) — ordem da lista não decide medalha nesse modo, numerar era enganoso, mesmo ajuste na bolinha da tela (vira dot colorido sem número); "Participação" (abaixo de bronze) vira "—" quando o produtor não configurou essa faixa como prêmio real; cabeçalho "K-Pop|Livre" ganhou legenda "ESTILO · CATEGORIA"; "Estúdio"→"Estúdio/Grupo" (nem toda coreografia tem estúdio formal). Nova seção **"Prêmios Especiais"** no PDF calcula vencedor por tipo (faixa/maior nota/deliberação/manual, mesma classificação do Telão) + coluna **Outlier (⚠)** marca linhas com divergência alta entre jurados (antes só um aviso genérico no topo, sem dizer quais linhas).
- **Cronograma — barra de progresso da trilha sumia no modo TRILHA** (`ec03b2b`): `trilhaProgress`/`trilhaDuration` ficavam presos atrás de `modoSistema`; modo TRILHA (só toca a música, sonoplasta narra ao vivo no mic) já rastreava progresso mas nunca renderizava. Bônus: badge "Trilha OK" mostra duração real (mm:ss) quando já lida.
- **Marcador de Palco — mesma classe de bug multi-tenant do Cronograma (2026-06-10/11), só que nunca corrigida aqui** (`4bfe5f6`): query de registrations sem filtro `event_id` (misturava coreografias de todos os eventos do produtor), config salva na row legacy `id='1'` (vazava entre produtores), sem filtro `excluded_from_schedule`/aprovação manual (usava só `status_pagamento` em vez do `SCHEDULABLE_REGISTRATIONS_OR_FILTER` compartilhado). Fix + badge "Ao vivo agora" novo (indicador situacional via `live_registration_id`, não navega sozinho — equipe de marcação trabalha adiantada em relação ao show).
- **Equipe — Apoio de Workshop invisível + gate de permissão no Telão/Avisos** (`fbc62c8`, migration `20260710_fix_apoio_workshop_team_visibility.sql`): RLS de `profiles` nunca incluía `APOIO_WORKSHOP` na lista de roles legíveis por equipe — membro convidado com esse papel funcionava (escaneava QR) mas sumia de `/minha-equipe`, sem editar/remover pela UI. `/telao-palco` e `/avisos` só tinham `PrivateRoute` (sem checagem de permissão) — qualquer membro de equipe logado acessava direto pela URL mesmo escondido do menu. Novas permissões `controle_telao`/`gerenciar_avisos` com `RequirePermission`; preset Coordenador ganha as duas por padrão.

**Pendências abertas desta sessão**: Voto Popular automático no Telão só validado por reprodução direta de HTTP (gateway 401→código alcançado), **não** com uma urna de verdade fechada e vencedor real — só vai ser exercitado ponta-a-ponta no dia do evento. Súmula em PDF nunca impressa de verdade. Acesso único do jurado (`?code=` autosubmit) só testado nesta sessão via código/leitura, sem QR físico escaneado num aparelho real. Marcador de Palco "Ao vivo agora" sem smoke com evento ao vivo.

### 2026-07-09 — Terminal de júri: nota duplicada + áudio nunca persistia + polimentos na Apuração ✅ SHIPADO

7 commits (`d55ed03` → `7137b51`). Disparado por relato do Hemer: "os quesitos que aparecem pro jurado não são os mesmos configurados" + depois "avaliei e não vejo áudio" + "entramos sem querer em 2 dispositivos com o mesmo jurado".

- **🐛 Investigação de quesitos divergentes — não era bug**: Sarah Tinel tem `competencias_artisticas` cobrindo quase todos os estilos dela (só K-Pop fica Técnico) — exatamente o exemplo documentado quando a feature "Tipo de Júri por estilo" foi criada em 2026-06-21. Config real do banco confirmada via SQL, nada a corrigir.
- **🚨 Bug real — avaliação duplicada por jurado em 2 dispositivos** (`d55ed03`, migration `20260709_evaluations_unique_judge_registration.sql`): `evaluations` nunca teve trava por `(judge_id, registration_id)` — reabrir/reavaliar (2 devices, ou só re-submeter) sempre fazia `INSERT`, nunca `UPDATE`. Achado real em prod: Victor Big com 2 notas divergentes pra mesma coreografia, Daniela Morais com 3 — a Apuração lia isso como "divergência entre jurados" (outlier falso) quando era a MESMA pessoa avaliando 2x. Fix: migration dedupe (mantém a nota mais recente) + `UNIQUE(judge_id, registration_id)` + os 3 pontos que gravam nota (`JudgeTerminal.tsx` fluxo legado, `judge-login/index.ts` fluxo PIN+outbox) viraram `upsert` no lugar de `insert`.
- **🚨 Bug real — áudio do jurado nunca persistia, 3 camadas** (`825c45a` → `d813ae7`):
  1. Bucket `audio-feedbacks` nunca existia em produção (commit anterior à sessão, `919d0aa`/migration `20260709_audio_feedbacks_bucket.sql` — aplicada nessa sessão).
  2. 2 pontos de erro **engolidos silenciosamente**, sem log algum: upload falho no fluxo legado (`catch` vazio) e permissão de microfone negada (`catch { /* mic denied */ }`) — ambos ganharam `console.error` explícito.
  3. **Causa raiz de verdade**: `outboxDrainer.ts` (fila offline do fluxo real de jurado via PIN) carrega toda a fila em memória 1x no início do `drainOutbox()`. Quando `upload-audio` processa com sucesso ANTES do `submit-evaluation` dependente (mesma rodada, ordenação intencional), ele patcha o `audio_url` real no IndexedDB — mas o objeto `submit-evaluation` já carregado no array `items` continha um snapshot desatualizado (`audio_url: null`), e era esse que ia pro servidor. Nenhuma das duas operações falhava, por isso não aparecia erro nenhum apesar de 3 rodadas de log adicionado. Fix: `processItem` agora relê o item direto do IndexedDB (`getOutboxItem`, novo helper em `offlineStore.ts`) antes de checar dependência de áudio e antes de despachar.
- **UI da Apuração — 3 ajustes** (`8dccd81`, `7137b51`):
  1. Comentário escrito do jurado (`feedback_text`) passou a aparecer também na trilha de auditoria da aba "Mostra Competitiva" (antes só existia na aba "Mostra Avaliada" — texto ficava salvo no banco mas invisível nessa tela).
  2. Nota numérica perdeu a cor por faixa (dourado/verde/âmbar/rosa) a pedido do Hemer — fica sempre branca/slate. Badge de medalha (Ouro/Prata/Bronze, configurável) continua colorido normalmente.
  3. **Player de áudio único** — "Ouvir Áudio" criava um `new Audio().play()` solto a cada clique; 2 cliques tocavam 2 comentários sobrepostos. Mesmo padrão já usado em `MyResults.tsx` (lado do inscrito, que já estava correto): 1 `audioRef` compartilhado, toggle play/pause, pausa o anterior antes de tocar um novo.
- **Descoberta lateral — `/entrar-juri` já existe e funciona**: código curto de 6 caracteres estilo Kahoot (migration `20260709_docs_voto_juri_short_code.sql`, já documentada) pra logar jurado em dispositivo novo sem QR/link longo. Só achado 1 link morto no rodapé de `EntrarJuri.tsx` ("Já tem o link direto? Acesse por aqui" aponta pra `/judge-login` sem token, que trava em "Link inválido") — **não corrigido ainda**, reportado.

### 2026-07-08 (noite) — Bundle Cronograma + webhook stale downgrade + migration não aplicada (2 bugs de produção) ✅ SHIPADO

6 commits (`77aed6e` → `97a58d7`). Sessão reativa — todos os itens vieram de reports do produtor testando o Cronograma do Usualdance Festival às vésperas do evento (11/07).

- **Cronograma — drag pra bloco vazio nunca funcionava** (`77aed6e`): bloco recém-criado (0 coreografias) renderizava só um texto, sem `SortableContext`/alvo soltável — dnd-kit não tem `over` válido pra detectar o drop. Novo `EmptyBlocoDropZone` (`useDroppable`) com highlight visual. `handleDragEnd` ganha branch pra tratar drop nesse placeholder (extrai `bloco_id` do id sintético, sem precisar de `arrayMove`).
- **🐛 Detecção de conflito de bailarino nunca disparava** (`77aed6e`): `buildConflictMap`/`generateSmartOrder` extraíam o id do bailarino via `dancer.cpf || dancer.full_name || dancer.name` — mas `bailarinos_detalhes` (fonte real de quase toda inscrição) usa a chave `nome` (PT), não `full_name`/`name`. Resultado: `id` sempre `undefined`, conflito nunca calculado, **"Gerar Ordem Inteligente" nunca evitou repetir bailarino em coreografias seguidas**. Rodei a lógica corrigida contra a ordem publicada real do Usualdance Festival: 39 conflitos reais não detectados (pior caso: 2 coreografias consecutivas com zero intervalo, mesmas bailarinas). Fix: soma `dancer.nome` como fallback nos 3 pontos de extração.
- **🚨 Webhook Asaas — evento fora de ordem revertia pagamento aprovado** (`77aed6e`): OVERDUE chegando *depois* do RECEIVED da mesma cobrança (retry/delay do Asaas) sobrescrevia `status_pagamento` de volta pra `VENCIDO` em **todos os 6 branches** do webhook (registration legacy, aggregate, audience_ticket, workshop, workshop_pass, video_selection) — só existia proteção parcial pra cobrança *diferente* (caso Carolina Mellin, 2026-06-27). Caso real descoberto: Princesa Florine/New Corpus. Novo `STALE_DOWNGRADE_STATUSES` bloqueia VENCIDO/PENDENTE de rebaixar um registro já aprovado, em qualquer branch; ESTORNADO continua aplicando normalmente (refund real).
- **Reconciliação manual — Princesa Florine**: investigação (extrato Asaas real + comprovantes Pix da inscrita) confirmou pagamento genuíno perdido numa 2ª fatura órfã (nunca rastreada em `payments`/`registrations`) — causa raiz: `create-payment-asaas` construía a descrição da cobrança com `coreo.nome` (coluna que não existe, é `nome_coreografia`), então **toda fatura avulsa** desse fluxo sempre mostrava "Inscrição - Coreografia" genérico, facilitando confundir qual cobrança é qual. Fix de 1 linha (`ac76cb4`... na verdade `445ebf9`) + registration marcada `APROVADO` manualmente + `platform_commissions` inserida (R$50 bruto/R$5 comissão/R$45 líquido, batendo com o extrato real).
- **Taxa de mensageria recorrente** (`d44a164`): as 6 edge functions de criação de pagamento só setavam `notificationDisabled:true` ao CRIAR customer novo na Asaas — customer *reaproveitado* por CPF (comprador recorrente, ou customer anterior ao fix de 2026-05-18) nunca tinha a flag reforçada, gerando SMS/email + taxa pra sempre, invisível pro one-shot `disable-asaas-notifications` (só varre snapshot do momento em que roda). Novo helper `_shared/asaas-customer.ts` (`ensureNotificationDisabled`) chamado após toda busca por CPF.
- **Title Case em Inscrito/Estúdio** (`445ebf9`, `ac76cb4`): nomes apareciam em qualquer caixa (`New Corpus escola de dança`) por vir cru do banco — cada inscrito digita diferente. Novo `toTitleCase()` (`utils/formatters.ts`, respeita conectores PT-BR minúsculos) aplicado em `Registrations.tsx` (tabela/mobile/painel) e no PDF de ordem exportado (`jspdf-autotable`, que não tinha CSS uppercase mascarando a inconsistência).
- **Modal "Blocos do Cronograma" — 2 bugs de UI** (`92dd19d`, `c04fbeb`): sumia clipado no topo em janelas menores (app PWA instalado no Windows) — `items-start`+`max-h-[85vh]` (vh estático) → `items-center`+`max-h-[85dvh]`. E não seguia o design system: usava âmbar (reservado pra estados de alerta) em vez do rosa da marca no ícone/setas/CTA principal, e `z-50` em vez do `z-[60]` padrão (colide com BottomNavBar).
- **"Salvar Ordem"/"Publicar pros Inscritos" falhavam sempre** (`7feba96`): `handleSaveOrder`/`handlePublishSchedule` usavam `.upsert(rows, {onConflict:'id'})` em lote — isso monta `INSERT ... ON CONFLICT DO UPDATE` no Postgres, que sob RLS pode falhar pro produtor (só tem policy de **UPDATE** em `registrations` desde 2026-05-22, não de INSERT), mesmo com toda linha existente. Fix: `UPDATE` direto por linha via `Promise.all` (nunca passa pelo caminho de INSERT). Mensagem de erro também passou a mostrar `err.message` real em vez de texto genérico.
- **🚨 Bug de produção mais sério do dia — migration commitada nunca aplicada em prod** (`97a58d7` + SQL rodado manualmente pelo user): `20260709_docs_voto_juri_short_code.sql` (colunas `documentos_extras`/`destaque_link_url`/`destaque_link_label` em `events`, feature usada pelo produtor essa mesma tarde) nunca foi colada no SQL Editor. Isso quebrou **2 telas ao mesmo tempo, silenciosamente**: a vitrine pública em domínio customizado (`festival.usualdance.com` → "Evento não encontrado") E a aba Configurações → Identidade (Instagram/TikTok/WhatsApp/GA4/Meta Pixel etc. sumiram da tela). Ambas fazem `select()` amplo incluindo essas 3 colunas sem checar `error` — Supabase retorna PGRST204, `data` vira `null` silenciosamente, indistinguível de "realmente não existe". **Nenhum dado foi perdido** (confirmado: `events`/`configuracoes` intactos; e o handler de salvar usa `myEvent.id`, que teria crashado — não sobrescrito — se o produtor tivesse clicado Salvar com a tela vazia). Fix: rodei a migration manualmente + adicionei log do `error` real em `PublicEventPage.tsx` (mesma classe de bug documentada desde sempre no comentário do próprio arquivo — "incluí-las aqui retorna PGRST204 e a query inteira cai pra null" — só que essas 3 colunas específicas escaparam da lista de exclusão).
- **Pendência**: `AccountSettings.tsx` (linha ~1750) tem o MESMO padrão de select-amplo-sem-checar-error que causou a metade "Configurações sumiu" do incidente — só recebeu a migration corretiva, não o log de erro defensivo que `PublicEventPage.tsx` ganhou. Vale replicar se aparecer de novo.

### 2026-07-08 — Certificado preset "Ouro Real" + fundo custom + preview PDF ✅ SHIPADO

Commit `c7a8a78`. Resolve pedido de modelo de certificado mais sofisticado — 3 direções visuais mockadas num artifact HTML fiel às fontes reais do gerador (`pdf-lib` só tem Helvetica/Times sem fonte embutida), "Ouro Real" (diploma tradicional dourado ornamentado) escolhido pelo user sobre "Editorial CoreoHub" (moderno).

- **Preset `ouro`** em `get-certificate-pdf/index.ts`: moldura dourada dupla + 4 cantos ornamentados (linhas), filete+losango entre título/subtítulo, Times inteiro (Roman/Bold/Italic/BoldItalic — `pickFont()` novo escolhe por `fontFamily:'times'` no `LayoutTag`), selo circular + fita bordô via `drawEllipse`+`drawRectangle rotate(degrees())`, nome do evento em bordô fixo (paleta do preset, não configurável na v1). `DEFAULT_LAYOUT_OURO_MOSTRA`/`_WORKSHOP` novos, paralelos aos já existentes.
- **Fix do gap real `background_url`**: o campo sempre existiu no schema/UI (desde 2026-05-04) mas o gerador NUNCA desenhava a imagem — sempre caía no fundo sólido. `embedBackgroundImage()` novo busca+embute (png/jpg) full-bleed com overlay branco 55% opacity por cima pra manter texto legível.
- **Preview em PDF real**: novo modo `{ preview: true, template_id }` no mesmo edge function — gera PDF com dados fictícios (Maria Silva Oliveira / Renascer / Festival CoreoHub Demo), valida ownership do template (producer_id ou COREOHUB_ADMIN), devolve o PDF direto na resposta (sem tocar `certificates_issued`/bucket). Botão "Pré-visualizar PDF" em `Certificates.tsx` (gate igual ao "Emitir": desabilitado até `template?.id` existir).
- **Frontend**: `PRESETS` ganha entrada 'ouro' pra mostra E workshop; `selectPreset()` novo aplica defaults de cor (dourado `#a97e2e` + tinta `#241c10`) só quando o template ainda não foi salvo (não sobrescreve customização existente).
- **2 bugs achados e corrigidos no mockup ANTES de codar** (artifact HTML, não no código real): selo sem `translateX(-50%)` ficava desalinhado da fita; rodapé "Emitido por CoreoHub" cruzava a linha da moldura por margem vertical apertada demais — ambos verificados via screenshot Playwright do próprio mockup antes de seguir pra implementação.
- **Validação real**: `deno check` limpo antes do deploy (achou 1 erro de tipagem real — `Response(pdfBytes,...)` precisa de `ArrayBuffer`, não `Uint8Array` direto, mesmo padrão já usado no upload). Deploy via CLI. Clique real em produção (autorizado pelo user): selecionou Ouro Real → Salvar → Pré-visualizar, capturado via `page.route()` interceptando a resposta do fetch (network response direto via `response.body()` retornava 0 bytes, mesmo com Service Worker bloqueado — causa não investigada a fundo, contornado com route interception). PDF real de 6.1KB confirmado com todos os elementos (moldura, selo, fita, QR, tipografia) batendo com o mockup aprovado.
- **⚠️ Efeito colateral real**: o clique de validação salvou "Ouro Real" como o template de Mostra ATIVO do evento do Hemer (antes era "Modelo padrão Mostra" sem preset escolhido) — não foi um teste descartável, é a config real agora. Avisado ao user.

**Pendências**: preview do preset 'ouro' e do fundo customizado só testados nesse 1 clique real — falta emitir um lote de verdade (produtor real clicando "Emitir certificados") pra confirmar que a mesma renderização se replica no fluxo de emissão em massa (`emit-certificates-batch` não foi tocado, só cria rows com `pdf_url=NULL`; a geração de fato passa pelo mesmo `generatePdf()` só quando o inscrito clica Baixar — deveria funcionar igual, mas não foi exercitado nesse caminho específico).

### 2026-07-08 — Botão de login fixo na vitrine ✅ SHIPADO (2 achados de revisão em aberto)

Commit `39b4138`. Disparado por relato de usuários do Usualdance Festival não achando onde fazer login na vitrine pública (`/evento/:slug` e domínio próprio `festival.usualdance.com`) — a página não tinha nenhum link de acesso à conta.

- **Fix**: "Entrar"/"Minha conta" adicionado ao `cta` do `EventAnchorNav` (menu fixed-top já existente), ao lado do "Inscreva-se". Primeira tentativa foi um breadcrumb sticky à parte — descartada porque ficava escondida atrás do `EventAnchorNav` (`z-40` vs `z-30`); solução final reaproveita o slot `cta` que já existia.
- **Validado**: confirmado ao vivo em prod que o mesmo componente renderiza em `app.coreohub.com/evento/:slug` E no domínio próprio (`festival.usualdance.com/`, raiz resolvida via `App.tsx` lookup de `custom_domain`) — nenhum código extra precisou ser duplicado. Sem sobreposição com "Inscreva-se" testado em 375/768/1440px.
- **🟡 Pendência 1 — flash de estado**: `isLoggedIn` começa `null` (comentário promete "evita flash Entrar" enquanto a sessão carrega), mas o ternário trata `null` igual a `false` — mostra "Entrar" antes da checagem terminar e pode trocar pra "Minha conta" alguns ms depois. Fix: só renderizar o botão quando `isLoggedIn !== null`.
- **🟡 Pendência 2 — destino errado pro produtor**: "Minha conta" sempre navega pra `/dashboard` (tela do inscrito), sem checar `profile.role`. Produtor conferindo a própria vitrine (cenário provável) cai na tela errada — mesmo bug que já tinha sido corrigido no pós-login do `Auth.tsx` (redireciona `ORGANIZER` pra `/qg-organizador`), reintroduzido aqui por um caminho diferente (clique direto, não login).
- Ambas reportadas via `/revisar`, **não corrigidas ainda** (usuário pediu só reportar).

### 2026-07-07 — Telão de palco (Fases 1+2+v2) + Avisos do produtor + Minimizar troca de jurados ✅ SHIPADO

9 commits (`26ca6ed` → `5bcc952`). Maior dia de feature nova desde o Voto Popular — 2 produtos internos novos (Telão + Avisos) do zero, todos com migration aplicada em prod.

- **Telão de palco (Fase 1 `26ca6ed`, light mode `62c9c9c`, Fase 2 Premiação `38df106`, v2 `f744822`)**: ver detalhes em "Features chave" acima. Disparado por necessidade real de exibir nota/média num LED/projetor durante o festival sem expor dado sensível. RPC única `get_telao_state` serve tanto o modo Ao vivo quanto Premiação — o computador plugado no LED nunca precisa saber qual modo está ativo, só resolve o estado retornado. v2 corrigiu o modelo de premiação pra bater com a config real do produtor (faixa de média do evento inteiro, não pódio-top-3-por-grupo — que tinha sido a hipótese errada da v1).
- **Avisos do produtor + ordem de apresentação (`1c8d01b`)**: ver "Features chave" acima. Primeira feature de comunicação direta produtor→inscrito fora de email/notificação in-app.
- **Minimizar troca de jurados no cronograma (`d654f6d`)**: `generateSmartOrder` em `Schedule.tsx` ganha critério opcional de desempate — cruza `estilo_danca` de cada coreografia com `competencias_generos` dos jurados ativos, gera uma "banca" (assinatura) por coreografia, e entre candidatas empatadas no hard constraint (intervalo de segurança, nunca sacrificado) prioriza a que mantém a mesma banca da anterior. Toggle opt-in (desligado = comportamento de sempre) + chip com iniciais dos jurados por linha + contagem de trocas de banca no resultado. Disparado por caso real do Usualdance: 4 jurados com competências de gênero parcialmente sobrepostas (nenhum cobre tudo), onde montagem manual só era viável pro caso fechado da especialista de K-Pop.
- **Fix Dashboard do inscrito — `event_id` errado por 2 bugs em sequência (`a83328e` + `5bcc952`)**: achado durante QA visual do card de Avisos. `fetchActiveEventConfig` sem hint cai na row legacy `id='1'` pra quem não é produtor — `event_id` dessa row não bate necessariamente com o evento real da inscrição do inscrito. Fix 1: deriva de `coreografias[0].event_id` (fonte real) com fallback pra config só quando o inscrito não tem nenhuma inscrição ainda. Fix 2 (mesma tarde): mesmo com a derivação certa, havia race — `inscritoEventId` caía no fallback legacy enquanto `coreografias` ainda carregava, disparando a busca de avisos/ordem com o evento errado antes do fetch real terminar. Resolvido só recalculando após `loading=false`.
- **Pendências abertas**: Avisos sem segmentação (v1 é "vale pro evento inteiro"). Telão validado ao vivo com LED físico no Usualdance Festival (11/07) — ver correção acima.

### 2026-07-06 — Cronograma modo SISTEMA + estúdio na origem (backfill+trigger) ✅ SHIPADO

5 commits (`f03164f` → `d684531`). Disparado por 3 bugs reportados pelo produtor no Cronograma (`f03164f`), seguido de 2 rodadas de `/code-review` que acharam e corrigiram mais 4 bugs reais (`f9744d4`, `420776f`) antes de fechar num refactor de reuso (`2f865c1`) e num fix de tema (`d684531`).

- **Robustez do modo SISTEMA** (`f03164f`): `startSequenceMode`/`playTrilhaWithSequence` ganham listeners de `error` + timeout de segurança (baseado em `duration_seconds`) — se o evento `'ended'` de uma narração/trilha não disparar, a sequência avança sozinha em vez de travar em silêncio (sintoma relatado: "narração toca e a trilha nunca começa"). `console.warn` explícito quando uma linha entra em SISTEMA sem `trilha_url`.
- **🐛→✅ Bug raiz do estúdio em branco na narração + credenciais + em qualquer leitor de `registrations.estudio`** — o Wizard atual grava o nome do estúdio só em `event_data.estudio_nome` (JSONB), nunca na coluna top-level `estudio`. Fix em 2 camadas:
  - Curto prazo (`f03164f`+`2f865c1`): helper `resolveEstudio()` (`utils/formatters.ts`) com fallback `estudio || event_data.estudio_nome`, aplicado nos ~7 lugares do Cronograma que ainda liam a coluna crua (header ao vivo, export ZIP, busca, listas, bloco-picker) + em `Credenciais.tsx` (credencial impressa mostrava em branco).
  - **Fix definitivo na origem** (`420776f`, migration `20260706_sync_registrations_estudio.sql`, **aplicada em prod**): backfill 1x (`UPDATE ... SET estudio = event_data->>'estudio_nome'`, resultado real: 38/44 registrations do evento ativo) + trigger `trg_sync_registrations_estudio` BEFORE INSERT/UPDATE que mantém a coluna sincronizada em todo INSERT/UPDATE futuro. Cobre inclusive edge functions Deno (certificados, credenciais) que não conseguem importar `utils/formatters.ts`. Decisão registrada: JS helper é bandaid, SQL trigger é a correção de verdade — mesmo padrão de `protect_registrations_status_columns`.
- **Melhorias na tela Narração IA** (`f03164f`, mockup aprovado antes de codar): aviso de Wi-Fi do modo Sistema visível (apagado) antes de selecionar; descrição da Narração de Saída muda conforme o modo; bloco "Duração estimada por apresentação" (narração+espera+trilha com dados reais do evento); chip "N/total prontas" na voz atual; botão de teste usa dropdown com coreografia real do evento em vez de "Eclipse/Studio Demo"; atalho recíproco "Ir pro Cronograma" ↔ "Ajustar Narração IA".
- **Achados de `/code-review` (2 rodadas, ambas fecharam com 0 bugs de correção sobrando)**:
  - Rodada 1 (`f9744d4`): botão "Ir pro Cronograma" apontava pra rota inexistente `/cronograma` (real é `/manage-schedule`, caía no catch-all pra home); header do player ao vivo mostrava o placeholder de "AGUARDANDO COMANDO" mesmo com coreografia tocando quando `estudio` vinha vazio; guard contra double-advance na trilha (`'ended'`+`'error'` podiam disparar os dois e tocar a saída 2x — mesmo padrão de proteção que a entrada já tinha).
  - Rodada 2 (`2f865c1`, sobre o refactor de reuso): sem bugs de correção; achados de manutenção resolvidos — `SCHEDULABLE_REGISTRATIONS_OR_FILTER` (`utils/registrationStatus.ts`) centraliza o filtro `.or()` de "inscrição agendável" (antes duplicado em Schedule.tsx/AccountSettings.tsx/CheckIn.tsx), `resolveEstudio()` centraliza o fallback, guard por `ref` evita refetch redundante quando o produtor troca de aba e volta pra Narração IA sem o evento ter mudado.
- **Fix de tema** (`d684531`): o `<select>` nativo "Testar com..." não tinha `dark:[color-scheme:dark]` (padrão já usado no input compartilhado da tela) — popup de opções renderizava sempre no chrome claro do SO por cima do app em dark mode.
- **Pendente de validação do user**: testar modo SISTEMA em produção com o Console (F12) aberto — se travar de novo, agora deve aparecer warning/error explicando o motivo exato.
- **Phase 6 (Mesa de Som offline-first) — não implementada, só re-estimada**: ~1 sessão de código (download do pacote via Cache API + camada de leitura offline-aware + indicador de status + outbox do `live_registration_id` + invalidação de cache) + validação real só rola testando sem rede de verdade (evento ao vivo ou simulação proposital). Ver P2 no backlog.

### 2026-07-02 — SEO semântico + rewrite da landing page com PAS ✅ SHIPADO

**SEO / metadados** (arquivo: `D:\Documentos\CoreoHub\index.html` — o único que importa, o SPA shell):
- Open Graph completo (title, description, image, url, type, site_name, locale pt_BR)
- Twitter Card `summary_large_image`
- `<link rel="canonical" href="https://www.coreohub.com/">`
- `<meta name="robots" content="index, follow">`
- JSON-LD `@graph` com 3 schemas: `Organization`, `SoftwareApplication` (com `offers` 10% comissão), `FAQPage` (6 perguntas — sincronizadas com o FAQ do `LandingPage.tsx`)
- `og:image` apontando para `/coreohub-avatar.png` (arquivo que existe e retorna 200 — `/coreohub-logo.png` não existe)
- Validado via Google Rich Results Test: SoftwareApplication ✅ 1 item válido. FAQPage válido mas não gera rich snippet visual (Google restringiu FAQ para gov/big sites desde 2023 — JSON-LD ajuda na citação por IAs)

**Sitemap dinâmico** (`supabase/functions/sitemap-xml/index.ts`):
- Fix: coluna `updated_at` não existe em `events` (só `created_at`) — query silenciosa retornava vazio
- Fix: `verify_jwt = false` faltava em `supabase/config.toml` — Googlebot recebia 401
- Adicionado: suporte a `custom_domain` — evento com domínio próprio usa URL canônica do domínio (evita conteúdo duplicado)
- Google Search Console: sitemap submetido e processado em ambas as propriedades (`www.coreohub.com` e `app.coreohub.com`), 6 páginas indexadas ✅

**Landing page rewrite** (`pages/LandingPage.tsx` — commit `27d8443`):
- Framework PAS (Problema → Agitação → Solução) no topo
- Hero: "Seu festival inteiro rodando sozinho. Você só cuida da arte."
- Seções novas: Problema, Agitação, Solução (com 3 passos + destaque "página gratuita")
- Prova Social: 3 cards placeholder (substituir por depoimentos reais de jurados/produtores)
- Mantidos: IA configura, Seletiva de Vídeo, Cronograma Inteligente, Credenciais Físicas, Narração IA, Calculadora, Comparativo, FAQ (crítico pro JSON-LD FAQPage), CTA Final
- Removidos: Em Campo, Galeria, Vitrine, Dia do Evento, Features Grid, Demo CTA, Festivais Ativos, Feito por quem dança, Bailarino procurando festival
- Removido: estado `festivaisAtivos`, interface `FestivalAtivo`, `fetchAtivos` useEffect, `fmtFestivalDate`
- 0 erros TypeScript (tsc --noEmit limpo)

**Nota**: `D:\Documentos\CoreoHub Site` é um protótipo estático local, **não** é o que está em produção. A pasta de produção é `D:\Documentos\CoreoHub` (Vite React app, auto-deploy Vercel via branch `main`).

### 2026-06-28 — Domínio próprio: fix preview WhatsApp/Telegram (filesystem ganha de rewrite) ✅ SHIPADO
1 commit (`cffdaab`), seguindo o domínio próprio por evento shipado mais cedo no mesmo dia (`e58590a`, `festival.usualdance.com`). Disparado pelo user notando que o preview ao compartilhar `festival.usualdance.com` no WhatsApp mostrava o card genérico do CoreoHub, não os dados do evento.

- **Causa raiz**: o `vercel.json` tinha um rewrite condicional (`has: host=festival.usualdance.com` + UA de bot) pra rota raiz `/` → `/api/og?slug=...`. Confirmado via doc oficial da Vercel: **a Vercel sempre prioriza o filesystem sobre rewrites quando o path bate com um arquivo literal** — `/` sempre resolve pro `index.html` do build, então aquele rewrite nunca era avaliado. Bots recebiam sempre as meta tags genéricas do `index.html`. Os outros rewrites de bot (`/evento/:slug`, `/festival/:slug`, `/workshop/:slug`) sempre funcionaram porque esses paths são rotas virtuais do SPA, não arquivos reais.
- **Fix**: `middleware.ts` novo na raiz do projeto (Vercel Routing Middleware, roda ANTES da resolução de arquivo estático — `@vercel/functions` como devDependency). `matcher: '/'` limita a execução só na rota raiz. Mapa `CUSTOM_DOMAIN_SLUGS` (hostname→slug) continua hardcoded — novo domínio custom = 1 linha nova + redeploy. Regra morta removida do `vercel.json`.
- **Validado em prod** via curl com UA `WhatsApp/2.23.20.0`: `og:title`/`og:description`/`og:image` corretos do evento.
- **Decisão registrada**: sem UI de domínio custom no painel do produtor por ora (avaliadas 2 opções — manter manual venceu). Mesmo com UI, ainda sobram 2 passos fora do app (você adicionar o domínio no Vercel + produtor configurar CNAME); baixo ROI com 1 produtor usando. Revisitar se aparecer 2º pedido. Detalhes em [[backlog-dominio-custom-evento]].

### 2026-06-21 — Terminal de júri: 2 bugs críticos + tela de fila como entrada ✅ SHIPADO
8 commits (`51da0cf` → `e2b53c9`). Disparado pelo Hemer testando o terminal do júri com o cronograma real do Usualdance pela 1ª vez (só tinha usado modo demo antes). Detalhes em [[terminal-juri-bugs-criticos-e-fila-shipado]].

- **🚨 Bug raiz — `resolveActiveEvent()` sempre retornava `null`** (`81a35f6`): a edge function `judge-login` selecionava `events.status`, coluna que **não existe** (é `events.state`). Query falhava (PostgREST 400), código só desestruturava `{ data }` sem checar `error` → terminal via PIN nunca resolvia o evento ativo **em produção, desde sempre**. Validado via curl direto na function antes/depois (32 registrations após o fix). Só não tinha sido percebido porque o demo (`seed-demo-event`) seta dados fictícios que mascaravam o problema.
- **Bug 2 — terminal só aceitava `status='APROVADA'`** (`c62adda`): mesmo critério que faltava desde o fix do Cronograma em 2026-06-10 — replicado `.or('status.eq.APROVADA,status_pagamento.eq.APROVADO,status_pagamento.eq.CONFIRMADO')` + `excluded_from_schedule`.
- **Fix logout** (`f27497e`): "Sair do terminal" navegava pra `/judge-login` sem o `producer_token` (rota real é `/judge-login/:token`) → caía em "Link inválido ou expirado". Corrigido pra preservar o token.
- **Feature — tela de fila como ponto de entrada** (`6462199`): pesquisa de mercado (CompetitionSuite Judge App) confirmou que jurado escolhe a apresentação numa lista antes de avaliar, não auto-avança. CoreoHub preserva o diferencial que o CompetitionSuite não tem (sync em tempo real via `live_registration_id` — Mesa de Som pode puxar o jurado pra apresentação certa mesmo direto da fila, sem precisar ter submetido nada antes).
- **Fix badge "AO VIVO"** (`9ecf592`): aparecia sempre que havia apresentação carregada, não só quando era de fato a marcada pelo Mesa de Som. Corrigido pra só mostrar quando `currentPerformance.id === liveRegistrationId`.
- **Itens já avaliados ficam visíveis mas apagados na fila** (`e2b53c9`): padrão do Voto Popular replicado — opacidade reduzida + CTA muda pra "AVALIADA" cinza.
- **Cleanup lateral** (`51da0cf`+`1f091fd`): dedup de gêneros duplicados no editor de jurados (`getAllGenres` sem `eventId` trazia de todos os eventos do produtor) + `utils/styleMatch.ts` novo, eliminando lógica de normalização de estilo triplicada.
- **Lição operacional**: 1º commit do dia ficou só local por ~20min antes de eu notar que faltava `git push` — deploy do Vercel só reflete `origin/main`. Ver [[feedback-commit-local-nao-e-deploy]].

### 2026-06-21 — Tipo de Júri (Técnico/Artístico) por estilo + limpar avaliações teste ✅ SHIPADO
4 commits (`06ae194` → `c71247c`). Detalhes em [[tipo-juri-por-estilo-shipado]].

- **Apuração**: botão "Limpar avaliações de teste" (header + por linha), só atua em `evaluations` de registrations não publicadas (`resultado_publicado=false`) — nunca toca resultado já visível pro inscrito.
- **Tipo de Júri corrigido a meio da sessão**: 1ª versão guardava 1 valor fixo por jurado (`judges.tipo_juri`, migration `20260626`). User corrigiu — jurado pode ser Técnico num estilo e Artístico noutro (ex: Sarah Tinel: Técnica em K-Pop, Artística em Danças Urbanas). Migrado pra `judges.competencias_artisticas TEXT[]` (migration `20260627`, revoga a anterior — nunca editar migration já aplicada).
- **UI do editor**: 2ª iteração de design (lista vertical + `<select>` Técnico/Artístico por estilo), escolhida via `AskUserQuestion` com mockup ASCII comparando padrões de mercado (Google Drive/GitHub) — user pediu visual antes de codar pra config voltada ao produtor. Ver [[feedback-mostrar-mockup-antes-ui-didatica]].
- **Vitrine pública**: badge Técnico/Artístico removido — pesquisa de mercado (DanceComp Genie/CompetitionSuite/Headliners/Inferno Dance) confirmou que nenhuma plataforma expõe essa distinção pro público.
- **Combinação de nota**: peso configurável Técnico×Artístico (default 50/50) em Avaliação → Critério Artístico, `ResultsPanel.tsx` mostra breakdown + warning quando um grupo mistura coreografias com escalas diferentes.

### 2026-06-20 (madrugada) — Bundle P1 + cosméticos do backlog ✅ SHIPADO
5 commits (`38cc6cb` → `8abf199`). Pegou os itens "P1 — Alta prioridade, baixo esforço" + 1 cosmético do backlog registrado na sessão anterior. Detalhes em [[pageheader-adocao-completa-shipado]].

- **`PageHeader.tsx` adoção completa (`38cc6cb`)**: as 10 páginas que faltavam migradas. Padrão pra headers com badge/eyebrow acima do h1 (SuperAdmin, JudgePractice, TermoProdutor): badge fica como sibling fora do `PageHeader`, que só cobre `icon`+`title`+`subtitle`+`actions`. Validado com Playwright (8 rotas) + print real do `/super-admin` confirmando o painel pós-MFA (2FA) — pendência fechada.
- **Bundle Workshops, 3 fixes do `/revisar` de 2026-06-18 (`5f86f21`)**: upload de capa saiu do `<Field>`/`<label>` (corrige abrir o seletor de arquivo 2x — click forwarding do label + onClick do botão Camera), capa 0.3MB→0.15MB, `rounded-xl`→`rounded-2xl`.
- **`resolveActiveWorkshopLot`/`findNextWorkshopLot`/`<AvisoViradaLote>` (`7c4e2ae`)**: deduplica a lógica de "lote vigente de workshop" + aviso "Sobe pra R$X em [data]" que estava em 4 lugares (`PublicEventPage` card de workshop, `WorkshopsManagement` painel do produtor, `PublicWorkshopPage` detalhe). Bug lateral corrigido de brinde: o painel do produtor pegava o lote de MENOR ordem entre os ativos (faltava `ORDER BY ordem DESC`), mesma classe de bug do RPC `get_workshop_stock` corrigido em sessão anterior (`cd2981b`), só que essa instância client-side nunca tinha sido pega.
- **Receita de Seletiva na Visão Geral (`049de7f`)**: avaliado e decidido **não implementar** precisão real — `registrations.charged_amount` é compartilhado entre o pagamento de inscrição e o de taxa de seletiva (a 2ª escrita sobrescreve a 1ª na mesma row), precisão exigiria coluna dedicada. Documentado inline, sem migration.
- **NotificationBell paginação (`57d5bd5`)**: trocado `limit(50)` por `.range()` + botão "Ver mais" no fim da lista, paginando por offset (não desloca a janela quando realtime insere notif nova no topo).
- **`/revisar` achou 4 itens, todos corrigidos (`8abf199`)**: 🟡 botão de refresh do CheckIn (só ícone) sem `aria-label`; 🟡 ícone `FileText` decorativo no `icon` do PageHeader do TermoProdutor sem `aria-hidden`; 🟡 label da capa do workshop perdeu associação com o botão de upload ao saltar fora do `<Field>` (fix com `aria-describedby`); 🟢 `resolveActiveWorkshopLot` trocou `is_active !== false` por `is_active === true` (intenção mais clara).
- **Validação**: `npm run lint` + `npm run build` limpos em todos os 5 commits. Screenshots Playwright em rotas afetadas (0 erros/sem overflow). Modais (capa do workshop, dropdown do sininho) não puderam ser validados visualmente pelo script read-only — confirmar manualmente na próxima vez que usar.

### 2026-06-20 (noite) — Auditoria de headers (continuação) + /revisar ✅ SHIPADO
2 commits (`0a2c1bf` + `d5436a0`). Continuação de uma sessão anterior que ficou lenta e foi retomada a partir do todo list parcial (print do user). Detalhes em [[auditoria-headers-revisar-shipado]].

- **Continuação da auditoria (`0a2c1bf`)**: fixados os itens que faltavam — `JudgeManagement.tsx` (`/equipe-jurados-config`, confirmado que é arquivo diferente de `JudgesManagement.tsx` que já estava correto), `JudgePractice.tsx`, `TermoProdutor.tsx` (removido `min-h-screen` duplicado — já vem do `PrivateLayout` — + itálico fora de modal). `RegistrationGradeConfig.tsx` e `Live.tsx` confirmados como stubs ("Página em construção") — fora de escopo. `StageMarker.tsx` investigado a fundo: não tem h1 (só eyebrow pequeno dentro do widget timer) — confirmado como exceção full-bleed intencional já documentada (mesma lista de `Schedule`/`CheckIn`), não é pendência.
- **`/revisar` achou 3 itens, todos corrigidos (`d5436a0`)**: 🟡 ícone `Award` dentro do h1 de `Certificates.tsx` sem `aria-hidden` (mesmo padrão já aplicado em WsMetric/Metric); 🟡 `components/PageHeader.tsx` criado na sessão anterior mas órfão (zero imports, nenhuma página migrada — o "drift" que o componente deveria resolver continuava possível); 🟡 subtítulos inconsistentes entre páginas (`text-[9px] font-bold` vs `text-[10px] font-black` canônico). Fix: migrado `Bailarinos.tsx` + `CentralDeMidia.tsx` + `Profile.tsx` pra consumir `PageHeader.tsx` (resolve a inconsistência de subtítulo de uma vez) + `aria-hidden` no ícone do Certificates + `aria-label` no botão "Recarregar" de CentralDeMidia (antes só tinha `title`, não confiável em leitor de tela).
- **Validação**: `npm run lint` + `npm run build` limpos nas duas rodadas. Screenshots Playwright contra prod em 16 rotas (produtor) + `/super-admin` (admin) — todas 0 console errors + sem overflow. **Pendência de validação**: `/super-admin` fica atrás do gate MFA (2FA) — o script read-only não digita OTP, então só o gate foi confirmado visualmente, não o painel real pós-h1.

### 2026-06-20 — Régua de KPIs no hub de Workshops + padronização de container ✅ SHIPADO
3 commits (`df3f0a5` + `d420e9e` + `933ccdb`). Só frontend, zero migration/edge. Resolve os 2 itens pendentes deixados pela sessão de 2026-06-19 (noite). Detalhes em [[workshops-kpis-e-padronizacao-container-shipado]].

- **Item A — Resumo de vendas em `/workshops-do-evento` (`df3f0a5`)**: a tela só mostrava receita escondida no modal "Compradores" de cada aula. Régua de 4 cards no topo (componente `WsMetric`, espelha o `Metric` do `VendasIngressos.tsx`): Receita (só `APROVADO`), Vagas vendidas (`X / Y · Z% lotado`, inclui cortesia/combo grátis que ocupam vaga), Pendentes, Presenças. Dados via 1 query batched `workshop_registrations.in('workshop_id', ids)` no `refresh()` (paralelizada com lots via `Promise.all`) — respeita o filtro de escopo. Pesquisa de mercado: Eventbrite/Sympla/Cvent combinam gestão + vendas na mesma tela. Cortesia/grátis conta em vagas mas R$ 0 na receita.
- **Item B — Padronização de container (`d420e9e`)**: aplicado `max-w-7xl mx-auto` no root de 6 telas full-bleed que são dashboards/listas (ProducerDashboard, JudgesManagement, EquipeProdutor, ResultsPanel, Deliberacoes, SuporteJuri). Exceções full-bleed mantidas de propósito: Schedule (kanban), StageMarker (grid de palco), CheckIn (scanner QR). Não tocados (formulários centrados leem melhor estreitos): Schedule `4xl`, RegulationAIParser `4xl`, Certificates `6xl`, AccountSettings. Container canônico: `max-w-7xl mx-auto space-y-6` (+ `pb-20` quando aparece no BottomNav mobile).
- **Achados do `/revisar` (`933ccdb`)**: 2 🟢 aplicados em `WsMetric` + `Metric` (VendasIngressos, pra paridade) — `aria-hidden` no ícone decorativo + label `text-[9px]` → `text-[10px]` (DESIGN_SYSTEM). 🟢 restante (card "Presenças" mostra `0 / 0` em escopo sem inscrição) deixado: é informativo e some com a 1ª inscrição.
- **Pendência**: validação visual Playwright não rodada (depende de auth de prod) — vale smoke do `% lotado` na régua e do respiro lateral das 6 telas em mobile 375px.

### 2026-06-19 (noite) — Padronização do topo do hub de Vendas + tab "Visão Geral" ✅ SHIPADO
Disparado pelo user notando que o topo (régua de abas + título) ficava em alturas/larguras diferentes ao trocar entre Inscrições/Ingressos/Workshops/Seletiva/Cupons. Causa: cada tela definia seu próprio container (`max-w-7xl` em 2 delas, full-width nas outras 3) e `space-y-8` vs `space-y-6` colidindo com o `mb-6` do `VendasTabs`, gerando gap de 24px numas e 32px noutras.

- **Moldura única nas 5 telas**: `Registrations.tsx`, `VideoSelection.tsx`, `Coupons.tsx` passam a `max-w-7xl mx-auto space-y-6` (igual `VendasIngressos.tsx`, que já estava correto). `WorkshopsManagement.tsx` já tinha `max-w-7xl mx-auto`, só precisava do título padronizado.
- **Título uppercase em todas**: `Registrations` (`text-2xl md:text-3xl` → `text-3xl` fixo), `WorkshopsManagement` (`tracking-tight` sem caixa → `uppercase tracking-tighter`, mantendo o ícone `GraduationCap` por decisão do user), `Coupons` (`text-2xl md:text-3xl` → `text-3xl`, preserva o eyebrow "Promoções" que é único dela).
- **Nova tab "Visão Geral" (`pages/VendasOverview.tsx`, rota `/vendas`)**: resolve a pendência registrada antes — card de Total Geral + 4 cards clicáveis (Inscrições/Ingressos/Workshops/Seletiva) com receita e contagem por fonte, navegam pra aba detalhada ao clicar. Cálculo client-side: `registrations` (via `isRegistrationPaid`), `audience_tickets` (`status_pagamento='APROVADO'`), `workshops`→`workshop_registrations` (join manual por `workshop_id IN (...)`, já que a tabela não tem `event_id` direto), `video_fee_status='paid'` × `events.video_selection_fee` pra seletiva (taxa fixa por evento, sem coluna de valor por registro). Adicionada como 1ª aba no `VendasTabs.tsx` e no `Sidebar.tsx` (ícone `LayoutDashboard`).
- **Sem impacto no inscrito**: as 6 telas do hub (5 + Visão Geral) são exclusivas do painel do produtor — inscrito nunca vê essa régua.

### 2026-06-19 — Hub de Vendas + Cortesia direta + Voto desacoplado da narração + fix mobile Credenciamento ✅ SHIPADO
4 commits (`ad54f38` feature + `01f3fc8` fix `/revisar` + `4db5a91` hub completo/alinha abas + `6cfd612` a11y `<nav>`) + 1 edge function nova deployada (`create-courtesy-entry`). Disparado por pedido do user de revisar 4 frentes com boas práticas de mercado (UX/UI). Detalhes em [[hub-vendas-cortesia-voto-mobile-shipado]].

- **Voto Popular ↔ Cronograma**: novo botão "Marcar AO VIVO" (ícone `Radio`) em `Schedule.tsx` grava `events.live_registration_id` (sincroniza o Voto via trigger `trg_notify_vote_stage`) **sem** disparar a narração IA — pro caso do anúncio ser feito manualmente no microfone do som. O botão "Iniciar" continua fazendo as duas coisas juntas como atalho padrão (comportamento anterior preservado).
- **Hub "Vendas"**: seção do menu "Bilheteria" renomeada pra "Vendas" (`Sidebar.tsx`). Componente `components/VendasTabs.tsx` é a régua de abas compartilhada. **Cobertura completa (`4db5a91`)**: o hub agora abrange TUDO que gera receita/comissão na ordem do funil — **Inscrições, Ingressos, Workshops, Seletiva, Cupons** (antes só Ingressos+Workshops+Cupons). Inscrições e Seletiva ficam **dual-list** (aparecem também em "Operação" pra curadoria) — mesma rota, contextos diferentes, igual o Cronograma já faz; a dedup do Sidebar é por-seção, então o dual-list é intencional, não bug. `VendasTabs` adicionado a `Registrations.tsx` e `VideoSelection.tsx` (faltavam). Continuam sendo rotas separadas (cada uma mantém state/fetch pesado), mas a navegação parece 1 hub único (padrão Stripe Dashboard / Sympla Painel de Vendas). Tab "Visão Geral" consolidada e padronização do topo shipadas na sessão seguinte (ver entrada acima).
- **Fix altura das abas (`4db5a91`)**: a régua `VendasTabs` aparecia em alturas Y diferentes entre telas porque `WorkshopsManagement` tinha wrapper extra (`min-h-screen` + `py-8 px-...`) por cima do `p-3 lg:p-4` que o `PrivateLayout` (App.tsx) já aplica a toda página. Colapsado pra `max-w-7xl mx-auto`, igual as outras — `VendasTabs` agora é o 1º filho sem padding-top próprio nas 5 telas → mesma posição em desktop e mobile.
- **A11y `<nav>` (`6cfd612`, achado do `/revisar`)**: a régua de abas comunicava o estado ativo só visualmente (cor + shadow). Agora `<nav aria-label="Vendas">` + `aria-current="page"` no botão ativo — leitor de tela anuncia qual aba está selecionada.
- **Cortesia (convite gratuito direto, sem cupom)**: schema já suportava (`status_pagamento='CORTESIA'` em `audience_tickets`/`workshop_registrations`, `video_fee_status='waived'` na seletiva) mas faltava a ação no painel. Nova edge function `create-courtesy-entry` (valida dono do evento/workshop via JWT, insere com `preco=0` sem passar pelo Asaas) + botão "Adicionar cortesia" em `VendasIngressos.tsx` (modal) e `WorkshopsManagement.tsx` (form inline no `BuyersModal`). Na Seletiva, botão "Isentar taxa" seta `video_fee_status='waived'` direto via `updateVideoFeeStatus` (RLS já permitia, só faltava UI). Inscrição continua usando cupom 100% (único caminho onde Asaas aceita cobrança R$0).
- **Credenciamento mobile** (`CheckIn.tsx`): botão "Check" `w-16→w-20` + `min-h-11` (alvo de toque), abas de filtro com `tracking-tight sm:tracking-widest` (texto não trunca mais em 375px), frame do scanner QR `w-64` fixo → `w-[min(70vw,256px)]` responsivo.
- **`/revisar` (`01f3fc8`)**: 4 achados corrigidos na hora — cor de cortesia inconsistente (`blue` só na VendasIngressos vs `violet` já usado em Workshops/MeuWorkshop, padronizado em `violet` nos 3 lugares novos + badge existente); modal de cortesia novo sem `createPortal` (violava a lição [[licao-createPortal-stacking-context]] já documentada); inputs do form inline de Workshops só com `placeholder` (sem `<label>` associado); botões de ícone (Anunciar, Marcar AO VIVO) sem `aria-label`.
- **Deploy**: `supabase functions deploy create-courtesy-entry` via CLI + push pra `main` (Vercel auto-deploy).

### 2026-06-18/19 — Lotes de workshop nunca funcionaram de fato + combo de desconto travado + UX de Lotes ✅ SHIPADO
4 commits (`a830b47`, `cd2981b`, `d55b346`, `3d96242`). Disparado por relato do user: "lotes publicados não estão aparecendo na vitrine" + depois "comprei testando desconto de inscrito e não mudou o preço". Detalhes em [[workshop-lotes-rpc-e-combo-shipado]].

- **Bug 1 — ordenação errada (`a830b47`)**: `get_workshop_stock` fazia `ORDER BY ordem ASC` (lote de ordem mais baixa) quando o comentário no próprio SQL dizia "ordem mais alta". Corrigido pra `DESC`.
- **Bug 2 — função sempre falhava silenciosamente (`cd2981b`, migration `20260624`)**: `get_workshop_stock` tinha `column reference "capacidade_max" is ambiguous` (a coluna de saída da `RETURNS TABLE` tem o mesmo nome de `workshops.capacidade_max`) — bug existente desde a criação original (`20260519_workshops.sql`), nunca pego porque o frontend só loga `console.warn` e cai no fallback `preco_padrao`. Resultado: a vitrine **nunca** mostrou preço de lote, sempre o preço padrão do workshop, independente de qualquer config de data/lote. Fix: qualificar a coluna com alias (`w.capacidade_max`).
- **Bug 3 — desconto de inscrito da mostra nunca aplicava (`d55b346`, migration `20260625`)**: `detect_workshop_combo` exigia `r.status = 'APROVADA'` (curadoria manual no painel, botão "Aprovar" em `Registrations.tsx`) ALÉM do pagamento aprovado. O Hemer nunca usa essa aprovação manual separada — pra ele, pagamento aprovado já é a inscrição válida. Resultado: **nenhuma** das inscrições pagas do Usualdance Festival se qualificava pro desconto. Fix: exige só `status_pagamento IN ('APROVADO','CONFIRMADO')`, mesmo critério de `emit-certificates-batch`/`isRegistrationPaid()`. Validado com inscrição real via `supabase db query --linked` antes de aplicar (`found: true`).
- **Feature — "Aplicar nos outros workshops" (`cd2981b`)**: modal de Lotes ganhou checkboxes de workshops-alvo + botão por lote que copia (ou faz upsert por `ordem`) preço/datas/estoque pra outros workshops do mesmo evento — evita reconfigurar lote por lote manualmente.
- **UX — botão Salvar explícito (`cd2981b`)**: modal de Lotes trocou autosave por campo (`onBlur`) por edição local + botão "Salvar" único (toggle Ativo/Remover continuam imediatos). Indicador visual "· não salvo" + guard de confirmação ao fechar com edição pendente.
- **Copy — "Inscrever-se" → "Comprar" (`cd2981b`)**: CTA de workshop pago alinhado ao padrão já usado em ingressos (mercado BR: "Comprar" = transação direta, "Inscrever-se" reservado pra competição/lista). Mantém "Confirmar inscrição grátis" no caso sem cobrança.
- **`/revisar` (`3d96242`)**: 3 achados corrigidos na hora — inputs de Preço/Estoque com `value` controlado + `Number(...)` no `onChange` comiam o ponto decimal a cada tecla (impossível digitar centavos fluentemente); mensagens de feedback sem `aria-live`; contraste de "· não salvo" (`amber-500` em fundo branco, ~2.4:1) abaixo do mínimo AA.
- **Operacional**: migrations `20260623`/`20260624`/`20260625` aplicadas direto em produção via `supabase db query --linked --file ...` (não pelo SQL Editor do Dashboard — primeira vez usando esse caminho na sessão, mais rápido pra correções urgentes; segue válido colar no Dashboard quando preferir o fluxo manual).

### 2026-06-18 (noite) — Vitrine de workshop: preço do lote vigente + aviso de virada ✅ SHIPADO
3 commits (`16248c3` → `dc74b70`). Disparado por pergunta do user: "deveria mostrar que é lote 1?". Detalhes em [[workshop-lote-vigente-vitrine-shipado]].

- **Bug encontrado**: o card de Workshop na listagem (`PublicEventPage.tsx`) sempre mostrava `ws.preco_padrao`, nunca consultava `workshop_lots` — preço cheio na vitrine mesmo com lote promocional ativo. A página de detalhe (`PublicWorkshopPage.tsx`) já resolvia certo via RPC `get_workshop_stock`, mas essa RPC só funcionou de fato depois do fix de coluna ambígua (`cd2981b`, mais cedo na mesma sessão) — antes caía silenciosamente no fallback.
- **Fix**: card busca o lote ativo de todos os workshops em 1 query batched (`workshop_lots .in(workshop_id)`), evitando N round-trips. Replica a mesma regra do RPC (maior `ordem`, `is_active`, dentro da janela `data_inicio`/`data_fim`).
- **Aviso "Sobe pra R$X em [data]"**: replicado no card e no detalhe, mesmo padrão de urgência já usado em Ingressos/Inscrições.
- **Achado do `/revisar` corrigido na hora**: polling de 30s do detalhe só recalculava o preço, não o aviso de virada — se a virada acontecesse com a aba aberta, o aviso ficava com data já passada. Extraído `fetchProximoLote()` chamado no mount e no tick.
- ✅ **Resolvido 2026-06-20**: lógica de "achar próximo lote" + JSX do aviso extraída pra `utils/lotes.ts` (`resolveActiveWorkshopLot` + `findNextWorkshopLot`) + `components/AvisoViradaLote.tsx`, aplicada nos 4 lugares (ingressos/inscrições/card workshop/detalhe workshop). Bug lateral corrigido de brinde: `WorkshopsManagement.tsx` (painel do produtor) pegava o lote de MENOR ordem entre os ativos (faltava ordenar DESC), não o vigente. Detalhes em [[pageheader-adocao-completa-shipado]].

### 2026-06-18 — Bundle melhorias Workshops (5 blocos) + bug crítico seed-demo ✅ SHIPADO
4 commits (`b6423d4` → `cd759cd`). Plano de 5 blocos aprovado + 1 fix crítico descoberto no meio da sessão. Detalhes em [[bundle-workshops-melhorias-2026-06-18]].

- **Bloco 1 — Frase na vitrine** (`b6423d4`): `<p>` com "Aprimore sua técnica com quem é referência. Inscritos da mostra têm preço especial." abaixo do `<h2>Workshops</h2>` em `PublicEventPage.tsx` (tinha sido removida no `0b3e69b`).
- **Bloco 2 — Capa do workshop** (`b6423d4`): upload no `WorkshopFormModal` (mesmo padrão de compressão webp da foto do professor, mas 1200×675 / 0.3MB). Hero do `PublicWorkshopPage.tsx` cai pra `professor_photo_url` quando não há capa.
- **Bloco 3 — Desconto visível no checkout** (`c0c5920`): botão mostra preço final (`Inscrever-se · R$ X`) + trava submit enquanto `comboLoading`.
- **Bloco 4 — Anti-fraude do combo (Opção 2)** (`c0c5920`, migration `20260622` **aplicada em prod**): `detect_workshop_combo` agora exige `p_user_id` e só aplica desconto se a inscrição achada pertence ao comprador logado OU o CPF buscado é o próprio CPF de perfil dele. Antes, qualquer CPF de inscrição aprovada dava desconto pra qualquer comprador (sem checar dono). Edge function decodifica o JWT pra extrair `user_id`; sem login, nem chama a RPC — frontend mostra CTA "Entre na sua conta".
- **Bloco 5 — CTA upsell em Minhas Inscrições** (`4dd4cd4`): card "Garanta seu workshop com preço de inscrito" por evento (só se o evento tem workshop publicado). Desconto só pra inscrição PAGA; pendente vira nudge "Finalize sua inscrição...".
- **🚨 Bug crítico descoberto e corrigido (`cd759cd`)**: user reportou que os workshops reais do Hemer (Usualdance Festival) desapareceram da vitrine e do painel depois que ele criou um evento DEMO pra teste. SQL confirmou tabela `workshops` com 0 rows em prod. Causa: `seed-demo-event` (`cleanupBeforeEventDelete`) tinha 2 `DELETE FROM workshops WHERE created_by = userId` **sem filtro `.is('event_id', null)`** — deveria limpar só workshops standalone órfãos do demo, mas sem o filtro apagava TODOS os workshops do produtor, inclusive os de eventos reais. Fix + redeploy via CLI. **Sem backup disponível (plano FREE do Supabase) — Hemer precisou recriar os workshops manualmente.**
- **Achados de revisão (`/revisar`, não corrigidos ainda)**: upload de capa ficou dentro de `<Field>` (que renderiza `<label>` envolvendo o input file + botão) — pode abrir o seletor de arquivo 2x ao clicar no botão Camera (🔴 bug funcional); capa comprimida a 0.3MB inline (2x a foto do professor) infla o payload de `select('*')` na listagem (🟡 perf); `rounded-xl` no preview da capa vs `rounded-2xl` no avatar do professor (🟢 cosmético). Ver pendência em "O que falta fazer".

### 2026-06-14 — Card de saldo do produtor: copy claro (não era bug) ✅ SHIPADO
Commit `2b5a7d5`. Só `ProducerBalanceCard.tsx`. User achou contraditório "R$ 0,00 disponível agora" ao lado do botão "Transferir agora — R$ 1.806,30". **Não era bug de cálculo** — D+7 correto (0 passou do hold, retido liberando em 17h; saldo real Asaas batendo). Só copy: "disponível agora" → **"já liberado"**, "a liberar {time}" → **"retido · libera {time}"**. A palavra "disponível" confundia porque o retido TAMBÉM é sacável agora (antecipação). Detalhes em [[settlement-d7-shipado]].

### 2026-06-14 — Voto Popular: MVP construído + plano de integração CoreoHub 🚧 EM ANDAMENTO

Produto **NOVO** da CoreoHub (voto popular ao vivo durante a live do YouTube), em **infra ISOLADA** (Supabase/repo/Vercel próprios — não mistura com a CoreoHub: protege a CoreoHub do pico de carga da live + zero PII cruzada). Piloto Usualdance 2026 (11/07). Pasta local `D:\Documentos\Voto Popular`, repo `coreohub/voto-popular`, domínio `vote.usualdance.com`, Supabase ref `aghmjmqrkwuxmrctslkf` (São Paulo). Decisão: produto CoreoHub, Usualdance = tenant 1 (não dono). Detalhes em [[voto-popular-mvp]].

- **MVP funcionando E2E em prod (1 dia):** login OTP-first (Supabase manda 8 dígitos, não 6) + Google (marca "Usualdance" verificada no GSC), schema multi-tenant + RLS, fluxo de voto (favorito→espera→voto→confirmação) em realtime, edge `cast-vote` (valida janela no backend + 1 voto/grupo via índice único), console `/operador` (allowlist de e-mail), resultado secreto por design.
- **✅ Integração Fluxo A (grupos do cronograma) SHIPADO 2026-06-14:** botão "Publicar no Voto" no `Schedule.tsx` → edge fn `publish-vote-groups` (CoreoHub) → `import-groups` (Voto). Push assinado por token, sem credencial cruzada. Unidade votável = coreografia; âncora em cascata `event_data.estudio_nome → coreografo_nome → inscrito_nome` (nunca vazia); bailarinos (nomes, sem CPF) só na busca + tela de voto; card com title-case; **fluxo de voto sequencial** (após votar → "aguarde o próximo" → pula sozinho quando abre a próxima janela). Bônus: painel de edição de inscrição (`Registrations.tsx`) ganhou 3 campos (nome coreografia/estúdio/coreógrafo, estúdio+coreógrafo merge no `event_data`) + Wizard normaliza CAIXA ALTA no save. Detalhes em [[voto-integracao-coreohub-plano]].
- **✅ Integração Fluxo B (voto abre sozinho) SHIPADO 2026-06-14 (testado):** trigger `trg_notify_vote_stage` AFTER UPDATE OF `events.live_registration_id` → edge fn `notify-vote-stage` (CoreoHub, segura o token) → `stage-webhook` (Voto) abre/fecha janela. Push assíncrono (pg_net), não trava a Mesa de Som. Clicar "Iniciar" no Cronograma já abre o voto sozinho. `/operador` vira fallback manual.
- **✅ OTP 6 caixinhas SHIPADO 2026-06-14:** lição — Supabase manda OTP de **8 dígitos por padrão** (Auth → Sign In/Providers → Email → "Email OTP length"); o login OTP da CoreoHub (`Auth.tsx`, webview Instagram) assumia 6 hardcoded e nunca foi testado com código real → **cliente bloqueado na inscrição** (receita parada). Fix: OTP length=6 no Supabase + componente `OtpBoxes` (6 caixas auto-avança, colar preenche todas, não hardcoda tamanho). Mesmo no Voto.
- **Falta no Voto:** dashboard de métricas (case de venda), patrocinador (telas 1/6), antifraude (Turnstile + rate-limit IP), polimentos pro dia (limite de e-mail, Broadcast pra escala, sender @usualdance.com), **ideia: mostrar grupos já apresentados + os que vêm (sem revelar votos)**. ✅ Gap do ESTÚDIO resolvido em 2026-07-06 (backfill + trigger `trg_sync_registrations_estudio`, ver Histórico recente) — `registrations.estudio` agora sincroniza sozinho a partir de `event_data.estudio_nome`.

### 2026-06-13 — Fluxo de criar 2º evento (ponto de entrada quebrado) ✅ SHIPADO
2 commits (`13d140e` + `4eed0b0`). Só frontend. Diagnóstico veio de pergunta do user ("existe fluxo pra produtor criar novo evento? não lembro").

- **Diagnóstico:** o suporte a múltiplos eventos por produtor SEMPRE existiu nos dados (`events` por `created_by`, `EventPickerSheet` troca, app multi-tenant por `event_id`) e o fluxo real de criação é o `OnboardingWizard` em **`/criar-evento`** (`CriarEventoGate` → se logado renderiza o wizard → `createEvent`). MAS nenhum botão in-app apontava certo: o botão **"Novo Evento"** do `ProducerDashboard` ia pra `/account-settings`, que **edita o evento mais recente** (`order created_at desc limit 1`), não cria. Por isso o produtor que voltava nunca achava o "criar novo".
- **Fix (`13d140e`):** botão "Novo Evento" → `navigate('/criar-evento')`.
- **Feature (`4eed0b0`):** atalho "+ Criar evento" no rodapé do `EventPickerSheet` (onde o produtor troca de evento) — opt-in via prop `onCreateNew?: () => void` (componente fica desacoplado de rota; usado em 5 telas, só o Dashboard passa a prop). Rodapé fixo nos 2 layouts (sheet mobile + dropdown desktop). Os dois pontos de entrada coexistem (botão do header + atalho no picker, padrão Sympla/Notion).
- **Guard anti-duplicata:** OnboardingWizard só redireciona pra evento existente quando nome+ano batem (case-insensitive) — criar com nome/ano diferente funciona normal. Detalhes em [[criar-segundo-evento-entrypoint-fix]].

### 2026-06-11 — Terminal júri navegação manual + voz narração + teclado PIN + frase LGPD ✅ SHIPADO
7 commits (`04c91a1` → `1c88753`). Só frontend — nenhuma migration/edge function. Tudo em prod (Vercel).

- **Teclado do PIN do login** (`04c91a1`): `JudgeLogin.tsx` só tinha numpad on-screen (onClick); faltava listener de keydown. No desktop digitar não fazia nada. Agora aceita 0-9/Backspace/Esc (guard de 4 dígitos dentro do updater pra robustez contra closure velha).
- **Voz da narração no cronograma** (`6b1fc39`): bug multi-tenant. `/narracao-ia` salva `voice_id` na row do evento (só grava legacy `id='1'` se super admin); `Schedule.tsx` lia de `id='1'` → narração com voz velha/default ("aleatória"). Fix: lê/grava config por `event_id` (fallback legacy). `handleSaveSettings` (tempo_entrada/intervalo) idem (era `id='1'` compartilhada = vazamento). Lição: `.eq('id',1)` direto em `configuracoes` = bug latente multi-tenant. Detalhes em [[cronograma-voz-narracao-event-row-fix]].
- **Navegação manual no terminal** (`0a50ee7` + `967b44f` + `6aed319` + `e211d9b`): jurado não decora número. Menu `⋮` vira painel com fila inteira rolável (toque pra ir) + "Anterior"/"Próximo" + atalho #N. `evaluatedSet` offline-aware alimenta ✓avaliada. `goToIndex` centraliza reset. Guard de unsaved (modal `role=dialog`). Click-outside via `pointerdown` (touch tablet) + Esc. Rolagem com momentum (`-webkit-overflow-scrolling` + `touch-action: pan-y` + `overscroll-contain`). i18n PT/EN/ES. Detalhes em [[terminal-juri-navegacao-manual-shipado]].
- **Frase de áudio/LGPD** (`89dab5d`): bloco "Condições de participação" no Resumo do `InscricaoWizard` (step 3, cobre inscrição + seletiva). Transparência (não consentimento) — bailarino é terceiro, inscrito aceita ao confirmar. Frase única pra todos os eventos. Jurado é coberto por contrato (sem popup). Antes NÃO existia no código (TermoProdutor não tinha, não há regulamento-base).
- **Demo do terminal 3 → 10 apresentações** (`1c88753`): com 3 itens a lista não overflowa e não dava pra testar rolagem. 10 apresentações variadas (estilos/estúdios/formações). Demo bypassa filtro de gênero de propósito (mostra todas, mesmo fora das competências do jurado) — em evento real a lista filtra pelos gêneros do jurado.
- **Navegação manual VALIDADA em demo pelo user** (2026-06-11): rolagem (mouse/dedo), Anterior/Próximo, lista com status, fechar ao clicar fora — todos OK no screenshot do user. **Resta:** validar terminal em **evento real** (#4 — coreografias do cronograma chegando no terminal no dia, depende de evento ao vivo). Backlog [[backlog-politica-audio-juri]] (config de política de áudio) segue aberto.

### 2026-06-10/11 — Terminal júri + Cronograma + Narração IA + Integrações de Marketing ✅ SHIPADO
Sessão grande (~6 commits). Tudo em prod, migrations `20260620`/`20260621` aplicadas, edge `judge-login` redeployada.

- **Terminal de júri** (`bec7b75` + `c2f1688`, migration `20260620`): fix numpad — dígito `1` não trava mais (digitar `1` depois `4` vira `1.4` automático; auto-decimal deixou de excluir o `1`) + bloqueia avanço de quesito com dígito solto (`isCurrentFieldReady`: BASE_10 exige nota completa, Enter/numpad físico era a causa). **Comentário escrito opcional** colapsável abaixo do ⭐ (não mexe no layout) → `evaluations.feedback_text` + `audit_log`. **Download pro inscrito** em `/meus-resultados`: botão "Baixar áudio" (fetch blob + objectURL) + comentário escrito + "Baixar folha de avaliação (PDF)" (jspdf, gated por publicado). **Spellcheck nativo** (`spellCheck`/`lang=pt-BR`/`autoCapitalize`/`autoCorrect`) nos 3 textareas de feedback. Edge `judge-login` persiste `feedback_text` no caminho do jurado por PIN. Detalhes em [[terminal-juri-comentario-numpad-download-shipado]].
- **Cronograma** (`77d0e20` + `819696e`, migration `20260621`): fix picker de eventos (não filtrava `created_by` → vazava eventos de OUTROS produtores, pois `events` é público). **Inscrições pagas aparecem automaticamente** — query filtra por PAGAMENTO (`status_pagamento` APROVADO/CONFIRMADO, setado pelo webhook via service_role; cobre gratuitos) OU `status='APROVADA'`, em vez de só `APROVADA` (que o trigger `protect_registrations_status_columns` impede o produtor de setar). **Remover/reincluir do cronograma** via coluna nova `excluded_from_schedule` (fora do trigger de proteção) — botão X na row + seção colapsável "Removidas (N)" com "Reincluir". Remover ≠ reprovar/estornar. Detalhes em [[cronograma-auto-aparecer-remover-shipado]].
- **Narração IA — voz** (`f3c200c`): `▶` de cada voz agora **também seleciona** (era só tocar sample, com `stopPropagation`) + botão "Testar com {voz}". NÃO era bug de save — SQL confirmou `voice_id='Kore'` salvo certo; era armadilha de UX. Detalhes em [[narracao-ia-voz-selecao-fix-shipado]].
- **Integrações de Marketing** (`a31cbe9` + `4950178`): reorganizado por plataforma (cards Meta/Google, era IDs-em-cima/tokens-embaixo que dava estado órfão) + Avançado colapsável (CAPI) + token travado até o ID existir. **Selo de status honesto** — "● Salvo" só quando bate com o snapshot do banco (`savedMarketing`), não "Conectado" ao digitar (não verificamos conexão real; verificação real só no server-side via webhook). Inclui nota GA4 cross-domain (mesmo Measurement ID nos 2 domínios `festival.usualdance.com` + `app.coreohub.com`, não precisa criar novo ID). Detalhes em [[integracoes-marketing-reorg-shipado]].
- **Backlog aberto:** política de áudio do júri (opcional/obrigatório/sem áudio + consentimento LGPD) em [[backlog-politica-audio-juri]] — NÃO implementar ainda.
- **Próximo passo combinado (#4):** terminal de júri em produção (hoje só aparece modo demo; garantir que coreografias do cronograma cheguem no terminal do jurado no dia do evento). Fluxo do dia do evento documentado em [[cronograma-auto-aparecer-remover-shipado]].
- **Pendência de teste (user):** validar tudo em prod limpando o Service Worker antes (PWA serve bundle antigo).

### 2026-06-07 — Login OTP no webview do Instagram + CPF do pagador no Solo ✅ SHIPADO
2 commits (`cab069d` + `7117d27`) pushados pra main. Só frontend — nenhuma edge function/migration.

- **Login por e-mail (OTP) dentro de in-app browser** (`7117d27`): o OAuth do Google quebra no webview do Instagram/Facebook/TikTok (Google bloqueia WebView, erro 403 `disallowed_useragent`). Solução opção 1 (só no webview): `utils/inAppBrowser.ts` (detector puro por user-agent + 7 testes) detecta o webview e `Auth.tsx` esconde o Google + mostra fluxo OTP (email → `signInWithOtp` com `shouldCreateUser` → código 6 dígitos → `verifyOtp({type:'email'})`). Fora do webview NADA muda. Descobertas-chave: cliente em **flow implícito** → link mágico funciona cross-browser; `auth-email-hook` JÁ manda link+código no mesmo e-mail (não tocou no e-mail). **Pré-req:** "Email OTP" habilitado no Supabase Auth. Opção 2 (passwordless pra todos) = futuro. Detalhes em [[otp-login-webview-instagram-shipado]].
- **CPF do pagador capturado no Solo** (`cab069d`): são 2 CPFs diferentes — bailarino (certificado, `elenco`) vs pagador (`profiles.cpf_cnpj`, fatura Asaas). Pagador é quase sempre o coreógrafo → auto-captura SÓ em Solo (1 bailarino); duo/trio/grupo coleta no pagamento. Modal de pagamento vira pré-preenchido + editável pra confirmar na 1ª cobrança (flag local `coreohub_cpf_confirmado_<id>`). Nunca cobra com CPF errado silenciosamente. Detalhes em [[cpf-captura-pagador-solo-shipado]].
- **Memória nova:** [[terminal-juri-offline-vs-navegador]] — pauta aberta (não implementar): terminal de júri nativo offline vs navegador/PWA.

### 2026-05-30 — A19 Parte A: testes automatizados ✅ SHIPADO
1 commit (`2f5181a`) + edge `create-audience-ticket` redeployada. Primeira rede de segurança automatizada do projeto. Detalhes em [[backlog-testes-automatizados-a19]].

- **Vitest 2.1.9** + **81 testes** (`tests/`, ~340ms) cobrindo lógica pura que toca dinheiro: lotes/preço, máscaras+CPF/CNPJ, status pagamento (regressão APROVADO vs CONFIRMADO), PIX/payout (`detectPixType`/`isKycPendingError`), distribuição comissão/cupom do carrinho.
- **Extração**: matemática de comissão/split saiu de inline na edge pra `_shared/audience-pricing.ts` (`computeAudienceCart`) — testável + não diverge do CheckoutIngresso. Edge importa de lá.
- **CI** `.github/workflows/ci.yml`: lint + test em cada push/PR (free tier).
- **Fix colateral**: `npm run lint` estava QUEBRADO (tsc varria edge Deno → centenas de erros). `tsconfig` agora exclui `supabase/functions`/`api`/`scripts`. Pegou 1 type error real no CheckoutIngresso (`lote.nome`).
- **Decisão**: só Parte A (lógica pura). Parte B (integração Asaas sandbox) deferida — atrito de credencial sandbox + secret não compensa com 1 produtor.

### 2026-05-30 — Multi-jurado seletiva de vídeo v1.1 ✅ SHIPADO
1 commit (`98f0b94`, +701 linhas) + edge `judge-login` redeployada. Backend de agregação já existia (migration `20260604`); faltava só o lado do jurado. Detalhes em [[plano-multi-jurado-seletiva-v1-1]].

- **Página `/jurado-seletiva`** (nova, `JuradoSeletiva.tsx`, standalone via JudgeStandaloneRoute) — fila blind: esconde estúdio/coreógrafo, mostra nº + estilo/categoria/formação + player. Aprovar/Reprovar/Condicional + feedback opcional + score opcional 0-10.
- **Edge `judge-login`** ganhou 2 actions (`get-video-queue` + `submit-video-evaluation`) + flag `video_selection_available` no validate. Reusa verifyJudge + token público + `judges.can_evaluate_video`.
- **`VideoSelection.tsx`** — config `video_evaluators_count`/`video_evaluation_rule` + aviso de número ímpar (evita empate) + painel read-only quando banca ≥2 (jurados decidem, não o produtor).
- **`JudgeLogin.tsx`** — pós-PIN oferece escolha "Seletiva de Vídeo" vs "Terminal de Palco" quando há banca ativa.
- **Decisões de produto**: score opcional; recomenda nº ímpar de jurados; produtor que queira julgar se cadastra como jurado normal (PIN) — reusa todo o fluxo de jurado, zero auth novo.
- **Smoke do trigger `fn_aggregate_video_evaluations` em prod** (BEGIN/ROLLBACK, 4/4 ✅): 2 approve+1 reject→approved; 1 approve+2 reject→rejected; qualquer conditional→conditional; empate 1-1 com voto faltando→submitted. E2E real do fluxo do jurado pendente (sem evento com banca configurado em prod).

### 2026-05-30 — Carrinho de ingressos multi-tipo na vitrine ✅ SHIPADO
2 commits (`0b726c7` feature + `aab5ddd` fix a11y) + migration `20260619_audience_cart_multi_type.sql` aplicada em prod + 2 edge functions redeployadas (`create-audience-ticket`, `send-email`).

Resolve gap vs Sympla/Eventbrite/Ticketmaster (que aceitam misturar tipos num só carrinho). Antes monotipo: comprador escolhia 1 tipo e finalizava; pra 2 Inteira + 1 Meia precisava de 2 compras separadas (2 PIX, 2 emails). Conversão (Baymard: cada checkout extra derruba ~15-25%). Plano fechado 2026-05-28 em [[backlog-carrinho-ingressos-multi-tipo]], implementação em [[carrinho-ingressos-multi-tipo-shipado]].

- **RPC v2** `try_reserve_audience_tickets_v2(p_items JSONB)` — reserva N tipos sob 1 `group_id`, advisory locks de estoque (por evento+tipo) + CPF adquiridos em ordem numérica determinística (impossível deadlock mesmo com carrinhos sobrepostos). Valida Lei 12.933 (máx 1 meia no cart), max_per_cpf (cart inteiro), estoque por tipo. Cupom cart-level incrementa `used_count` por total de tickets. v1 monotipo intacta pra fallback (não-breaking).
- **Edge `create-audience-ticket`** retro-compat: `items[]` (preferido) OU `ticket_type_idx + quantity` (legado). Resolve preço/kind/estoque por tipo, distribui desconto proporcional, 1 cobrança Asaas "2x Inteira, 1x Meia - Evento".
- **Vitrine + checkout** — +/− por tipo + sticky bar; checkout `/checkout-ingresso/<slug>` com resumo editável (decisão de produto: editável, padrão Sympla). Rota legada `/<idx>` ainda funciona.
- **Webhook AT + email** — zero/quase-zero mudança (já agrupavam por `payment_id`).
- **Smoke RPC v2 validado em prod** (3 cenários em BEGIN/ROLLBACK): cart misto → 3 tickets sob 1 group_id; 2 meia → erro Lei 12.933; 8 ingressos limite 6 → erro de limite. E2E ponta-a-ponta com PIX real pendente (precisa evento INTERNO com `audience_sales_enabled=true` — nenhum em prod hoje).
- **Incidente git paralelo**: sessão de `/revisar` simultânea arrastou 3 arquivos do WIP via `git add -A` (commit `77721bf`). Reconciliado sem perda. Lição em [[feedback-git-add-paralelo]] — nunca `git add -A` em projeto com sessões paralelas.

### 2026-05-28 (noite) — Cupom multi-scope + Funil 5 etapas + Drill-down + UTM + Recovery ✅ SHIPADO
1 commit (`b2ff5a7`) + 4 migrations (`20260615`–`20260618`) aplicadas em prod + cron `send-abandoned-reminders` agendado (job_id 33).

Sessão grande (~6h) consolidando 8 blocos tematicamente coesos sobre **conversão e cupom**. Pesquisa de mercado em cada bloco (Stripe/Sympla/Eventbrite/Mixpanel/Bizzabo). Detalhes em [[bundle-conversao-cupom-shipado]].

**Bloco 1 — Super-admin "Signups sem atribuição"**: card no `/super-admin` agrega `profiles WHERE entry_event_id IS NULL`. Total + 30d + breakdown por `entry_source`. Padrão Sympla/Eventbrite: plataforma é dona dos leads não-atribuídos.

**Bloco 2 — Funil 5 etapas (Visitas → Leads → Iniciadas → Pagas → Compareceu)**:
- Migration `20260618` + RPC `increment_event_view_count` (service_role only).
- Edge function `track-event-view` deployada — dedup client via localStorage TTL 30min (cookies cross-origin entre coreohub.com e supabase.co não funcionam).
- `PublicEventPage.tsx` dispara fire-and-forget no useEffect de view_event.
- `ProducerDashboard.tsx` card antigo 3 → 5 etapas. Padrão Stripe Checkout / Eventbrite Insights.

**Bloco 3 — Cupom multi-scope estrutural**: migra `coupons.scope TEXT` enum (6 valores) → `coupons.scopes TEXT[]` array (15 combinações). Backfill: `'both'` → `[inscription, audience]`, `'all'` → 4 surfaces, else `[scope]`. Índice GIN + CHECK constraint cardinality>=1. Coluna legacy `scope` mantida durante transição (~2 semanas pra rolar PWA cache) — edge functions filtram com `OR`. UI Coupons.tsx aceita qualquer combinação 1-4; mensagem "Combinação não suportada" sumiu. RPCs `validate_audience_coupon` + `validate_workshop_coupon` v2 com filtro `ANY(scopes)`. 5 edge functions redeployadas. types.ts: `Coupon.scopes?` array; `scope` deprecated.

**Bloco 4+7 — Drill-down "Quem usou" + Performance R$**: hook `refresh` em [Coupons.tsx](pages/Coupons.tsx) agrega counter+revenue+discount por `coupon_id` em 4 fontes (payments aggregate, registrations, audience_tickets, workshop_registrations). Inline na coluna: `7 usos · R$ 1.260 · -R$ 252`. Click no contador abre Drawer via `createPortal(document.body)` — lição [[licao-createPortal-stacking-context]] aplicada. `Registrations.tsx fetchData` agora hidrata `couponCodeById` (1 query). Side panel mostra código real (`SARAHTINEL · R$ 20,00 de desconto`) — antes era genérico "Cupom".

**Bloco 5 — UTM source no signup** (migration `20260617`): `profiles.entry_source TEXT` + índice partial. `Auth.tsx` captura UTM param ou infere de `document.referrer` (instagram/facebook/whatsapp/google/youtube/tiktok/bing/referral) ou `'direct'`. `persistLeadEntrySource` separado do `persistLeadEntryEventId` — captura mesmo signups direct (sem evento). Padrão GA4 default channel grouping.

**Bloco 6 — Coorte temporal no funil**: seletor "Todo período / 30d / 7d" no header do card. Aplica `gte('created_at')` em todas as queries do funil. Visitas só aparecem em "Todo período" (view_count é acumulado, não tem cohort por design — mostra "—" nas janelas). Padrão Mixpanel/Amplitude.

**Bloco 8 — Email "Você esqueceu de finalizar sua inscrição?"**:
- Template `buildAbandonedRegistration` + case `'abandoned_registration'` em send-email. Subject: `[Evento] Você esqueceu de finalizar sua inscrição?`.
- Edge function `send-abandoned-reminders` deployada (JWT decode + role check). Critério: `status_pagamento=PENDENTE` + `payment_id IS NULL` + `payment_group_id IS NULL` + `created_at` em [24h, 7d] + `user_id NOT NULL`. Agrupa por (user_id, event_id). Idempotência single-shot via notif type `abandoned_registration`. Skip se `prazo_inscricao < today`. Limit 500 por execução.
- Cron `send-abandoned-reminders` agendado job_id 33 (10:30 BRT diário). Vai disparar vazio até houver inscrições abandonadas reais.
- Recovery rate típico Sympla/Hotmart: 15-25%.

**Decisão registrada (não implementada)**: cupom com produtos específicos (Stripe-style por modalidade/categoria/workshop/ticket_type) defer no P2 até produtor pedir granularidade.

### 2026-05-28 — Blindagem do label governo + workshop gratuito ✅ SHIPADO
1 commit (`2af8c21`) + migration `20260613_protect_event_type.sql` aplicada em prod.

**Brecha identificada**: qualquer produtor podia marcar o próprio evento como "Governamental" no [CreateEvent.tsx:291](pages/CreateEvent.tsx#L291), escapando da etiqueta oficial CoreoHub-tem-contrato e poluindo o filtro Governo do super-admin. Inscrição vira grátis (já tinha [create-payment-asaas:58](supabase/functions/create-payment-asaas/index.ts#L58) bloqueando cobrança em governo), mas integridade da classificação fica corrompida. Coluna `event_type` também não estava no trigger `protect_commission_columns`, então dava pra mudar via curl direto.

**Fix em 3 frentes**:
- **CreateEvent.tsx**: remove o card "Governamental" do self-service. Produtor só cria "Privado". Substitui pelo helper card emerald explicando como rodar evento gratuito sem governo: taxa R$ 0 nas formações → Checkout detecta + mostra "GRATUITO" + APROVADO sem Asaas (fluxo já existia em [Checkout.tsx:213-226](pages/Checkout.tsx#L213-L226)).
- **Migration `20260613_protect_event_type.sql`**: estende trigger pra cobrir `event_type`. Padrão idêntico ao das outras colunas financeiras desde [20260429](supabase/migrations/20260429_protect_commission_columns.sql). Super-admin (COREOHUB_ADMIN) e service_role continuam podendo (seed-demo-event precisa setar event_type ao criar demo).
- **SuperAdmin.tsx**: quando super-admin troca `event_type` pra `government` no EventCommissionModal, aplica default `politica_ingressos='GRATUITO'` (boa prática BR: editais públicos quase sempre têm entrada franca). Espelha em events + configuracoes. Best-effort.

**Polimento workshop** (mesmo commit):
- Badge "Gratuito" emerald em [WorkshopsManagement.tsx:443](pages/WorkshopsManagement.tsx#L443) quando preço efetivo (`activeLot.preco ?? preco_padrao`) é 0 E não tem `gratis_para_inscritos`. Antes ficava só "R$ 0,00" ambíguo. Paraleliza com a política "Gratuito" dos ingressos.

**Decisões registradas** (não implementadas — viraram backlog):
- **"Vendas de Ingressos" mantém o nome**: colisão com `/ingressos` da página de comprador (MyTickets em [Ingressos.tsx](pages/Ingressos.tsx)). Padrão Sympla/Eventbrite: produtor vê "vendas", comprador vê "meus ingressos". Renomear quebraria a semântica.
- **Tipos de ingresso continuam na aba GERAL**: sem o rename, migrar criação pra página de Vendas perderia coerência semântica (página fica focada em sales).
- **Gap "produtor 100% grátis"** [[backlog-modelo-comercial-evento-gratis]]: produtor pode rodar evento Privado com taxa 0 + workshops grátis + ingressos grátis usando toda infra (IA Gemini, narração ElevenLabs, Júri Terminal, certificados) sem gerar receita. 4 modelos de mercado mapeados — recomendação Setup fee por evento grátis (~R$ 297-997, padrão Bizzabo/Cvent, ~3h implementação). Decisão comercial pendente.
- **Painel de contrato governo**: user vai pensar se modela dentro da CoreoHub ou usa ERP separado. Plano técnico (tabela `government_contracts` + super admin section) escrito em `~/.claude/plans/modular-forging-scroll.md` mas NÃO implementado.

### 2026-05-27 — Prêmios com valor R$ + DEMO/Regulamento IA atualizados ✅ SHIPADO
3 commits (`ae0e4f4` → `0752eda`).

**Premiação valor R$ + template Coreógrafo(a) (`ae0e4f4`, `74e3be3`, `d2d0284`)**:
- Campo `valor?: number` em `SpecialAward` ([AccountSettings.tsx:236](pages/AccountSettings.tsx#L236), JudgeTerminal TS-only, vitrine pública em emerald quando >0).
- Descrições padrão dos templates **limpadas** (poluíam — só nome + descrição opcional + valor).
- Novo template **"Melhor Coreógrafo(a)"** (id `tpl_coreografo_pessoa`) — premia a pessoa, separado de "Melhor Coreografia" (a obra). ID do template original `tpl_coreografo` preservado pra não quebrar premios já salvos (merge por id).
- Templates **NÃO editáveis no nome** (decisão de produto: template = sugestão consistente de mercado, custom = liberdade). Descrição/valor editáveis dão flexibilidade.
- 6 `aria-label` adicionados nos inputs de prêmio.
- **Incidente**: durante refactor removi declaração do state `editingAwardId` por engano → tab Prêmios quebrou com ReferenceError em prod (~10min). Fix em `74e3be3`. Lição [[feedback-smoke-tabs]]: smoke do Playwright só carrega URL default, NÃO navega tabs — mudança em tab específica precisa validação manual da tab.

**DEMO + Regulamento IA cobrindo features novas (`0752eda`)**:
- **seed-demo-event**: 6 prêmios (com Coreógrafo(a)) + 3 com valor R$; bailarinos com **CPF válido (módulo 11) + data_nascimento** inseridos em `elenco` real (IDs em bailarinos_detalhes) — elimina banner falso "dados pendentes" no Dashboard do demo; cleanup deleta só elenco do demo (preserva real); cupons com `max_uses_per_user`.
- **gemini-analysis**: schema ganha `prizes[].valor` (instrução forte anti-alucinação "só se PDF citar explícito"), `formacoes[].min/max_performers`, `bonifications[]` (bolsas/intercâmbios textuais).
- **RegulationAIParser + geminiService**: propaga `valor` no merge (preserva existentes via dedup por nome).
- 2 edge functions redeployadas. Build OK.
- **NF emission**: decisão de NÃO implementar agora (produtor MEI Hemer não emite NF automática; demanda vira WhatsApp manual).

### 2026-05-22 (noite — maratona cupom UX + idempotência + cleanup) ✅ SHIPADO
16+ commits (`e66ca37` → `b6c8031`). Sessão mais longa registrada. Cobriu refator dados-pendentes (top1 da sessão anterior), cupom no fluxo agregado/single/seletiva ponta-a-ponta, UX padrão Stripe/Sympla, idempotência via webhook, +3 pendências separadas resolvidas no fim. Detalhes em [[refactor-elenco-dados-pendentes-shipado]] + [[cupom-aggregate-shipado]].

**Refator dados-pendentes (6 commits `e66ca37` → `f57a9cb`)**: helper `hasIncompleteBailarinos` antes lia `bailarinos_detalhes` (JSONB com só id+nome+@) procurando CPF/data_nascimento que ficam na tabela `elenco`. Refator: migrations RLS pro produtor ler `elenco` filtrado por subquery LATERAL nos bailarinos_detalhes das inscrições do evento dele (não vaza elenco entre produtores). 3 frontends + edge function `send-incomplete-data-reminders` agora consultam `elencoById` hidratado em batch. Editor inline em Registrations + MinhasCoreografias faz UPSERT em elenco (não no JSONB). Falso positivo "10 com dados incompletos" eliminado em prod.

**checkViolation (`d5f8488`)**: mesmo bug do dados-pendentes — triagem etária lia `b.data_nascimento` do JSONB (sempre undefined) → outOfRange sempre vazio → aba TRIAGEM nunca funcionou. Fix: lê via elencoById (deps atualizadas).

**Cupom no fluxo agregado (10 commits `b5c2be4` → `b6c8031`)**: o refator do carrinho de 2026-05-19 deixou cupom órfão no `Checkout.tsx` — Wizard navega direto pra `/minhas-coreografias` sem passar por lá. Implementado fim-a-fim:
- Edge `create-aggregate-payment-asaas` aceita `coupon_code`, distribui desconto linearmente nos baseFee, persiste `coupon_id` em payments+registrations
- Edge `create-payment-asaas` (single) aceita `coupon_code` além de `coupon_id`
- Edge `cancel-aggregate-payment` nova — cancela fatura PENDENTE no Asaas (DELETE /payments/{id}) + zera local. Diferencia 4xx/5xx (response `partial_cancel` quando 5xx)
- Webhook (branches AGG, single, video_selection) incrementa `used_count` idempotente via markers `payments.coupon_redeemed_at` + `registrations.coupon_redeemed_at`. Retry de webhook vira noop. Cancelar antes de pagar não infla used_count.
- UI padrão Stripe/Sympla: "Tem cupom?" sempre visível → botão Aplicar (validateCoupon client) → linha verde com X "Remover cupom" unificado pros 2 estados (applied client + fatura PENDENTE com cupom). Click X dispara cancel silencioso se há fatura. UX padrão Sympla — vocabulário do user, não interno.
- `coupons.max_uses_per_user` (limite por inscrito) — boa prática Sympla/MP. Validado client+server (dupla checagem).
- Edge function aggregate detecta "ghost pointer" (registration.payment_group_id apontando pra payment morto): zera silenciosamente e segue. Resolve race condition do user que clica PAGAR TUDO → PAGAR SÓ ESTA → PAGAR TUDO em sequência.
- Spinner travado pós-bfcache fixado (listener `pageshow`).

**3 pendências separadas resolvidas (`b6c8031`)**:
1. Idempotência seletiva — `create-video-selection-payment` + webhook branch VS espelha o que foi feito no aggregate/single.
2. DROP policies fantasmas em `registrations` — "Acesso Total Registrations" + "Permitir Atualização" (ambas com `true/true`, criadas via Dashboard manual, esquecidas). Risco de segurança alto (qualquer authenticated UPDATE qualquer registration).
3. Crons 401 — `app.settings.service_role_key` NULL em prod fazia 5 crons retornarem 401. Reagendados via `cron.schedule` com chave hardcoded em texto (visível só pra superuser). 6 crons HARDCODED OK. Pendência: se rotacionar service_role, rodar `DO $$` block de novo (anotado em [[feedback-cron-app-settings-null]]).

**Migrations aplicadas em prod (7)**: 20260601 (RLS produtor lê elenco) → 20260602 (RLS produtor UPDATE elenco) → 20260603 (payments.coupon_id) → 20260604 (payments.coupon_redeemed_at) → 20260605 (coupons.max_uses_per_user) → 20260606 (registrations.coupon_redeemed_at) → 20260607 (DROP policies fantasmas).

**Lições dolorosas anotadas**: vocabulário do user > implementação ("Cancelar fatura" → "Remover cupom"), pesquisar boas práticas ANTES de propor UI, sempre unificar UI pelo gesto não pela complexidade interna, bfcache preserva React state (listener `pageshow` resolve), SQL diagnóstico antes de hipótese.

### 2026-05-22 (madrugada — virou de quinta) — 🚨 Sessão A DESABILITADA em prod por bug grosso

Commit `4602573`. Após shipping da Sessão A (~6h antes), Grazieli avisou que cadastrou os 40 CPFs sim. Auditoria via SQL confirmou: **61 bailarinos em `elenco`, 61 com CPF, 61 com data_nascimento**. Tudo completo. Helper meu lia o lugar errado.

**Bug**: helper `hasIncompleteBailarinos` em 3 lugares (`Registrations.tsx`, `Dashboard.tsx`, edge function) procurava `cpf`/`data_nascimento`/`nome` em `registrations.bailarinos_detalhes` — JSONB que SÓ tem `{id, nome, instagram_handle}` (referência leve). CPF/data ficam na tabela **`elenco` separada**, código `pages/InscricaoWizard.tsx:826-835` mostra isso explicitamente. Eu deveria ter aberto o INSERT antes de codar o helper.

**Impactos em prod** (antes do disable):
1. Cron rodou 09:30 BRT e inseriu notif falsa pra TODOS os inscritos pagos
2. Banner amber no `/dashboard` aparecia pra todos
3. Chip "X com dados incompletos" no painel `/registrations` sempre não-zero
4. Badge "DADOS PENDENTES" + fundo amber por bailarino — idem
5. User passou vergonha com cliente real

**Fix temporário (`4602573`)**:
- Edge function retorna 200 com `sent=0` cedo (no-op)
- `hasIncompleteBailarinos` retorna `false` constante
- Banner amber removido do Dashboard
- Notif falsas deletadas via SQL no Dashboard

**Mantido (não dão dano sozinhos)**: editor inline de bailarinos em `/minhas-coreografias`, componente `BailarinosEditor.tsx`, conta `inscrito@coreohub.com`, storage Playwright inscrito.

**Top 1 próxima sessão**: refator com `elenco` JOIN (`plano_refactor_elenco_dados_pendentes.md`). Migration RLS elenco pro produtor + refator helper x3 + hidratar elencoById + refator editor pra UPSERT em elenco + re-habilitar edge function. ~5h.

**Lição capturada**: antes de escrever helper que LÊ um campo, abrir o INSERT que ESCREVE nesse campo + confirmar shape exato. TS type permite shape vago `any[]`, mas conteúdo real é definido no INSERT. Reforço em [[feedback-consultar-memoria-antes-implementar]].

### 2026-05-22 (tarde/noite) — Sessão A (notificação Grazieli) ⚠️ SHIPADO mas DESABILITADO mesma noite
6 commits (`b6e4444` → `0c9af1a`). Objetivo: garantir que quando a Grazieli abrir o app (via notificação in-app + WhatsApp manual), ela tem pra onde ir e como completar os 40 CPFs faltando em 10 inscrições do Usualdance. Sem A1+A2+A3, WhatsApp dela cairia em beco sem saída.

**A2 — Edge function `send-incomplete-data-reminders`** (`b6e4444`): cron diário 09:30 BRT que varre registrations APROVADAS/CONFIRMADAS com bailarinos incompletos (CPF/nome/data_nascimento vazios) e insere notif in-app pro user. Idempotência por dia via `metadata->>'reminder_type' = 'dados_pendentes_YYYY-MM-DD'`. Sem email — WhatsApp manual é o canal de pressão. Texto da notif diferenciado pra zumbi (sem array de bailarinos) vs incompleto parcial. Cron agendado job_id 13.

**A1 — Editor inline de bailarinos em `/minhas-coreografias`** (`ef818ae`): inscrito edita CPF/nome/data/Instagram dos próprios bailarinos sem precisar pedir pro produtor. Botão "Editar dados" em cada card (PENDENTE ou APROVADO/CONFIRMADO). Modal centralizado com novo componente compartilhado `components/BailarinosEditor.tsx` (refactor — `pages/Registrations.tsx` também passou a usar, removendo ~120 linhas de Tailwind inline). Decisão 1B locked: edita perene quando `prazo_inscricao` está vazio; trava com mensagem clara após expirar. Validação CPF + data ISO + Instagram lowercase. RLS `inscrito_own_registrations` cobre. `.select('id')` no `.update()` detecta bloqueio silencioso (mesmo hardening do painel do produtor).

**A3 — Banner "dados pendentes" no `/dashboard`** (`4846055`): card amber sticky no topo quando inscrito tem ≥1 coreografia paga com bailarinos incompletos. CTA "Completar →" navega pra `/minhas-coreografias`. Helper `hasIncompleteBailarinos` replicado inline (mesma lógica do painel + edge function).

**A4 — Storage state Playwright pra inscrito**: `scripts/playwright.auth.mjs` + `scripts/screenshot.mjs` ganham `--as=inscrito` no `validRoles`. `.env.local.example` agora documenta 3 contas (admin/produtor/inscrito). Conta `inscrito@coreohub.com` criada via Dashboard > Add User (signup endpoint do app retornou 500 — bug separado anotado em pendência operacional).

**Fix crítico JWT decode anti-pattern** (`0c9af1a`): smoke E2E descobriu que `send-incomplete-data-reminders` + `send-trilha-reminders` usavam `authHeader === \`Bearer \${env.SUPABASE_SERVICE_ROLE_KEY}\`` — exatamente o anti-pattern documentado em [[feedback-jwt-role-check]] desde 2026-05-20. Causa: pg_cron envia JWT válido mas que pode ser de geração diferente do env (caso de rotação histórica). String não bate, retorna 401. Fix: decodifica payload do JWT + valida `role === 'service_role'` (Supabase platform já valida assinatura via verify_jwt=true antes da function rodar). Pattern já aplicado em `daily-release-funds/index.ts:63` desde 2026-05-20 — só não tinha sido replicado nas 2 funções novas. Ambas re-deployadas via CLI.

**Conta `inscrito@coreohub.com` criada via Dashboard Add User** porque o signup endpoint do app está retornando 500 em prod. Bug crítico em produção (qualquer inscrito novo não consegue se cadastrar via app). Pendente investigar em `Authentication → Logs` ou Auth Hooks. Workaround: produtor cria conta manualmente pelo Dashboard.

**Smoke E2E validado end-to-end**:
- Como inscrito → `/dashboard` mostra banner amber "1 coreografia com dados pendentes" + Guia de Inscrição + cards do evento
- `/minhas-coreografias` mostra card da inscrição mock com botão "EDITAR DADOS" + chips COMPETITIVA/JUNIOR/JAZZ + status CONFIRMADA
- Cron invocado via `net.http_post` → status 200, `total_scanned: 11, total_pending: 11, sent: 11`
- Sininho com badge "1" aparece no header em tempo real (realtime postgres_changes)

**Decisões da sessão**:
- Grazieli vai receber **1 notif consolidada** ("Você tem 10 coreografias com bailarinos sem CPF") em vez de 10 spammando o sino. Inserida manualmente via SQL (delete das 10 que o cron tinha inserido + insert da agregada). Em sessão futura vale agregar por user no próprio cron.
- 10 outras contas no Auth são leads (logaram via Google/Email mas zero inscrições). Vale virar feature "Funil de leads" no painel produtor (P3 backlog).

**Achados de auditoria**:
- 11 users em `auth.users`, apenas Grazieli (10) e `inscrito@coreohub.com` (1 mock — agora deletado) tinham inscrição no Usualdance. Resto é signup sem follow-through. Não é bug, é funil de conversão.
- Erro de console **400** em `/minhas-coreografias` quando user sem inscrição abre a página. Cosmético — UI renderiza empty state OK. Suspeita: query Supabase com IN ([]) vazio.
- **5 crons agendados em prod** (`send-payment-reminders`, `expire-pending-payments`, `cleanup-orphan-tracks`, `send-trilha-reminders`, `send-incomplete-data-reminders`). 2 deles (`send-trilha-reminders` e a nova) tinham o anti-pattern JWT — agora corrigido. Os 3 antigos provavelmente já estavam OK (memória cita que `daily-release-funds` usa o pattern correto desde 2026-05-20).

### 2026-05-22 (madrugada — virou de domingo) — Bundle Sessão 4 + Opção B notificações ✅ SHIPADO
Madrugada 21→22, 13 commits + 2 migrations + 3 edge function deploys. Maior sessão registrada nessa janela. Estrutura em 4 grandes blocos:

**Bloco 1 — Bundle Sessão 4.1 painel /registrations** (`415877b` + audit `5140051`):
- **P5 comparativo edição anterior** — banner "vs edição 2025" com delta % de receita + inscrições quando produtor tem evento com `edition_year < atual` no dropdown. Mesmo princípio do `ResultsPanel.tsx:259`.
- **P8 banner alertas inteligentes** — 3 chips no topo (vencendo 24h / trilhas pendentes / fora da faixa). Click filtra a lista direto ou pula pra aba TRIAGEM. Heurística "vencendo 24h" usa `created_at` entre 48-72h (default dueDate Asaas = +3d).
- **Sort header** — click em Coreografia/Data/Valor/Pagamento alterna asc/desc. Default data desc.
- **Paginação 50 rows** — só renderiza quando passa de 50; reset auto pra página 1 quando filtros/sort mudam.
- **Audit fixes pós-shipping**: "Limpar filtros" não zerava `quickAlert`/`tipoApresentacaoFilter`/`dateFilter`; CSV exportava em ordem de banco ignorando sort visível.

**Bloco 2 — UX review sequencial baseado em screenshots do user** (commits `1f9b9e5` → `b2f9649`, 5 commits):
- **Item 1+4 (`1f9b9e5`)**: "Abrir no Asaas" → "Abrir fatura" no modal. Chip persistente "Filtrando: X (N resultados) [×]" quando quickAlert ativo — fix do bug visual em que todas as 10 inscrições do user matchavam o filtro de trilhas pendentes e nada parecia mudar.
- **Item 3 (`f59d5f0`)**: alerta "X com dados incompletos" no banner P8 + badge "DADOS PENDENTES" no header da seção Bailarinos + fundo amber em cards individuais. Helper `hasIncompleteBailarinos` detecta 3 cenários (array ausente / array vazio com modalidade declarada / qualquer bailarino sem CPF/nome/nasc).
- **Item 2 v1 (`30bb8da`)**: editor inline de bailarinos no modal — nome/CPF (`maskCpfCnpj`)/data/instagram. Botão "+ Adicionar bailarino" + "Remover" por linha.
- **Item 6 (`f2abaa8`)**: drawer único de filtros estilo Sympla/Linear. Search + botão "Filtros (N)" no canto + chips rosa dos filtros ativos abaixo (com X individual + "Limpar tudo"). Modal centralizado com 7 selects empilhados.
- **Item 2 v2 (`b2f9649`)**: revisão do editor de bailarino seguindo regra do Wizard público — Instagram per-bailarino só em Solo/Duo/Trio (`maxMembers <= 3`), 1 campo único `instagram_principal` no rodapé pra Grupo/Conjunto. Data nasc trocada de `<input type="date">` (calendário lento pra ano antigo) por input mascarado DD/MM/AAAA (`maskData` + `parseDataISO`). Save normaliza handles obsoletos quando muda modalidade.

**Bloco 3 — Bug crítico RLS produtor UPDATE em registrations** (`ded2e3e` + migration `20260525`):
Auditoria descobriu que a edit inline (modalidade/categoria/estilo/mostra desde sessão Grazieli + editor de bailarinos agora) **NUNCA funcionou pra produtor não-super-admin desde 2026-04-28**. Só existia policy `producer_reads_event_registrations` FOR SELECT + bypass de super_admin. PostgREST retornava 0 rows sem erro, modal fechava, React atualizava state, banco continuava intacto. F5 perdia tudo.
- Migration `20260525_producer_updates_registrations.sql` aplicada em prod via Dashboard: nova policy FOR UPDATE com USING + WITH CHECK = produtor é `created_by` do evento; trigger `protect_registrations_status_columns` estendido pra também reverter `user_id`, `event_id`, `created_at` (impede produtor "roubar" inscrição mudando dono ou movendo entre eventos).
- Hardening em `handleSaveEdit`: `.select('id')` no `.update()` força PostgREST a devolver as rows afetadas. Se RLS bloqueia, throw com mensagem clara em vez de bug silencioso.
- **Validado em prod**: user logado como **produtor real do Usualdance** (não super_admin) editou CPF de bailarino DESERT FIRE → persistiu após F5 ✅.
- **Benefício colateral importante**: outras 2 telas que faziam UPDATE em registrations também estavam silently broken: [pages/CheckIn.tsx:125](pages/CheckIn.tsx#L125) (check-in QR não persistia) e [pages/ResultsPanel.tsx:207-218](pages/ResultsPanel.tsx#L207-L218) (publicar resultados não publicava). Ambas agora funcionam pra produtor real. Validar em smoke quando rolar evento presencial.

**Bloco 4 — Opção B notificações multi-canal** (commits `85310ea`, `681d275`, `0437548`):
- **B5 PWA install tracking** (`85310ea`): plugou `trackEvent` no `usePWAInstall` hook. 5 eventos custom em GA4 + Meta Pixel paralelo via `fbq('trackCustom', ...)`: `pwa_install_available`, `pwa_install_prompt_shown`, `pwa_install_accepted/dismissed`, `pwa_installed`. Cada um carrega `display_mode` (browser/standalone). Try/catch defensivo — pixel bloqueado nunca quebra a feature.
- **B1 schema in-app inbox** (`681d275`): migration `20260526_notifications_inbox.sql` aplicada em prod. Tabela `notifications` (12 cols: id/user_id/event_id/type/severity/title/body/cta_url/cta_label/metadata JSONB/read_at/created_at). RLS: user lê e atualiza próprias (trigger `protect_notifications_columns` impede mexer em tudo exceto `read_at`); super_admin bypass; INSERT/DELETE deliberadamente sem policy = só service_role e super_admin podem criar. 2 indices (partial em unread + full em created_at desc). RPC `mark_all_notifications_read()` SECURITY DEFINER pra bulk update.
- **B2 NotificationBell** (`0437548`): componente novo `components/NotificationBell.tsx` plugado no Header entre toggle theme e user menu. Bell + badge rosa "1"→"9+" com contagem de não lidas. Dropdown popover (max 50 itens) com card por notif (ícone colorido por severity, título bold quando não lida, body line-clamp-2, tempo relativo "agora/há 5 min/há 2h/há 3d", CTA inline rosa). Realtime via `postgres_changes` filter `user_id=eq.${userId}` — inserts/updates aparecem sem refetch. Click marca lido optimistic + navega (URL relativa via React Router, absoluta abre nova aba). Botão "Tudo lido" usa o RPC. Empty state amigável.
- **B3 send-trilha-reminders** (`0437548`): nova edge function deployed (`supabase functions deploy ...` rodado direto). Lê `configuracoes.prazo_trilhas` (TEXT YYYY-MM-DD) de todos eventos. Classifica em 2 janelas catch-up: '15d' (13-16d) e '5d' (3-6d). Pra cada evento na janela, pega registrations APROVADO sem trilha_url + user_id NOT NULL. Idempotência via tabela `notifications`: query filter por `metadata->>'registration_id'` + `metadata->>'reminder_type'`. Sem marker em registrations precisar de migration extra. Insere notif in-app primeiro + send-email best-effort (template novo `trilha_reminder` em send-email com 2 tons — last chance quando ≤5d). Hoje em America/Sao_Paulo via `Intl.DateTimeFormat({ timeZone: 'America/Sao_Paulo' })` pra evitar off-by-one do Deno UTC.
- **Cron diário 09:00 BRT** agendado via `cron.schedule` (job_id 10). Mesmo padrão dos crons `send-payment-reminders`, `expire-pending-payments`, `cleanup-orphan-tracks` — usa `current_setting('app.settings.service_role_key', true)`.

**Smoke validado**: INSERT manual de notif `severity=warning` → badge "1" apareceu no sino do user logado **sem F5** (realtime), dropdown abriu com card amarelo, click marcou lido (bolinha some) + navegou pra `/registrations`. End-to-end OK.

**Decisões locked pra próxima sessão**:
- Email "trilha faltando" usa `configuracoes.prazo_trilhas` (não data do evento). ✅ Já implementado em B3.
- In-app inbox aprovado. ✅ B1+B2.
- PWA install tracking conecta nos analytics existentes (GA4 master + Meta Pixel master) — sem novo provider. ✅ B5.
- **B4 Push Web defer** — ~4h de esforço pra cobertura 15-30% (taxa típica de aceitar permissão). Faz sentido só depois do inbox virar baseline + algum produtor demandar.

**Achados de auditoria pendentes** (não bloqueiam, anotados):
- `audience_tickets`, `workshop_registrations`, `video_selections`, `aggregate_payments`, `coupons` — mesmo padrão "produtor só SELECT" pode estar silently broken. Auditoria sistemática ~2h numa sessão futura.
- Performance: `send-trilha-reminders` faz 1 query de count por registration (otimizar pra bulk `IN`). `notifications.metadata` sem GIN index (criar quando crescer).
- Cosméticos: `formatRelative` no Bell não atualiza com tempo passando (notif fica "agora" indefinidamente); limit 50 sem paginação no dropdown.

### 2026-05-21 (tarde) — Bundle P1-4 (residuais destravados) ✅ SHIPADO
Commit `9463e66`. 4 itens da fila "destravados pra próxima sessão" — autorizados em bloco pelo user:
- **Checkout.tsx** — UPDATE direto pra `'APROVADO'` (remove duplo CONFIRMADO→APROVADO legacy do cupom 100%). 1 query a menos por checkout gratuito.
- **Schedule drag fade** — `animate-pulse` → keyframe `dropIn` (fade 0→1 + translateY -4px→0 em 240ms) declarado em `index.css`. Mascara o "pop" visual do dnd-kit quando linha cruza SortableContext. Solução A (CSS-only) — não precisou refatorar pra B (delay setState) nem C (mover update pro onDragOver).
- **Wizard rascunho localStorage** — `coreohub:wizard:<eventId>`, TTL 24h, debounce 300ms. Helpers `readWizardDraft/writeWizardDraft/clearWizardDraft` em `pages/InscricaoWizard.tsx`. Hidrata após `setEvent(ev)` (sobrescreve defaults de `coreografo_nome`/`bailarinos`). Limpa logo após registration commitada — cobre 4 caminhos de saída (demo bypass / Asaas redirect / minha-seletiva / minhas-coreografias).
- **Email "Repasse liberado"** — novo template `payout_released` em `send-email/index.ts` (cifrão 36px verde + PIX mascarado `•••• XXXX` + count de comissões + CTA pro /qg-organizador). Disparado em `daily-release-funds` após cada `result.swept`, best-effort (falha não reverte). Idempotência natural via `released_at` IS NULL filtro da query — cron rodando 2x no mesmo dia não duplica email.

Edge functions deployadas via CLI: `send-email` + `daily-release-funds`.

**Smoke E2E executado 2026-05-21 (noite):** 2 PIX taxa de seletiva R$ 33 cada pagos pela conta Cultural Estúdio → 2 platform_commissions criadas (R$ 30 net cada) → forçado `release_at = NOW() - 1h` na TESTE 2 → invocado cron via curl → sweep transferiu R$ 30 pro PIX do Hemer + chamou send-email (`released_at` + `release_transfer_id` preenchidos) ✅. Refund via painel também coberto: TESTE 1 reprovada em /seletiva-video → `process-video-refund` aplicou `partial_refund 50%` = R$ 16,50 (policy default do evento Usualdance). Email `payout_released` foi enviado pra `festival@usualdance.com` (email do profile do Hemer) — não conferido visualmente porque user não tem acesso ao webmail desse domínio. Funcionalmente OK.

**Bug pré-existente descoberto + corrigido**: faltava policy RLS `producer_reads_own_commissions` em `platform_commissions`. ProducerBalanceCard ficava com R$ 0 pra qualquer produtor não-super-admin (só policy de super_admin existia desde 20260428). Policy aplicada em prod via SQL Editor (versionar a migration ainda pendente — user pediu pra deixar pra depois).

**Cuidado operacional 2026-05-21:** durante o smoke, a `SUPABASE_SERVICE_ROLE_KEY` vazou no transcript por erro do bash (`source .env` quando faltava `=` no arquivo, linha virou comando, chave foi printada no stderr). User vai rotacionar quando puder. Lição em [[feedback-secret-vazou-source-env]] — usar `awk -F=` no lugar de `source` pra extrair secret de .env.

### 2026-05-21 (noite) — Fixes pós-smoke + saldo Asaas em tempo real
Continuação do smoke. 5 commits:

- **`51c3046` fix(refund) — reflete estorno em platform_commissions + saldo bate com Asaas** — bug descoberto no smoke (TESTE 1 estornada R$ 16,50 mas `platform_commissions` ficava intacta, ProducerBalanceCard mostrava R$ 30 fantasma). Fix em 3 arquivos: `asaas-webhook` ganha branch genérico após `statusInterno` mapping que detecta `PAYMENT_REFUNDED`/`PAYMENT_PARTIALLY_REFUNDED`/`ESTORNADO`, busca total via `GET /payments/{id}/refunds` e marca `refund_amount` + `refunded_at` (refunded_at só em refund 100% — parcial deixa NULL pra comissão seguir válida pela diferença). `daily-release-funds` filtra `refunded_at IS NULL` e subtrai `refund_amount` do `net_amount` sweepável. `ProducerBalanceCard` espelha a mesma lógica. Bonus: ícone gigante cinza claro de fundo dos MetricCards do ProducerDashboard removido. Detalhes em [[bug-refund-nao-atualiza-platform-commissions]].

- **`67f2853` chore(migrations) — versiona policy `producer_reads_own_commissions`** — RLS aplicada em prod durante o smoke, agora versionada em `supabase/migrations/20260524_producer_reads_own_commissions.sql` pra não perder em `db reset`.

- **`b242ded` feat(saldo) — botão 'Conferir saldo Asaas'** — nova edge function `get-producer-asaas-balance` (JWT do produtor, lê `asaas_api_key` do profile, chama `GET /finance/balance` da Asaas). UI no card mostra divergência em âmbar quando |asaas − coreohub| > 0,01. Padrão Stripe Connect/Mercado Pago: carteira local + reconciliação sob demanda.

- **`0f147c8` refactor(saldo) — unifica refresh + Asaas em 1 botão + fix manual-transfer-now** — produtor achou redundante ter 2 botões. Refresh manual agora consulta saldo Asaas em paralelo (mount inicial + realtime continuam só local pra não martelar API). E corrigi um bug encontrado na auditoria pós-fix do refund: `manual-transfer-now` tinha o mesmo problema que `daily-release-funds` tinha antes (não filtrava `refunded_at` nem descontava `refund_amount`) — botão "Transferir agora" tentaria sacar comissão já estornada e Asaas rejeitaria.

- **`ff1b6d2` fix(auth) — produtor cai no /qg-organizador após login** — antes todo user ia pro `/dashboard` (tela do inscrito). Produtor via tela vazia "Sem inscrições" antes de achar o painel no sidebar. Auth.tsx agora lê `profiles.role` pós-SIGNED_IN: `ORGANIZER` → `/qg-organizador`, demais → `/dashboard`. Lookup fora do callback de `onAuthStateChange` (dentro de `setTimeout`) pra não deadlockar o lock interno do auth-js (issue supabase/auth-js#762).

**Validação retroativa:** corrigi a comissão da TESTE 1 (`de563afd-...`) com `PATCH /rest/v1/platform_commissions` setando `refund_amount=16.50`. Card desceu de R$ 30 → R$ 13,50 = bate com a realidade (net 30 − refund 16,50). Asaas mostra subconta R$ 0 (já foi sacado o R$ 30 da TESTE 2 + Asaas reteve o R$ 13,50 da TESTE 1 em pasta "a liberar" que não aparece na view "Saldo" padrão).

**Auditoria pós-fixes:** revisei os 5 commits. Achei o bug do `manual-transfer-now` (já fixado em `0f147c8`). Resto OK. Único finding cosmético sobrando: `pages/ProducerDashboard.tsx:197` faz query de `platform_commissions` sem filtrar refunds — usado pra gráficos históricos, não bloqueia faturamento total (que vem de `registrations.valor_pago`). Anotado pra polir depois.

### 2026-05-21 — Bundle P1 pós-auditoria + bugs P0 reportados
Detalhes em [[bundle-p1-pos-auditoria-2026-05-21]]. 7 commits, todos em prod:

- **Schedule cronograma — 3 bugs críticos**
  - Drag entre blocos persiste imediato no DB (handleAssignBloco + handleDragEnd). Toast verde "Movida pra Bloco X" + highlight pulsante 2s. Sem isso produtor movia, fechava sem clicar "Salvar Ordem", perdia.
  - Filtro `is_demo` no picker do Schedule: em demo só mostra demos, em real só reais. Não mistura.
  - Campo de busca no header destaca rows com ring âmbar (não filtra, só sinaliza).
- **EventPickerSheet — badge DEMO + fix mobile**
  - Badge âmbar "DEMO" no trigger + bottom-sheet mobile + dropdown desktop quando `ev.is_demo`.
  - Fix BottomNav cobrindo bottom-sheet: `z-[60]` + `bottom-16` + backdrop `z-[55]`. Lista inteira agora visível acima do nav.
- **ProducerBalanceCard — refresh + realtime**
  - Botão refresh manual no header do card.
  - Realtime subscription em `platform_commissions` (filter `producer_id`). Cron `daily-release-funds` libera silenciosamente — card recarrega sozinho via `postgres_changes`.
- **AccountSettings KYC — botão "Verificar agora"**
  - Edge function `check-producer-kyc` estendida pra PERSISTIR `asaas_kyc_status` + `asaas_onboarding_url` no profile (antes só lia).
  - `refreshKycStatus` re-lê o profile após invoke. Banner amarelo some sozinho quando Asaas aprovar — sem produtor recarregar a tela inteira.
- **Header switch VISÃO — redirect**
  - Trocar role no dropdown redireciona pra `/`. Antes super admin trocando pra "Inscrito" ficava em rota órfã (/registrations é só produtor → tela vazia).
- **Cupom no checkout da Seletiva**
  - `create-video-selection-payment` aceita `coupon_code` (texto) além do `coupon_id` (UUID). Lookup ILIKE em coupons.
  - MinhasCoreografias ganha UI inline "Tem cupom?" abaixo de cada row de seletiva com taxa pendente.
  - Workshop já tinha cupom completo (CheckoutWorkshop.tsx verificado).
- **Validação playlist YouTube**
  - URL de vídeo seletiva rejeita playlists. Regex `[?&]list=` ou `youtube.com/playlist`. Padrão de mercado (Joinville, Catanduva, SESI): jurado precisa de 1 vídeo único.
  - Aplicado em 2 pontos: validateStep do Wizard + handleSaveVideoLink em /minhas-coreografias.
- **Demo bypass Asaas — corrigido em 2 commits**
  - Wizard pula Asaas e marca `video_fee_status='waived'` se `event.is_demo === true`. Modo demo agora é gratuito end-to-end.
  - Pegadinha: `event.is_demo` era `undefined` porque o SELECT na linha 421 do InscricaoWizard não incluía `is_demo`. Adicionado.
- **Instagram input lowercase**
  - 2 inputs do Wizard (bailarino solo/duo/trio + grupo/coreógrafo) forçam `.toLowerCase()` no onChange. Evita duplicatas (@USUARIO vs @usuario tratados como bailarinos diferentes).
- **Z-index sweep preemptivo**
  - 3 modais bumpados de `z-50` → `z-[60]` antes de aparecer bug: Schedule.tsx:1657 (bloco picker), VendasIngressos.tsx:521 (drawer comprador), WorkshopsManagement.tsx (3 modais).
- **EventPickerSheet em mais telas**: Coupons, Credenciais, VideoSelection (`<select>` nativo Android era horrível).

### 2026-05-20 (madrugada) — Bundle P1 (7 polimentos)
Commit `5077112`. Sequência rápida de melhorias pós-D+7:
- **#40 Exceções de validação agrupadas** — modal de subgênero em AccountSettings ganha header explícito "Exceções de validação (opcional)" agrupando Categoria Livre + Trilha de Repertório.
- **Sweep `status_pagamento`** — novo `utils/registrationStatus.ts` com helper `isRegistrationPaid()`. Aplicado em `CheckIn.tsx` (3 lugares) que ainda usava só `'CONFIRMADO'` (bloqueava check-in de quem pagou via fluxo novo `APROVADO`).
- **Guia "Esconder por 14 dias"** — botão "Esconder" no header do guia incompleto. Volta a aparecer em 14d se ainda incompleto, ou some permanente se chegou em 100% (padrão Stripe/Linear/Slack).
- **#35 Polimento selo Asaas no login** — variante `mono` (oficial Asaas) + 100×30 (proporção 3.3:1 preservada) + microcopy "Pagamentos via". 100% dentro do Playbook BaaS.
- **#36 Auditoria selo Asaas** — verificação confirmou que está OK em todos os lugares. ProducerDashboard já removido em 2026-05-13. VendasIngressos mantém (listagem de cobrança = obrigatório). Template base de email já carrega selo.
- **#42 KYC banner "Complete sua verificação"** — migration `20260523` (`profiles.asaas_onboarding_url` + `asaas_kyc_status` + `asaas_kyc_checked_at`). `create-asaas-subconta` consulta `GET /myAccount/documents` após criar subconta e captura `onboardingUrl` + status. Banner amarelo em `/account-settings → Pagamentos` quando `kyc_status != 'APPROVED'` (NOT_SENT/PENDING/REJECTED distinguidos). CTA "Completar agora" abre `onboardingUrl` em nova aba.
- **EventPickerSheet custom** — novo `components/EventPickerSheet.tsx` substitui `<select>` nativo do Android (que vira bottom sheet ruim sem controle). Bottom sheet em mobile (handle visual + backdrop blur + Esc fecha) + dropdown ancorado em desktop. Search automática quando ≥6 eventos (Baymard Institute). Aplicado em `Registrations.tsx` e `ProducerDashboard.tsx`.

### 2026-05-20 (noite) — Bundle de bug fixes pós-smoke
Commit `25261dc`. 5 bugs reportados pelo user durante exploração do painel:
- **Filtros Inscrições mobile** — `MOSTRA` agora ao lado de `ESTILO` (par lógico "estilo+mostra"). `INSCRITO` move pro final em col-span-2.
- **Cupom — campo %** aceitava valores >100 (print mostrou `06494949494`). Clamp 0-100 no `onChange` quando `type=percent`.
- **Cupom — botão Salvar** ficava escondido atrás do teclado mobile. Troca `max-h-[92vh]` por `max-h-[92dvh]` (dynamic viewport).
- **Cupom — scope** expandido pra 6 opções via dropdown (Inscrição/Plateia/Workshop/Seletiva/Insc+Plat/Todos). Banco/RPCs já suportavam desde 2026-05-19/20, só faltava UI. `couponService.createCoupon` type estendido.
- **Sidebar mobile** — primeiro item (Painel) ficava escondido atrás do Header sticky `h-16`. Nav ganhou `pt-20` mobile.
- **Receita Usualdance zerada** (R$ 0 + 0 inscrições no dashboard) — bug do enum `status_pagamento`: `ProducerDashboard` filtrava `=== 'CONFIRMADO'` mas o valor real é `'APROVADO'`. `'CONFIRMADO'` é legacy. Fixado em 3 lugares: `ProducerDashboard.tsx`, `StageMarker.tsx`, `ProducerAlerts.tsx`. Lição: sempre usar helper `isPago(s) = s === 'APROVADO' || s === 'CONFIRMADO'` ao filtrar.

### 2026-05-20 — Settlement period D+7 ✅ SHIPADO + VALIDADO
Commits: `7fe91aa` → `08fdaa7` → `a13a164` (mais ajustes UX `afca00a`/`3893e49`/`021eb78`/`fae9c2a`). Mudança arquitetural grande:
- Webhook **deixou de fazer auto-saque imediato** (era o que causava o bug do refund pós-sweep).
- Saldo agora **fica retido 7 dias na subconta Asaas** (`platform_commissions.release_at = paid_at + 7d`).
- Cron diário `daily-release-funds` (00:00 BRT) libera no D+7+ via PIX.
- Botão "Transferir agora" no `/qg-organizador` antecipa via `manual-transfer-now` (modal de risco).
- Card `ProducerBalanceCard` unificado (padrão Stripe/Mercado Pago: 1 número grande + breakdown disponível/a-liberar).
- Termo do Produtor v1.1 → **v1.2** com cláusula 6 sobre janela D+7 e antecipação sob risco.
- Migrations: `20260521_settlement_period.sql` + `20260522_onboarding_dismissed.sql`.
- Guia do Produtor: etapa 4 "Compartilhar link" **removida** (redundante com header), etapas concluídas colapsam pra linha fina, card "Tudo pronto!" ganhou botão Fechar persistente.
- Smoke E2E em produção real: cenário 1 (settlement retém) + 1.5 (refund antes do D+7 funciona) + 2 (cron libera + refund pós-D+7 deixa subconta negativa) **TODOS PASSARAM**. Cenário 3 (botão) ainda pendente — opcional.
- **Insight descoberto:** Asaas não rejeita refund por saldo insuficiente — ele aceita e deixa a subconta negativa, debitando dos próximos créditos. Sem D+7, todo refund vira dívida não-consensual do produtor.

### 2026-05-19/20 — Carrinho fatura única (3 sessões)
Wizard cria registration PENDENTE, `/minhas-coreografias` agrupa, 1 PIX pra N inscrições. Sessão 1 = backend retro-compat, Sessão 2 = UI + Wizard refactor, Sessão 3 = auto-saque + banner KYC + fix timezone + send-payment-reminders + quick wins do painel. Todas validadas com PIX real.

### 2026-05-19 — Seletiva por vídeo v1
3 modelos cobertos (Modelo 1/2/3). Migration `20260604` + 3 edge functions novas (`create-video-selection-payment`, `process-video-refund`, `trigger-registration-payment`) + estendeu `asaas-webhook` com branch `VS:`. Multi-jurado tem infra de banco pronta, UI v1 só single producer (v1.1 backlog).

### 2026-05-18 — Aviso taxa subconta
Termo v1.0 → v1.1 com cláusula 5 (taxas Asaas, R$ 12,90 setup). 3 displays em `AccountSettings`.

### 2026-05-17 — 2FA super admin + Fase 4B pixels
MFA TOTP via Supabase Auth pro `/super-admin`. CAPI server-side Meta + GA4 MP, validação inline, fix bug duo.

### 2026-05-15 — Asaas em produção (marco BaaS)
BaaS aprovado, secrets prod configurados, webhook ativo, primeira subconta criada (Hemer KYC APPROVED). Pix nativo + boleto + cartão funcionando. Migrou 100% de Mercado Pago (legacy MP removido).

